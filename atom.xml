<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>GoDm@&#39;s Blog</title>
  
  <subtitle>记录自己所爱。</subtitle>
  <link href="https://blog.godmao.top/atom.xml" rel="self"/>
  
  <link href="https://blog.godmao.top/"/>
  <updated>2026-04-20T08:44:17.829Z</updated>
  <id>https://blog.godmao.top/</id>
  
  <author>
    <name>GoDm@</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>网络模型之上两层讲解</title>
    <link href="https://blog.godmao.top/posts/40289/"/>
    <id>https://blog.godmao.top/posts/40289/</id>
    <published>2026-04-20T07:01:02.000Z</published>
    <updated>2026-04-20T08:44:17.829Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><blockquote><p>对于我，如果不是想要深入计算机网络，只是做应用的话，上两层需要理解得更深一点，下三层只需了解即可</p></blockquote><pre class="line-numbers language-table-of-contents" data-language="table-of-contents"><code class="language-table-of-contents"><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="1-传输层"><a class="markdownIt-Anchor" href="#1-传输层"></a> 1. 传输层</h2><p>如果说网络层（IP层）解决的是“如何把数据从主机 A 送到主机 B”，那么传输层解决的就是“如何把数据从主机 A 的某个程序，送达主机 B 的另一个特定程序”。这被称为<strong>端到端（进程到进程）</strong> 的通信。</p><h3 id="11-传输层的核心作用"><a class="markdownIt-Anchor" href="#11-传输层的核心作用"></a> 1.1. 传输层的核心作用</h3><ul><li><p><strong>端口寻址（Port Addressing）：</strong> 一台服务器上可能同时运行着 Web 服务器、数据库、SSH 后台服务。传输层通过**端口号（Port Number，0-65535）**来区分这些不同的应用程序。比如，默认情况下 HTTP 使用 80 端口，HTTPS 使用 443 端口，MQTT 通常使用 1883 端口。</p></li><li><p><strong>分段与重组：</strong> 应用层丢下来的数据往往很大（比如一个固件更新包），传输层会将其切割成网络能够承受的小块（报文段/数据报），并在接收端将它们按顺序拼装回原样。</p></li><li><p><strong>可靠性控制：</strong> 它可以决定是否要对丢失的数据进行重传，是否要控制发送的速度以免接收方处理不过来。</p></li></ul><h3 id="12-传输层协议tcp和udp"><a class="markdownIt-Anchor" href="#12-传输层协议tcp和udp"></a> 1.2. 传输层协议TCP和UDP</h3><p>传输层最主要的两个协议。它们具有两种不同的设计哲学。</p><h4 id="121-tcp传输控制协议transmission-control-protocol"><a class="markdownIt-Anchor" href="#121-tcp传输控制协议transmission-control-protocol"></a> 1.2.1. TCP（传输控制协议，Transmission Control Protocol）</h4><ul><li><p><strong>特点：</strong> 面向连接、极其可靠、保证顺序、有流量控制和拥塞控制。</p></li><li><p><strong>机制：</strong> 在发送数据前，双方必须先进行著名的“三次握手”建立连接。如果在传输过程中有数据包丢失，TCP 会自动要求对方重传。</p></li><li><p><strong>适用场景：</strong> 绝不能容忍数据丢失或错乱的场景。比如：网页浏览、文件传输（FTP）、设备的 OTA 固件升级、以及物联网设备上报关键遥测数据的 MQTT 协议，底层无一例外都是基于 TCP 的。</p></li></ul><h4 id="122-udp用户数据报协议user-datagram-protocol"><a class="markdownIt-Anchor" href="#122-udp用户数据报协议user-datagram-protocol"></a> 1.2.2. UDP（用户数据报协议，User Datagram Protocol）**</h4><ul><li><p><strong>特点：</strong> 无连接、不可靠、尽力而为、轻量且速度极快。</p></li><li><p><strong>机制：</strong> 没有握手，没有确认，也不管接收方能不能处理得过来，直接把数据“扔”过去。如果丢了，那就丢了。</p></li><li><p><strong>适用场景：</strong> 对实时性要求极高、能容忍少量丢包的场景。比如：视频流媒体、在线游戏。在嵌入式开发中，局域网内的快速设备发现（如 UDP 广播）、轻量级的 CoAP 协议，或者 NTP 网络时间同步，通常会选择 UDP 来节省系统开销。</p></li></ul><h3 id="13-补充1tcp的三次握手四次挥手"><a class="markdownIt-Anchor" href="#13-补充1tcp的三次握手四次挥手"></a> 1.3. 补充1：TCP的“三次握手”“四次挥手”</h3><p>TCP为了建立可靠的数据连接，会在连接时进行“三次握手”，在断开连接时“四次挥手”。</p><h4 id="131-三次握手"><a class="markdownIt-Anchor" href="#131-三次握手"></a> 1.3.1. “三次握手”</h4><p>当你调用 Socket API 的 <code>connect()</code> 函数时，底层网络栈就开始执行三次握手。其核心目的是：<strong>双方互换初始序列号（Sequence Number，简称 seq），并确认双方的收发能力都正常。</strong></p><p>为了方便理解，我们把主动发起连接的设备称为<strong>客户端（Client）</strong>，等待连接的称为<strong>服务端（Server）</strong>。</p><ul><li><p><strong>第一次握手：客户端“抛出橄榄枝”</strong></p><ul><li><p><strong>动作：</strong> 客户端向服务端发送一个带有 <strong>SYN（Synchronize，同步）</strong> 标志位的报文。</p></li><li><p><strong>内容：</strong> “我想和你建立连接。我的初始数据序列号是 <code>x</code>。”</p></li><li><p><strong>状态变化：</strong> 客户端进入 <code>SYN_SENT</code> 状态。</p></li><li><p><em>目的：</em> 测试服务端的接收能力。</p></li></ul></li><li><p><strong>第二次握手：服务端“确认并反向抛出橄榄枝”</strong></p><ul><li><p><strong>动作：</strong> 服务端收到 SYN 报文后，同意连接，并向客户端发送一个同时带有 <strong>SYN</strong> 和 <strong>ACK（Acknowledgment，确认）</strong> 标志位的报文。</p></li><li><p><strong>内容：</strong> “你的请求我收到了（ACK，确认号为 <code>x+1</code>）。我也想和你建立连接，我的初始序列号是 <code>y</code>（SYN）。”</p></li><li><p><strong>状态变化：</strong> 服务端进入 <code>SYN_RCVD</code> 状态。</p></li><li><p><em>目的：</em> 确认客户端的发送能力和自己的接收能力正常，同时测试客户端的接收能力。</p></li></ul></li><li><p><strong>第三次握手：客户端“最终确认”</strong></p><ul><li><p><strong>动作：</strong> 客户端收到服务端的 SYN+ACK 报文后，向服务端发送一个带有 <strong>ACK</strong> 标志位的报文。</p></li><li><p><strong>内容：</strong> “你的请求我也收到了（ACK，确认号为 <code>y+1</code>），我们开始传输数据吧！”</p></li><li><p><strong>状态变化：</strong> 客户端进入 <code>ESTABLISHED</code>（已建立连接）状态。服务端收到此报文后，也进入 <code>ESTABLISHED</code> 状态。</p></li><li><p><em>目的：</em> 确认自己的接收能力和服务端的发送能力正常。</p></li></ul></li></ul><h4 id="132-四次挥手"><a class="markdownIt-Anchor" href="#132-四次挥手"></a> 1.3.2. “四次挥手”</h4><p>当你调用 <code>close()</code> 函数准备关闭 Socket 时，底层执行的是四次挥手。TCP 是<strong>全双工通信</strong>（数据可以同时双向流动），这就意味着<strong>关闭连接也必须是双向独立的</strong>。一方不想发数据了，不代表另一方也没数据要发了。</p><p>任何一方都可以主动发起挥手，我们假设还是客户端先发起。</p><ul><li><p><strong>第一次挥手：客户端“我发完了”</strong></p><ul><li><p><strong>动作：</strong> 客户端发送一个带有 <strong>FIN（Finish，结束）</strong> 标志位的报文。</p></li><li><p><strong>内容：</strong> “我的数据都发完了，我不会再给你发应用层数据了（但我还能接收）。”</p></li><li><p><strong>状态变化：</strong> 客户端进入 <code>FIN_WAIT_1</code> 状态。</p></li></ul></li><li><p><strong>第二次挥手：服务端“我知道你发完了”</strong></p><ul><li><p><strong>动作：</strong> 服务端收到 FIN 报文后，立刻回复一个 <strong>ACK</strong> 报文。</p></li><li><p><strong>内容：</strong> “收到了你的关闭请求。但是稍等，我可能还有一些数据没处理完/没发完。”</p></li><li><p><strong>状态变化：</strong> 服务端进入 <code>CLOSE_WAIT</code> 状态；客户端收到后进入 <code>FIN_WAIT_2</code> 状态。</p></li><li><p><em>注意：此时 TCP 连接处于“半关闭状态”，客户端到服务端的通道关闭了，但服务端到客户端的通道还在开启。</em></p></li></ul></li><li><p><strong>第三次挥手：服务端“我也发完了”</strong></p><ul><li><p><strong>动作：</strong> 等服务端把手头剩下的数据都发送完毕后，它向客户端发送带有 <strong>FIN</strong> 标志位的报文。</p></li><li><p><strong>内容：</strong> “我的数据也全发完了，可以彻底断开了。”</p></li><li><p><strong>状态变化：</strong> 服务端进入 <code>LAST_ACK</code> 状态。</p></li></ul></li><li><p><strong>第四次挥手：客户端“彻底再见”</strong></p><ul><li><p><strong>动作：</strong> 客户端收到 FIN 后，回复一个 <strong>ACK</strong> 报文。</p></li><li><p><strong>内容：</strong> “收到，彻底再见。”</p></li><li><p><strong>状态变化：</strong> 服务端收到 ACK 后立即进入 <code>CLOSED</code> 状态。而客户端则会进入一个特殊的 <strong><code>TIME_WAIT</code></strong> 状态，等待一段固定的时间（通常是 2 个 MSL，最长报文段寿命）后，才会最终进入 <code>CLOSED</code> 状态，释放 Socket 资源。</p></li></ul></li></ul><h4 id="133-为什么建立是三次断开却是四次"><a class="markdownIt-Anchor" href="#133-为什么建立是三次断开却是四次"></a> 1.3.3. 为什么建立是三次，断开却是四次？</h4><ul><li><p><strong>建立连接时（三次）：</strong> 服务端收到客户端的 <code>SYN</code> 连接请求时，它什么包袱都没有，可以直接把**同意连接（ACK）<strong>和</strong>自己也要连接（SYN）**这两个动作合并在一个数据包里（<code>SYN+ACK</code>）发过去，因此只需要三次。</p></li><li><p><strong>断开连接时（四次）：</strong> 客户端发来 <code>FIN</code> 表示它要关了，但此时<strong>服务端很可能还在忙着处理上层的业务逻辑，或者发送缓冲区里还有没发完的数据</strong>。服务端只能先回一个 <code>ACK</code>（表示“我收到了你的断开请求”），然后继续把没发完的数据发完。等彻底发完后，服务端才能发送自己的 <code>FIN</code>（表示“我也准备好断开了”）。<strong>因为 <code>ACK</code> 和 <code>FIN</code> 之间存在时间差（要等应用层代码把数据处理完），所以无法合并，必须分两次发送。</strong></p></li></ul><h3 id="14-补充2udp的通信"><a class="markdownIt-Anchor" href="#14-补充2udp的通信"></a> 1.4. 补充2：UDP的通信</h3><p>UDP是无连接的协议。</p><ul><li><p><strong>发送方：</strong> 不需要调用 <code>connect()</code>。创建好 Socket 后，直接调用 <code>sendto()</code> 函数。在这个函数里，你必须<strong>每一次都显式地附带上目标 IP 和目标端口</strong>，然后把数据发出去。</p></li><li><p><strong>接收方：</strong> 不需要调用 <code>listen()</code> 和 <code>accept()</code> 去等待连接。只需创建一个 Socket，绑定本地端口，然后死循环调用 <code>recvfrom()</code> 阻塞在那儿。任何从网络上飞过来的、只要目标端口匹配的 UDP 包，都会被内核收下来丢给这个程序。</p></li></ul><blockquote><p>UDP把极致的速度和灵活性交给了开发者。代价就是开发者必须自己承担处理网络不确定性的风险。如果你使用 UDP，但又想保证数据不丢，你必须在自己的应用层代码里实现一套简单的确认和重传机制。</p></blockquote><h2 id="2-应用层"><a class="markdownIt-Anchor" href="#2-应用层"></a> 2. 应用层</h2><p>应用层（Application Layer）位于最顶层。它是整个网络体系结构中唯一直接与应用程序和终端用户交互的一层。</p><p>如果说<strong>底下的四层解决了“如何将数据从A点可靠/不可靠地搬运到B点”的底层硬件通信和路由问题</strong>，那么应用层解决的就是“这些数据代表什么，以及应用程序应该如何去理解和处理这些数据”。可以说，如果没有应用层，通信双方收到的只是一连串毫无意义的 <code>0</code> 和 <code>1</code> 二进制流。</p><h3 id="21-应用层的核心作用"><a class="markdownIt-Anchor" href="#21-应用层的核心作用"></a> 2.1. 应用层的核心作用</h3><ul><li><p><strong>定义数据交互规则：</strong> 规定了通信双方的应用程序如何交互信息。例如，定义了请求报文和响应报文的格式、字段的含义，以及何时发送和如何处理这些报文。</p></li><li><p><strong>提供网络接口：</strong> 提供标准化的接口（如 Socket API），让开发者编写的程序能够调用底层网络功能，而无需关心复杂的网卡驱动或 TCP/IP 状态机。</p></li><li><p><strong>数据序列化与呈现：</strong> 决定了数据在网络传输前的组织形式（如 JSON、XML、Protocol Buffers 或是纯文本）。</p></li></ul><h3 id="22-常见的应用层协议"><a class="markdownIt-Anchor" href="#22-常见的应用层协议"></a> 2.2. 常见的应用层协议</h3><p>应用层包含了数量最庞大、种类最繁多的协议，以满足各种不同的业务需求：</p><ul><li><p><strong>Web 与文件传输：</strong></p><ul><li><p><strong>HTTP/HTTPS:</strong> 网页浏览的基石，采用请求-响应模型。</p></li><li><p><strong>FTP:</strong> 用于在服务器和客户端之间传输文件。</p></li></ul></li><li><p><strong>网络基础服务：</strong></p><ul><li><p><strong>DNS (Domain Name System):</strong> 互联网的“电话簿”，负责将人类可读的域名（如 <code>google.com</code>）解析为机器通信所需的 IP 地址。</p></li><li><p><strong>DHCP:</strong> 动态主机配置协议，自动为网络中的设备分配 IP 地址。</p></li></ul></li><li><p><strong>物联网 (IoT) 与嵌入式协议：</strong></p><ul><li><p><strong>MQTT (Message Queuing Telemetry Transport):</strong> 一种轻量级的发布/订阅消息传输协议。</p></li><li><p><strong>CoAP:</strong> 专为资源受限的设备（如低功耗单片机）设计的类 HTTP 协议，通常基于 UDP 运行，开销极小。</p></li></ul></li></ul><h3 id="23-应用层在嵌入式开发者中的视角"><a class="markdownIt-Anchor" href="#23-应用层在嵌入式开发者中的视角"></a> 2.3. 应用层在嵌入式开发者中的视角</h3><p>从底层系统或 RTOS 开发的视角来看，应用层的界限非常清晰：</p><p>当你在开发一个带网络功能的嵌入式系统时，底层通常由芯片厂商提供的网络协处理器或软件协议栈（如 LwIP）来包揽物理层到传输层（MAC/PHY 控制、IP 路由、TCP/UDP 封装）。而应用层的工作就是你手写的代码逻辑。</p><ol><li><p><strong>打包（发送方向）：</strong> 将裸数据（如传感器采集的 ADC 原始值或计算后的浮点数）按照指定的格式（比如 JSON 字符串）拼接好，加上特定协议（如 MQTT）的固定报头（Header），然后调用类似 <code>send()</code> 或 <code>write()</code> 的 Socket 函数，把这坨内存数据丢给传输层。</p></li><li><p><strong>解析（接收方向）：</strong> 从 Socket 缓冲区里 <code>recv()</code> 读出一条字节流。底层只保证这是无误的字节，而你的应用层代码需要去解析这段字节——提取命令、校验应用层业务逻辑，最后驱动硬件执行动作（比如控制继电器或调节 PID 参数）。</p></li></ol><h3 id="24-应用层和传输层之关系"><a class="markdownIt-Anchor" href="#24-应用层和传输层之关系"></a> 2.4. 应用层和传输层之关系</h3><ol><li><p>服务提供者与消费者的关系 (Socket 接口)</p><p>传输层是<strong>服务提供者</strong>，应用层是<strong>服务消费者</strong>。 它们之间的物理/代码界限通常就是 <strong>Socket API（套接字接口）</strong>。当你在应用层写代码时（无论是 Python 脚本还是 C 语言的嵌入式程序），你不需要手写 TCP 三次握手的逻辑。你只需要调用 <code>socket()</code> 创建一个套接字，指定它是 TCP 还是 UDP，然后调用 <code>connect()</code>、<code>send()</code> 和 <code>recv()</code>。操作系统内核（或者 RTOS 中的 LwIP 协议栈）会在底层自动帮你处理复杂的传输层状态机。</p></li><li><p>数据封装的关系</p><p>当应用层产生数据（Payload）并准备发送时，数据会向下传递给传输层。传输层不会关心应用层的数据是什么内容（无论它是 JSON 还是加密的二进制码），它只把这些数据当成一个整体。传输层会在这些数据前面加上自己的<strong>报头（Header）</strong>，其中最关键的就是<strong>源端口号</strong>和<strong>目标端口号</strong>。</p><ul><li><p>应用层数据 + TCP 报头 = <strong>TCP 报文段（Segment）</strong>。</p></li><li><p>接收端则是一个逆向的“拆快递”过程：传输层剥离 TCP 报头，检查端口号，然后将里面的“纯应用层数据”顺着 Socket 丢给对应的应用程序去解析。</p></li></ul></li></ol><h2 id="3-socket-套接字概述"><a class="markdownIt-Anchor" href="#3-socket-套接字概述"></a> 3. Socket 套接字概述</h2><p>前面我们聊了应用层和传输层，也提到了它们之间的桥梁就是 <strong>Socket（套接字）</strong>。现在我们就简单了解一下套接字。</p><h3 id="31-什么是套接字"><a class="markdownIt-Anchor" href="#31-什么是套接字"></a> 3.1. 什么是套接字？</h3><p>“Socket” 这个词的本义是“插座”。想象一下你家墙上的电源插座：插座背后隐藏着极其复杂的国家电网（发电厂、变电站、高压线），但作为用户，你完全不需要懂电力工程，你只需要把电器的插头（Plug）插进插座，就能通电。</p><p>在网络世界里也是一样：</p><ul><li><p><strong>国家电网</strong> = 复杂的 TCP/IP 协议栈（三次握手、拥塞控制、IP 路由）。</p></li><li><p><strong>插座 (Socket)</strong> = 操作系统内核提供给程序员的一组标准化 API。</p></li></ul><p>通过这组 API，你的应用程序就可以把数据“插”进网络中，或者从网络中“拔”出数据，而无需手写底层的协议控制代码。</p><h3 id="32-一切皆文件"><a class="markdownIt-Anchor" href="#32-一切皆文件"></a> 3.2. 一切皆文件</h3><p>你一定知道 Unix/Linux 的核心哲学：<strong>一切皆文件（Everything is a file）</strong>。</p><p>Socket 完美地贯彻了这一哲学。在内核层面，<strong>Socket 其实就是一个特殊的文件。</strong></p><p>当你调用 <code>socket()</code> 函数创建一个网络连接时：</p><ol><li><p>操作系统内核会为你分配一小块内存（通常包含发送缓冲区和接收缓冲区），并维护这个连接的各种状态（比如 TCP 的 <code>ESTABLISHED</code> 状态）。</p></li><li><p>内核会向你的应用程序返回一个 <strong>文件描述符（File Descriptor，简称 fd，本质上就是一个整数索引）</strong>。</p></li></ol><p>从这一刻起，在代码层面，操作网络就变得和操作普通的本地文件，或者是操作一个 UART 串口、一个 I2C 设备节点没有任何区别了。</p><ul><li><p>你想发数据？调用 <code>write(fd, buffer, length)</code> 或者 <code>send()</code>，把字节流写进这个“文件”。内核会自动把这些字节拿走，切片、加 TCP 头、加 IP 头，最后扔给网卡硬件（MAC/PHY）发出去。</p></li><li><p>你想收数据？调用 <code>read(fd, buffer, length)</code> 或者 <code>recv()</code>，从这个“文件”里读取。如果网卡没收到数据，你的当前任务/线程就会被内核挂起（阻塞等待），直到网卡触发中断，内核把数据剥离头部后放进 Socket 缓冲区，再唤醒你的程序去读。</p></li></ul><h3 id="33-socket-必要的参数"><a class="markdownIt-Anchor" href="#33-socket-必要的参数"></a> 3.3. Socket 必要的参数</h3><p>如何确定一个独一无二的 Socket 通道？内核通过一个经典的 <strong>“五元组”</strong> 来精确定位一个 Socket 通信：</p><ol><li><p><strong>协议（Protocol）：</strong> TCP 还是 UDP？</p></li><li><p><strong>本地 IP 地址：</strong> 你的设备可能有多张网卡（比如同时连着 Wi-Fi 和以太网），走哪个 IP？</p></li><li><p><strong>本地端口（Local Port）：</strong> 你的应用在哪个端口？</p></li><li><p><strong>目标 IP 地址：</strong> 数据要发给哪台机器？</p></li><li><p><strong>目标端口（Remote Port）：</strong> 对方机器上的哪个程序来接这个数据？</p></li></ol><p>只要这五个参数确定了，一条端到端的逻辑数据线就确立了。</p><h3 id="34-socket-的生命周期"><a class="markdownIt-Anchor" href="#34-socket-的生命周期"></a> 3.4. Socket 的生命周期</h3><p>以Berkeley Sockets（BSD Sockets）标准接口为例。我们以一个 TCP 服务端为例，看看它的全生命周期：</p><ol><li><p><strong><code>socket()</code>：买个新插座</strong></p><p>向内核申请创建一个指定协议（TCP/UDP）的 Socket 文件描述符。</p></li><li><p><strong><code>bind()</code>：把插座钉在墙上</strong></p><p>把这个 Socket 和某个特定的本地 IP 及端口号绑定起来。意思是：“我就守在这个端口听消息了。”</p></li><li><p><strong><code>listen()</code>：通电并开始接客（仅限 TCP）</strong></p><p>告诉内核，这个 Socket 不是用来主动发起连接的，而是用来被动等待别人连接的。内核会为此准备一个等待队列。</p></li><li><p><strong><code>accept()</code>：诞生一个“分身”插座</strong></p><p>这是服务端最核心的一步。当客户端的三次握手完成时，<code>accept()</code> 会被唤醒。<strong>注意：它不会用原来的那个 Socket 和客户端通信，而是由内核动态“克隆”出一个全新的 Socket 文件描述符（专用于跟这个客户端聊天），而最初的那个 Socket 继续回去监听新客户。</strong></p></li><li><p><strong><code>send()</code> / <code>recv()</code>：读写数据</strong></p><p>在应用层缓冲区和内核 Socket 缓冲区之间搬运字节流。</p></li><li><p><strong><code>close()</code>：拔掉插头，拆毁插座</strong></p><p>触发四次挥手，释放内核中的控制块（TCB）内存，回收文件描述符。</p></li></ol><h3 id="35-mcusoc上的socket"><a class="markdownIt-Anchor" href="#35-mcusoc上的socket"></a> 3.5. MCU/SoC上的Socket</h3><p>在资源受限的嵌入式系统中（比如跑在微控制器 MCU 上的 RTOS），由于内存极其有限，通常跑不动完整的 Linux 庞大网络栈。</p><p>这个时候，通常会使用轻量级的网络协议栈（比如最著名的 <strong>LwIP</strong>）。LwIP 在底层精简了大量的内存拷贝操作，但为了方便开发者，它依然在最上层封装了一套与标准 BSD Sockets 几乎一模一样的 API。</p><h3 id="36-总结"><a class="markdownIt-Anchor" href="#36-总结"></a> 3.6. 总结</h3><p>无论你是写一个庞大的后端服务器，还是仅仅想让一个小小的芯片通过传感器采集数据并推送到云端，<strong>Socket 就是那个屏蔽了所有底层硬件交互、网络路由和协议状态机的“超级隔离带”</strong>。你只要会读写 Socket 这个特殊的“文件”，就能玩转整个网络通信。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="网络" scheme="https://blog.godmao.top/categories/%E7%BD%91%E7%BB%9C/"/>
    
    
  </entry>
  
  <entry>
    <title>计算机网络及其五层模型概述</title>
    <link href="https://blog.godmao.top/posts/50865/"/>
    <id>https://blog.godmao.top/posts/50865/</id>
    <published>2026-04-15T11:24:33.000Z</published>
    <updated>2026-04-17T13:52:36.609Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><blockquote><p>本篇仅作为我学习MQTT的前置知识学习，并不是专业讲解。</p></blockquote><h2 id="1-计算机的通信从网线开始"><a class="markdownIt-Anchor" href="#1-计算机的通信从网线开始"></a> 1. 计算机的通信——从网线开始</h2><h3 id="11-网线"><a class="markdownIt-Anchor" href="#11-网线"></a> 1.1. 网线</h3><p>如果两台计算机想要通信，最简单的方法就是连接一根网线。</p><h3 id="12-集线器"><a class="markdownIt-Anchor" href="#12-集线器"></a> 1.2. 集线器</h3><p>如果计算机太多了，每台计算机都要和其他计算机之间连一根网线，这样的话网线就会显得错综复杂。</p><p>于是我们使用集线器，把所有网线都插在集线器上，由集线器把数据包广播出去——它仅仅是把数据包转发到所有出口，没有任何处理。</p><p>所以集线器的缺点显而易见：</p><ol><li>信息会发送给所有设备。</li><li>多台设备同时发送消息会有干扰。</li></ol><h3 id="13-交换机"><a class="markdownIt-Anchor" href="#13-交换机"></a> 1.3. 交换机</h3><p>交换机则是比集线器更智能的设备，交换机通过内部的MAC地址表来决定信息流向，接入交换机的设备都必须要有自己的MAC地址，MAC地址是设备出厂就设定好的、唯一的。</p><p>在一开始，由于MAC地址表是空的，交换机和集线器没有什么区别。</p><p>当设备A想要发送数据给设备B时，设备A在自己的发送的数据包的头部填入自己的MAC地址和目标MAC地址（设备B）。</p><p>当数据流经交换机时，交换机能识别出 设备A 的MAC地址并记录到 地址-接口映射表 中。由于交换机并不知道 设备B 的地址，它也会像集线器那样把数据广播给所有设备，设备解包后发现目标MAC地址与自己MAC地址不一样，选择不回应；而设备B发现目标MAC地址和自己的MAC地址一样，则回应，回应消息也要包含 自己的MAC地址 和 目标MAC地址（设备A），此时设备B的MAC地址被交换机更新到 地址-接口映射表。</p><p>此后如果再发送数据，则可以直接从A到B，不再广播数据。</p><blockquote><p>这个广播数据的过程叫做“泛洪”（flooding）。</p></blockquote><h3 id="14-ip地址"><a class="markdownIt-Anchor" href="#14-ip地址"></a> 1.4. IP地址</h3><p>MAC地址是与设备绑定的，如果更换网卡，MAC地址就变更了，交换机里关于该设备的记录就全部作废了。因此我们引入了IP地址。IP地址可以看作是MAC地址的抽象。通过IP地址如何通信呢？</p><p>同样以A向B发送消息为例。</p><ul><li>A发送数据——（自己的IP地址、自己的MAC地址、目标IP地址）+数据</li><li>交换机广播数据（转发到所有接口）</li><li>其他设备发现IP地址与自己的IP不符，不回应</li><li>设备B匹配到IP地址，把数据包中包含的 设备A的MAC地址 和 设备A的IP地址 绑定起来。</li><li>设备B发送回应消息——（自己的IP地址、自己的MAC地址、目标IP地址）+数据包</li><li>设备A收到回应，把收到的数据包中包含的 设备B的MAC地址 和设 备B的IP地址 绑定起来。</li><li>整个过程中，交换机也记录下了两台设备的MAC地址和对应端口。</li><li>知道了设备B的MAC地址，下次发消息，A就可以通过MAC地址配合交换机直达了。</li></ul><p>这个由IP地址获得MAC地址的过程就是<strong>ARP协议</strong>，MAC地址与IP绑定的表就是ARP记录表。</p><h3 id="15-子网掩码"><a class="markdownIt-Anchor" href="#15-子网掩码"></a> 1.5. 子网掩码</h3><p>由于交换机的泛洪特性，我们可以把多台交换机串联起来，组成一个较大的计算机网络，可是再大也有极限，由于交换机会把所有经过的MAC地址保存起来，如果设备数量巨多，MAC地址表总有存不下的时候。</p><p>于是我们将网络隔离开来，将网络分成不同的网段。交换机只处理同一网段的消息。<br />这里就引入了子网掩码的概念。简单来讲，子网掩码就是用来判断两个IP是否属于同一网段的。IP地址和子网掩码作与运算会生成子网ID，子网ID相同的IP地址在同一网段。</p><p>例如：</p><table><thead><tr><th>子网掩码</th><th>IP地址</th><th>子网掩码与IP地址做与运算</th></tr></thead><tbody><tr><td>255.255.255.0</td><td>192.168.1.1</td><td>192.168.1.0</td></tr><tr><td>255.255.255.0</td><td>192.168.1.2</td><td>192.168.1.0</td></tr><tr><td>255.255.255.0</td><td>192.168.2.1</td><td>192.168.2.0</td></tr></tbody></table><p>192.168.1.1 和 192.168.1.2 就在同一网段。192.168.2.0 对于前两者来说就是外网。</p><p>同样的情况下，如果子网掩码改为 <code>255.255.0.0</code> 计算机则会判断只要是 192.168.x.x 的IP的地址都在同一网段。</p><h3 id="16-路由器"><a class="markdownIt-Anchor" href="#16-路由器"></a> 1.6. 路由器</h3><p>前面提到交换机只处理内网的消息（同一网段），那内网发往外网的消息呢？路由器登场了！路由器的每个端口都有一个独立的MAC地址。</p><p>在内网的交换机的端口上会连着一个或多个路由器，它就是通往外界的桥梁。</p><p>当一台设备通过子网掩码得知消息要发给外网时，只需将消息通过交换机发给路由器即可。</p><p>设备之所以知道要交给哪个路由器，是因为设备的<strong>网关</strong>配置的是路由器的IP。网关，顾名思义就是网络的关口，向外网传达的消息都经过网关出去。</p><p>路由器收到消息后，下一步又把消息交给谁？路由器内维护一个路由表，可以配置“网段”和“下一跳”，用来决定各网段的消息下一步应该交由哪个路由器，这样各个路由器就可以通过接力的方式传到到目标网段的目标计算机。</p><p>路由表是手动配置的，路由设备增多，人工维护就会变得困难，于是有了 <strong>OSPF</strong>、<strong>BGF</strong> 等协议，能自动学习，自动管理路由表。</p><h3 id="17-总结"><a class="markdownIt-Anchor" href="#17-总结"></a> 1.7. 总结</h3><p>当发送信息时：</p><ul><li><p><strong>电脑视角</strong></p><ol><li>首先我要知道我的IP和对方的IP</li><li>通过子网掩码判断目标设备是否在同一网段</li><li>是，则直接通过交换机，配合ARP协议，使用MAC地址传输。</li><li>否，通过交换机，配合ARP协议，使用MAC地址发给网关。</li></ol></li><li><p><strong>交换机视角</strong></p><ol><li>收到数据包（必须包含目标MAC地址）</li><li>查MAC地址表</li><li>有记录，则直接转发给指定端口</li><li>无记录，则泛洪。</li></ol></li><li><p><strong>路由器视角</strong></p><ol><li>收到数据包（必须包含目标IP地址）</li><li>查路由表</li><li>有记录，则转发给指定端口</li><li>无记录，则返回一个路由不可达数据包</li></ol></li></ul><h2 id="2-网络的五层模型"><a class="markdownIt-Anchor" href="#2-网络的五层模型"></a> 2. 网络的五层模型</h2><p>网络是一个及其复杂的系统，功能十分繁杂，为了实现这个系统，我们需要采用分层的思想，分而治之。</p><p>我们就来看一下网络的经典五层模型，从上至下——应用层、传输层、网络层、数据链路层、物理层。</p><p><img src="Snipaste_2026-04-15_21-27-57.png" alt="" /></p><p>我们先来讲下面的三层——网络层、数据链路层和物理层。</p><p>结合第一节讲的内容，看看数据是如何在这个模型里流动的。还是一样的例子，设备A发送数据给设备B</p><p><img src="Snipaste_2026-04-15_22-55-16.png" alt="" /></p><h3 id="21-物理层"><a class="markdownIt-Anchor" href="#21-物理层"></a> 2.1. 物理层</h3><p><strong>负责数字信号与模拟信号的转换。</strong></p><p><strong>假设我们的物理层设备是网口。</strong></p><p>图中，设备A中的物理层将经过层层封装的数据包（比特流）转化为网口的差分信号并传输到交换机的物理层去。交换机的物理层接收到信号后又把它转为数字信号提供给数据链路层。</p><h3 id="22-数据链路层"><a class="markdownIt-Anchor" href="#22-数据链路层"></a> 2.2. 数据链路层</h3><p><strong>负责相邻网络节点间的数据传输。点对点。</strong></p><p>图中，交换机通过数据链路层解析出”MAC头“，配合MAC地址表，确定要发送到哪个端口（路由器端口），然后将数据包重新封装好，通过物理层发送给路由器。</p><h3 id="23-网络层"><a class="markdownIt-Anchor" href="#23-网络层"></a> 2.3. 网络层</h3><p><strong>负责为数据从源到目的选择路由。端到端。</strong></p><p>图中，路由器接收到交换机的比特流，通过数据链路层解析出MAC地址，通共比对解析出的MAC地址确认数据是发送给自己的，然后通过网络层解析出IP，通过路由表确定要发往的下一个节点，封装好IP（<strong>注意IP地址始终保持不变</strong>），此时又进入数据链路层，此时会修改MAC头部——源MAC变为自己的出口MAC，目的MAC变为下一个路由器的入口MAC。然后重新将数据包封装好，通过物理层发送出去。</p><h3 id="24-上层-下层与同层间的联系"><a class="markdownIt-Anchor" href="#24-上层-下层与同层间的联系"></a> 2.4. 上层、下层与同层间的联系</h3><p>下层为上层封装好接口，为上层提供服务。</p><p>两个设备的同层之间依靠通信协议传输数据，然后逐层向上，通过下层提供的服务解析出信息；两个设备的不同层之间不能传递数据。</p><p>至于传输层和应用层，且听下回分解。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="网络" scheme="https://blog.godmao.top/categories/%E7%BD%91%E7%BB%9C/"/>
    
    
  </entry>
  
  <entry>
    <title>FreeRTOS的内部机制5</title>
    <link href="https://blog.godmao.top/posts/29966/"/>
    <id>https://blog.godmao.top/posts/29966/</id>
    <published>2026-04-12T14:12:24.000Z</published>
    <updated>2026-04-14T15:09:46.002Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><p>本节我们正式开始讲解进程间通信的内容。首先要讲的就是队列了，为什么要先讲队列呢？因为信号量和互斥量都会复用队列的代码，所以先讲队列又利于我们后面的学习。</p><p>要想理解队列，只需理解三部分内容——环形缓冲区（存储数据）和链表（任务管理）</p><h2 id="1-队列详解"><a class="markdownIt-Anchor" href="#1-队列详解"></a> 1. 队列详解</h2><h3 id="11-队列结构体"><a class="markdownIt-Anchor" href="#11-队列结构体"></a> 1.1 队列结构体</h3><p>在 <code>queue.c</code> 中，我们可以找到队列的核心控制块 <code>Queue_t</code>（为了清晰，我精简了结构）：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">typedef</span> <span class="token keyword">struct</span> <span class="token class-name">QueueDefinition</span><span class="token punctuation">&#123;</span>    <span class="token class-name">int8_t</span> <span class="token operator">*</span>pcHead<span class="token punctuation">;</span>           <span class="token comment">/* 指向队列存储区的起始位置 */</span>    <span class="token class-name">int8_t</span> <span class="token operator">*</span>pcWriteTo<span class="token punctuation">;</span>        <span class="token comment">/* 下一个要写入的位置 */</span>        <span class="token keyword">union</span> <span class="token punctuation">&#123;</span>        QueuePointers_t xQueue<span class="token punctuation">;</span> <span class="token comment">/* 包含 pcReadFrom 指针，指示下一个要读取的位置 */</span>        <span class="token comment">// ... (省略了互斥量特有的变量)</span>    <span class="token punctuation">&#125;</span> u<span class="token punctuation">;</span>    <span class="token comment">/* 核心：两个链表 */</span>    List_t xTasksWaitingToSend<span class="token punctuation">;</span>    <span class="token comment">/* 等待发送链表：当队列满了，想塞数据的任务在这排队 */</span>    List_t xTasksWaitingToReceive<span class="token punctuation">;</span> <span class="token comment">/* 等待接收链表：当队列空了，想拿数据的任务在这排队 */</span>    <span class="token keyword">volatile</span> UBaseType_t uxMessagesWaiting<span class="token punctuation">;</span> <span class="token comment">/* 队列里现在有几个数据 */</span>    UBaseType_t uxLength<span class="token punctuation">;</span>                   <span class="token comment">/* 队列总共能装几个数据 */</span>    UBaseType_t uxItemSize<span class="token punctuation">;</span>                 <span class="token comment">/* 每个数据有多大 (比如 sizeof(int)) */</span><span class="token punctuation">&#125;</span> xQUEUE<span class="token punctuation">;</span><span class="token keyword">typedef</span> xQUEUE Queue_t<span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="12-环形缓冲区"><a class="markdownIt-Anchor" href="#12-环形缓冲区"></a> 1.2 环形缓冲区</h3><p>队列使用的数据存取结构是环形缓冲区（Circular Buffer，也叫 Ring Buffer）。经典的数据结构，完美解决了两个问题：<strong>内存固定（不产生碎片）</strong> 和 <strong>极速读写（时间复杂度永远是 O(1)）</strong>。</p><p><strong>一个标准的环形缓冲区只需要四个核心元素</strong>：</p><ol><li><p><strong>一块连续的内存</strong>（比如一个数组 <code>buffer[SIZE]</code>）。</p></li><li><p><strong>读指针（Head / ReadIndex）</strong>：指向下一个要被读取的数据位置。</p></li><li><p><strong>写指针（Tail / WriteIndex）</strong>：指向下一个新数据要写入的位置。</p></li><li><p><strong>求余运算（Modulo <code>%</code>）</strong>：这是让数组首尾相连变成“环”的关键。当指针走到数组末尾时，<code>index = (index + 1) % SIZE</code> 就能让指针又回到 0。</p></li></ol><blockquote><p>至于环形缓冲区的读写方法，请自行查阅相关资料。</p></blockquote><ol><li><p><strong>和普通数组有何区别？</strong></p><p>假设我们有一个长度为 5 的普通数组用来做缓冲区。</p><ol><li><p>我们按顺序写入了 3 个数据：<code>[A, B, C, 空, 空]</code>。</p></li><li><p>现在我们读取了 1 个数据（读走 A）。数组变成了 <code>[空, B, C, 空, 空]</code>。</p></li><li><p>如果继续接着写，写到末尾时 <code>[空, B, C, D, E]</code>，数组尾部就没空间了。</p></li><li><p><strong>笨办法</strong>：把 B,C,D,E 全部往前挪一格（极度消耗 CPU 时间，也就是内存拷贝）。</p></li><li><p><strong>聪明办法（环形缓冲区）</strong>：尾部写满了？直接把“写指针”绕回数组的第 0 个位置！</p></li></ol></li><li><p><strong>如何判断“空”和“满”？</strong></p><p>如果读指针和写指针重合了（<code>Head == Tail</code>），这到底代表数组是被<strong>彻底写满</strong>了，还是被<strong>彻底读空</strong>了？这是一个著名的二义性问题。</p><p>有两种主流的解决办法：</p><ul><li><p><strong>办法 1（牺牲一个存储单元）</strong>：规定队列里永远留一个空位。如果 <code>(Tail + 1) % SIZE == Head</code>，就认为满了。Linux 内核很多地方用这个。</p></li><li><p><strong>办法 2（引入计数器）</strong>：加一个变量 <code>count</code>。写数据 <code>count++</code>，读数据 <code>count--</code>。如果 <code>count == SIZE</code> 就是满，<code>count == 0</code> 就是空。<strong>FreeRTOS 的队列就是用这种方式</strong>（上一节的 <code>uxMessagesWaiting</code> 吗？）。</p></li></ul></li></ol><h3 id="13-队列的发送和接收链表操作"><a class="markdownIt-Anchor" href="#13-队列的发送和接收链表操作"></a> 1.3 队列的发送和接收（链表操作）</h3><h4 id="131-读取接收"><a class="markdownIt-Anchor" href="#131-读取接收"></a> 1.3.1 读取/接收</h4><p>大致的流程是这样的，一图胜千言：其中</p><p style="color:green">绿色箭头代表判断为True</p><p style="color:red">红色则为False</p><p><img src="Snipaste_2026-04-14_21-45-00.png" alt="" /></p><h4 id="132-发送"><a class="markdownIt-Anchor" href="#132-发送"></a> 1.3.2 发送</h4><p>发送也是一样的道理，完全可以类推。</p><h3 id="14-值传递"><a class="markdownIt-Anchor" href="#14-值传递"></a> 1.4 值传递</h3><p>FreeRTOS 的队列是 <strong>“值传递（Copy by Value）”</strong>。使用的是 <code>memcpy</code> 函数</p><ul><li><p><strong>优点</strong>：极其安全。Task B 把局部变量塞进队列后，就算 Task B 的局部变量被销毁了，队列里的数据依然是完整的。</p></li><li><p><strong>缺点</strong>：如果传很大的结构体，拷贝极其耗时。</p></li><li><p><strong>FreeRTOS 的解决办法</strong>：如果你要传大块数据，你可以<strong>把指向大块数据的“指针”作为一个变量（4字节）塞进队列</strong>，这样既享受了队列的同步机制，又避免了大量内存拷贝。（但此时你需要自己管理那块大内存的生命周期）。</p></li></ul><h2 id="2-信号量"><a class="markdownIt-Anchor" href="#2-信号量"></a> 2. 信号量</h2><p>有了队列，我们看信号量就很简单了，因为他们的核心逻辑都是<strong>等待/唤醒机制</strong>。</p><p>我们可以把信号量看作是一个大小为 0 字节的“消息”。以二值信号量为例：</p><p>现在我们定义一个特殊的队列。长度为1，消息大小为0。给该队列分配一个0字节大小的环形缓冲区（地址为NULL）。</p><p>任务A（消费者）调用 <code>QueueReceive</code> 读队列数据。</p><ul><li>队列为空（因为消息数为0）</li><li>任务A被阻塞</li></ul><p>然后，一个任务B（生产者）在执行完自己的逻辑后调用 <code>QueueSend</code> 向队列写数据。</p><ul><li>memcpy拷贝0字节数据到NULL。</li><li>队列消息+1（队列满了）</li><li>任务A被唤醒</li></ul><p>任务A开始运行。直到再次因 <code>QueueReceive</code> 被阻塞。</p><p>我们把 <code>QueueSend</code> 换成 <code>SemaphoreGive</code>，<code>QueueReceive</code> 换成 <code>SemaphoreTake</code>。</p><p>任务 A 在等数据（信号量），任务 B 发送数据。这和队列“生产者-消费者”的模型在数学逻辑上是等价的。</p><p>这就是二值信号量的逻辑。我们充分利用了队列结构体中的 “uxMessagesWaiting”<br />成员制作二值信号量。</p><p>计数信号量呢？就是一个长度为 N 的队列。</p><h2 id="3-互斥量"><a class="markdownIt-Anchor" href="#3-互斥量"></a> 3. 互斥量</h2><p>互斥量和二值信号量，长得很像，但是完全复用队列的逻辑是不能实现互斥量的机制的。<br />因为它引入了 <strong>“所有权”（Ownership）</strong> 的概念，解决“优先级翻转”的问题。这个是队列逻辑无法覆盖的，主要体现在以下三点：</p><ol><li><p><strong>优先级继承机制 (Priority Inheritance)</strong></p><p>这是互斥量最核心的特性，也是它与信号量/队列最大的区别。</p><ul><li><p><strong>逻辑：</strong> 当高优先级任务 A 等待低优先级任务 B 持有的互斥量时，内核会动态地将任务 B 的优先级提升到与 A 相同。</p></li><li><p><strong>队列无法实现：</strong> 普通队列或信号量不关心“谁持有它”。队列只知道“现在有没有东西可取”，它没有记录“是谁取走了东西”，因此无法回溯并提升那个任务的优先级。</p></li></ul></li><li><p><strong>递归锁定 (Recursive Locking)</strong></p><ul><li><p><strong>互斥量：</strong> 支持“嵌套”获取。同一个任务可以多次 <code>Take</code> 同一个互斥量而不会把自己死锁，只要对应的 <code>Give</code> 次数相同即可。</p></li><li><p><strong>队列：</strong> 如果你对一个长度为 1 的队列连续执行两次 <code>Take</code>，第二次一定会因为队列为空而导致任务自我死锁。</p></li></ul></li><li><p><strong>解锁权限 (Strict Unlocking)</strong></p><ul><li><p><strong>信号量/队列：</strong> 任务 A 可以 <code>Take</code>，然后任务 B 来 <code>Give</code>（常用于任务间同步）。</p></li><li><p><strong>互斥量：</strong> <strong>必须由加锁的任务来解锁</strong>。如果任务 A 拿到了互斥量，任务 B 尝试去释放它，RTOS 内核通常会报错或操作无效。这种“所有权”约束是队列逻辑不具备的。</p></li></ul></li></ol><p>以前我也不理解二值信号量和互斥信号量有什么区别，现在有了更深的理解，下面是一个对比表：</p><table><thead><tr><th><strong>特性</strong></th><th><strong>信号量 (Semaphore) / 队列</strong></th><th><strong>互斥量 (Mutex)</strong></th></tr></thead><tbody><tr><td><strong>底层模型</strong></td><td>资源计数器 / 生产者-消费者</td><td>资源锁 / 所有权模型</td></tr><tr><td><strong>主要用途</strong></td><td>任务间同步（我发你收）</td><td>临界资源保护（我用你等）</td></tr><tr><td><strong>所有权</strong></td><td>无（谁都能发，谁都能收）</td><td><strong>有</strong>（谁加锁谁解锁）</td></tr><tr><td><strong>优先级继承</strong></td><td><strong>无</strong>（会导致优先级翻转）</td><td><strong>有</strong>（解决优先级翻转）</td></tr><tr><td><strong>实现复杂度</strong></td><td>较低（基础阻塞机制）</td><td>较高（需记录持有者、处理优先级动态调整）</td></tr></tbody></table><p>至于互斥量的实现，且听下回分解。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="RTOS" scheme="https://blog.godmao.top/categories/RTOS/"/>
    
    
  </entry>
  
  <entry>
    <title>ESPIDF-SDK配置</title>
    <link href="https://blog.godmao.top/posts/19167/"/>
    <id>https://blog.godmao.top/posts/19167/</id>
    <published>2026-04-05T10:10:13.000Z</published>
    <updated>2026-04-08T03:48:02.078Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><hr /><p>*<em><strong>AI生成内容</strong></em></p><hr /><h2 id="一-前置准备与核心说明"><a class="markdownIt-Anchor" href="#一-前置准备与核心说明"></a> 一、前置准备与核心说明</h2><ol><li><strong>环境要求</strong>：已完成ESP-IDF开发环境搭建（优先推荐VS Code + ESP-IDF插件），已创建并正确打开ESP-IDF项目（必须打开包含顶层<code>CMakeLists.txt</code>和<code>main</code>文件夹的项目根目录）。</li><li><strong>核心特性</strong><ul><li>配置为<strong>项目级</strong>，每个项目独立拥有<code>sdkconfig</code>文件，不同项目配置互不影响。</li><li><strong>禁止手动修改<code>sdkconfig</code>文件</strong>，所有修改必须通过配置编辑器完成，避免配置冲突、依赖失效。</li><li>配置项与目标芯片强相关，编辑器会自动过滤当前芯片不支持的功能（如ESP32-C3无经典蓝牙，对应配置会自动隐藏）。</li></ul></li></ol><h2 id="二-vs-code环境-打开配置编辑器主流图形化方式"><a class="markdownIt-Anchor" href="#二-vs-code环境-打开配置编辑器主流图形化方式"></a> 二、VS Code环境 打开配置编辑器（主流图形化方式）</h2><p>提供3种稳定打开方式，新手优先使用命令面板方式：</p><ol><li><strong>方式1：命令面板（通用无差异）</strong> 按下<code>Ctrl+Shift+P</code>（Windows/Linux）/<code>Cmd+Shift+P</code>（Mac）调出命令面板，输入<code>ESP-IDF: SDK Configuration Editor</code>，选中并回车，等待几秒加载完成（首次加载会解析项目Kconfig配置文件）。</li><li><strong>方式2：左侧ESP-IDF面板</strong> 点击VS Code左侧活动栏的乐鑫ESP-IDF图标，打开插件面板，在顶部区域找到「SDK Configuration Editor」选项，点击即可打开。</li><li><strong>方式3：底部状态栏快捷入口</strong> 查看VS Code底部状态栏，找到ESP-IDF区域的<strong>齿轮图标（SDK Config）</strong>，点击一键打开。</li></ol><h2 id="三-编辑器界面结构与基础操作"><a class="markdownIt-Anchor" href="#三-编辑器界面结构与基础操作"></a> 三、编辑器界面结构与基础操作</h2><h3 id="31-核心界面布局"><a class="markdownIt-Anchor" href="#31-核心界面布局"></a> 3.1 核心界面布局</h3><table><thead><tr><th>区域</th><th>核心功能</th></tr></thead><tbody><tr><td>顶部搜索栏</td><td>最高频核心功能，输入配置关键词（如<code>flash size</code>）或宏名称（如<code>CONFIG_ESP32_DEFAULT_CPU_FREQ_MHZ</code>），可瞬间定位目标配置，无需逐层查找菜单</td></tr><tr><td>左侧配置分类树</td><td>所有配置项按功能模块树形分类，顶层分类包含串口烧录、分区表、系统设置、组件配置、引导程序、编译器选项等</td></tr><tr><td>右侧配置详情区</td><td>选中左侧分类后，展示该分类下的所有配置项，包含官方说明、操作控件（下拉框/复选框/输入框）、对应的<code>CONFIG_</code>宏名称</td></tr><tr><td>底部操作栏</td><td>提供保存、重置、撤销、重做等核心操作按钮</td></tr></tbody></table><h3 id="32-基础操作规则"><a class="markdownIt-Anchor" href="#32-基础操作规则"></a> 3.2 基础操作规则</h3><ul><li>复选框：勾选=启用功能（对应宏设为<code>y</code>），取消勾选=禁用（对应宏设为<code>n</code>）。</li><li>下拉框：点击展开可选值，选择与硬件/需求匹配的参数（如Flash大小、CPU主频）。</li><li>输入框：直接输入数值（如栈大小、波特率）或字符串（如自定义分区表路径）。</li><li>帮助查看：鼠标悬停在配置项上，会弹出官方完整说明，包含功能、取值范围、依赖关系、注意事项。</li></ul><h2 id="四-核心高频配置项-分步详解新手必配"><a class="markdownIt-Anchor" href="#四-核心高频配置项-分步详解新手必配"></a> 四、核心高频配置项 分步详解（新手必配）</h2><h3 id="41-基础硬件与烧录配置serial-flasher-config"><a class="markdownIt-Anchor" href="#41-基础硬件与烧录配置serial-flasher-config"></a> 4.1 基础硬件与烧录配置（Serial flasher config）</h3><p>项目创建后首个必须核对的配置，直接决定烧录成功率和芯片启动稳定性。</p><ul><li><p><strong>Flash size</strong>：SPI Flash容量，必须与开发板硬件完全一致。常见选项：2MB/4MB/8MB/16MB，如ESP32-WROOM-32默认4MB，WROVER系列多为8MB/16MB。</p><blockquote><p>踩坑提醒：配置容量大于实际硬件，会导致烧录失败、程序崩溃；小于实际容量，会浪费Flash空间。</p></blockquote></li><li><p><strong>Flash SPI mode</strong>：Flash通信模式，默认QIO（4线高速模式），若烧录后无法启动，可改为DIO（2线兼容模式）。</p></li><li><p><strong>Flash SPI speed</strong>：Flash时钟频率，常用40MHz/80MHz，主频越高读写速度越快，硬件质量不佳时可降为40MHz提升稳定性。</p></li><li><p><strong>Flash baud rate</strong>：烧录波特率，默认115200，可提升至921600加快烧录速度，烧录不稳定时降回低波特率。</p></li><li><p><strong>Monitor baud rate</strong>：串口监视器波特率，必须与代码中UART日志输出波特率一致，行业通用默认值115200。</p></li></ul><h3 id="42-分区表配置partition-table"><a class="markdownIt-Anchor" href="#42-分区表配置partition-table"></a> 4.2 分区表配置（Partition Table）</h3><p>决定Flash空间的分配规则，直接影响固件、OTA、数据存储的可用空间。</p><ul><li><p><strong>Partition Table</strong> 核心下拉选项：</p><ol><li><p><code>Single factory app, no OTA</code>：单固件分区，无OTA功能，适合无需远程升级的简单项目，占用Flash空间最小。</p></li><li><p><code>Factory app, two OTA definitions</code>：默认选项，带双OTA分区，支持远程固件升级，是物联网项目的首选。</p></li><li><p><code>Custom partition table CSV</code>：自定义分区表，适合需要调整分区大小、新增自定义分区的场景，需指定CSV文件路径。</p><blockquote><p>注意：修改分区表配置后，必须执行<strong>全量清理→全量编译→全量烧录</strong>，否则会出现分区不匹配、启动失败。</p></blockquote></li></ol></li></ul><h3 id="43-系统核心设置esp-system-settings"><a class="markdownIt-Anchor" href="#43-系统核心设置esp-system-settings"></a> 4.3 系统核心设置（ESP System Settings）</h3><p>芯片运行的核心参数，直接影响性能、功耗、内存管理。</p><ul><li><strong>CPU frequency</strong>：CPU主频，ESP32系列默认240MHz（最高性能），可选择160MHz/80MHz，降低主频可显著降低功耗，适合低功耗电池项目。</li><li><strong>Main XTAL frequency</strong>：主晶振频率，开发板默认均为40MHz，禁止随意修改，否则芯片无法正常起振运行。</li><li><strong>RTOS tick rate (Hz)</strong>：FreeRTOS系统时钟节拍，默认100Hz（10ms一个tick），高实时性项目可改为1000Hz（1ms一个tick），但会小幅增加CPU开销。</li><li><strong>Main task stack size</strong>：主任务（app_main）栈大小，默认8192字节，若主任务中使用大数组、深度递归、复杂函数调用，出现栈溢出报错时，需调大该值。</li><li><strong>SPIRAM 相关配置</strong>：若开发板带外部PSRAM（如ESP32-WROVER系列），必须开启「Enable SPI RAM support」，否则无法使用外部内存；同时可配置WiFi/LWIP优先从PSRAM分配内存，缓解内部RAM压力。</li></ul><h3 id="44-引导程序配置bootloader-config"><a class="markdownIt-Anchor" href="#44-引导程序配置bootloader-config"></a> 4.4 引导程序配置（Bootloader config）</h3><p>芯片启动引导相关设置，新手仅需关注基础日志配置，安全加密相关量产时再配置。</p><ul><li><strong>Bootloader log verbosity</strong>：Bootloader日志级别，默认Info，调试启动问题时改为Debug，发布产品时改为Warning/Error，减少日志输出，加快启动速度。</li><li><strong>Bootloader optimized for</strong>：优化方向，默认平衡模式，可选择<code>Size</code>（减小Bootloader体积）或<code>Speed</code>（加快启动速度）。</li></ul><h3 id="45-组件配置component-config"><a class="markdownIt-Anchor" href="#45-组件配置component-config"></a> 4.5 组件配置（Component config）</h3><p>ESP-IDF最核心的配置分类，包含所有内置组件的功能开关、参数配置，几乎所有外设、协议栈、中间件的配置都在这里。</p><h4 id="451-freertos-配置"><a class="markdownIt-Anchor" href="#451-freertos-配置"></a> 4.5.1 FreeRTOS 配置</h4><p>路径：Component config → FreeRTOS</p><ul><li><strong>Idle task stack size</strong>：空闲任务栈大小，出现空闲任务栈溢出报错时调大。</li><li><strong>Enable FreeRTOS stats formatting functions</strong>：开启系统统计功能，调试时可查看CPU占用率、任务运行状态、堆栈剩余空间，发布版建议关闭以节省资源。</li><li><strong>Use vTaskTicklessIdle for low power</strong>：低功耗Tickless空闲模式，电池供电的低功耗项目必须开启，可大幅降低待机功耗。</li><li><strong>Stack overflow check</strong>：栈溢出检测，调试阶段建议开启，可快速定位栈溢出的问题代码。</li></ul><h4 id="452-日志输出配置"><a class="markdownIt-Anchor" href="#452-日志输出配置"></a> 4.5.2 日志输出配置</h4><p>路径：Component config → Log output</p><ul><li><strong>Default log verbosity</strong>：全局默认日志级别，从低到高为：Error &lt; Warning &lt; Info &lt; Debug &lt; Verbose。<ul><li>调试阶段：设为Debug，输出全量调试日志，快速定位问题。</li><li>发布阶段：设为Info/Warning，减少日志输出，加快运行速度，减小固件体积。</li></ul></li><li><strong>Maximum log verbosity</strong>：编译进固件的最大日志级别，低于该级别的日志不会被编译进固件，发布版设为Info可显著减小固件体积。</li><li><strong>Use ANSI terminal colors in log</strong>：日志彩色输出，默认开启，串口监视器中不同级别日志用颜色区分，可读性更强。</li></ul><h4 id="453-wifi-配置"><a class="markdownIt-Anchor" href="#453-wifi-配置"></a> 4.5.3 WiFi 配置</h4><p>路径：Component config → Wi-Fi</p><ul><li><strong>WiFi log verbosity</strong>：WiFi模块日志级别，调试WiFi连接、断连问题时改为Debug。</li><li><strong>Maximum number of WiFi stations connected</strong>：AP模式下最大接入的设备数量，默认4，可根据需求调整。</li><li><strong>WiFi TX power</strong>：WiFi发射功率，默认20dBm（最大值），低功耗项目可降低功率，减少功耗和干扰。</li><li><strong>WiFi NVS flash</strong>：开启WiFi配置NVS存储，默认开启，开启后可自动保存SSID和密码，重启后无需重新配网。</li></ul><h4 id="454-蓝牙ble-配置"><a class="markdownIt-Anchor" href="#454-蓝牙ble-配置"></a> 4.5.4 蓝牙/BLE 配置</h4><p>路径：Component config → Bluetooth</p><ul><li>首先勾选「Bluetooth」启用蓝牙功能，然后选择蓝牙模式：BLE（低功耗蓝牙）、Classic Bluetooth（经典蓝牙）、Dual Mode（双模）。</li><li><strong>BLE Max connections</strong>：BLE最大连接数，默认1，多设备连接项目需调大该值。</li><li><strong>BLE MTU size</strong>：BLE最大传输单元，默认23字节，大数据传输时可上调至512字节，提升传输速度。</li><li><strong>BLE Controller stack</strong>：蓝牙协议栈，默认推荐NimBLE（体积小、功耗低，专为BLE优化），也可选择Bluedroid（支持经典蓝牙+BLE双模）。</li></ul><h4 id="455-lwip-网络协议栈配置"><a class="markdownIt-Anchor" href="#455-lwip-网络协议栈配置"></a> 4.5.5 LWIP 网络协议栈配置</h4><p>路径：Component config → LWIP 所有TCP/IP、Socket、HTTP、MQTT等网络通信的底层配置，核心项：</p><ul><li><strong>TCP send/receive buffer size</strong>：TCP发送/接收缓冲区大小，大文件传输、高速通信时调大，低内存项目可适当调小。</li><li><strong>TCP maximum segment size (MSS)</strong>：TCP最大报文段长度，默认1460（以太网标准值），非特殊需求不建议修改。</li><li><strong>DHCP client options</strong>：DHCP客户端配置，可调整IP获取超时时间、重试次数，优化弱网环境下的配网成功率。</li></ul><h3 id="46-编译器选项配置compiler-options"><a class="markdownIt-Anchor" href="#46-编译器选项配置compiler-options"></a> 4.6 编译器选项配置（Compiler options）</h3><ul><li><strong>Optimization level</strong>：编译器优化等级<ul><li>调试阶段：选择<code>Debug (-Og)</code>，优化最少，调试信息最全，支持断点调试所有代码。</li><li>发布阶段：选择<code>Release (-Os)</code>，优化固件体积和运行速度，是量产首选。</li></ul></li><li><strong>Enable C++ exceptions</strong>：C<ins>异常支持，默认关闭，使用C</ins>开发且用到try/catch异常机制时必须开启。</li><li><strong>Enable stack smashing protection</strong>：栈溢出保护，调试阶段建议开启，可检测并上报栈溢出问题。</li></ul><h2 id="五-配置的保存-生效与版本管理"><a class="markdownIt-Anchor" href="#五-配置的保存-生效与版本管理"></a> 五、配置的保存、生效与版本管理</h2><h3 id="51-保存配置"><a class="markdownIt-Anchor" href="#51-保存配置"></a> 5.1 保存配置</h3><ul><li><p>基础保存：修改完成后，点击编辑器右上角/底部的<strong>Save</strong>按钮（软盘图标），编辑器会自动将配置写入项目根目录的<code>sdkconfig</code>文件，同时自动处理配置项的依赖关系。</p></li><li><p>进阶版本管理：可通过命令<code>ESP-IDF: Save Default SDKCONFIG file (save-defconfig)</code>生成<code>sdkconfig.defaults</code>文件，该文件存储项目自定义默认配置，可纳入Git版本控制，团队协作、项目移植时可自动同步配置。</p><blockquote><p>注意：<code>sdkconfig</code>文件不建议纳入Git版本控制，<code>sdkconfig.defaults</code>是官方推荐的版本管理文件。</p></blockquote></li></ul><h3 id="52-配置生效规则"><a class="markdownIt-Anchor" href="#52-配置生效规则"></a> 5.2 配置生效规则</h3><ul><li>普通配置修改：保存后，<strong>必须重新编译项目</strong>，配置才会生效（配置宏在编译阶段被处理）。</li><li>特殊配置修改（分区表、Bootloader、Flash核心参数）：保存后，必须先执行<code>Full Clean</code>全量清理，再全量编译，最后全量烧录（包含Bootloader、分区表、app固件），否则配置不生效，甚至导致芯片无法启动。</li></ul><h3 id="53-配置重置"><a class="markdownIt-Anchor" href="#53-配置重置"></a> 5.3 配置重置</h3><ul><li>若配置混乱，点击编辑器的<strong>Reset</strong>按钮，一键恢复当前ESP-IDF版本对应芯片的出厂默认配置。</li><li>也可直接删除项目根目录的<code>sdkconfig</code>文件，重新打开编辑器，会自动生成全新的默认配置文件。</li></ul><h2 id="六-命令行配置方式idfpy-menuconfig全环境兼容"><a class="markdownIt-Anchor" href="#六-命令行配置方式idfpy-menuconfig全环境兼容"></a> 六、命令行配置方式（<a href="http://idf.py">idf.py</a> menuconfig，全环境兼容）</h2><p>若不使用VS Code，或在Linux/服务器环境开发，可使用官方原生字符界面配置工具，与图形化编辑器的配置项完全一致，底层共用同一套Kconfig系统。</p><ol><li><p>操作步骤：打开终端，cd到ESP-IDF项目的<strong>根目录</strong>，执行命令 <code>idf.py menuconfig</code>，回车启动字符配置界面。</p></li><li><p>核心操作快捷键：</p><table><thead><tr><th>按键</th><th>核心功能</th></tr></thead><tbody><tr><td>上下方向键</td><td>移动光标，选择配置项</td></tr><tr><td>回车</td><td>进入子菜单/确认选择</td></tr><tr><td>空格/Y/N</td><td>切换复选框的启用/禁用状态</td></tr><tr><td>/</td><td>搜索配置项，输入关键词即可定位</td></tr><tr><td>?</td><td>查看当前配置项的官方详细帮助</td></tr><tr><td>Esc</td><td>返回上一级菜单/退出界面</td></tr><tr><td>S</td><td>保存配置</td></tr><tr><td>Q</td><td>退出界面</td></tr></tbody></table></li><li><p>保存与生效：按S键保存配置，回车确认，按Q退出后，重新编译项目即可生效，规则与图形化完全一致。</p></li></ol><h2 id="七-常见问题与排查方案"><a class="markdownIt-Anchor" href="#七-常见问题与排查方案"></a> 七、常见问题与排查方案</h2><ol><li><p><strong>编辑器一直加载，无法打开</strong></p><ul><li>常见原因：项目根目录选择错误、ESP-IDF环境未正确加载、CMakeLists.txt存在语法错误。</li><li>解决：确认打开的是包含顶层<code>CMakeLists.txt</code>的项目根目录，查看VS Code左下角是否正确识别ESP-IDF版本和目标芯片，重新执行<code>ESP-IDF: Set ESP-IDF Environment</code>命令加载环境，修复CMakeLists.txt语法错误。</li></ul></li><li><p><strong>修改配置后编译不生效</strong></p><ul><li>常见原因：未保存配置、未重新编译、宏名称拼写错误、配置项依赖功能未启用。</li><li>解决：确认点击Save保存配置，执行<code>Full Clean</code>→<code>Build</code>全量编译，核对代码中<code>CONFIG_</code>宏名称与配置项完全一致，检查配置项的依赖功能是否已启用。</li></ul></li><li><p><strong>配置后烧录，开发板无法启动</strong></p><ul><li>常见原因：Flash大小/模式配置错误、晶振频率配置错误、分区表不匹配、Bootloader配置异常。</li><li>解决：恢复默认配置，核对开发板硬件参数，重新配置后执行全量编译+全量烧录（包含Bootloader和分区表）。</li></ul></li><li><p><strong>找不到目标配置项</strong></p><ul><li>解决：直接使用顶部搜索栏，输入配置项的关键词或<code>CONFIG_</code>宏名称，瞬间定位；同时确认目标芯片支持该功能，部分功能仅特定芯片型号支持。</li></ul></li></ol><h2 id="八-代码中使用配置的示例"><a class="markdownIt-Anchor" href="#八-代码中使用配置的示例"></a> 八、代码中使用配置的示例</h2><p>所有配置项都会生成对应的<code>CONFIG_</code>宏，可直接在代码中调用，实现配置与代码联动：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;stdio.h></span></span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">"sdkconfig.h"</span></span><span class="token keyword">void</span> <span class="token function">app_main</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span><span class="token punctuation">&#123;</span>    <span class="token comment">// 打印配置的CPU主频</span>    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"当前CPU主频：%d MHz\n"</span><span class="token punctuation">,</span> CONFIG_ESP32_DEFAULT_CPU_FREQ_MHZ<span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token comment">// 条件编译：仅开启PSRAM时执行这段代码</span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">ifdef</span> <span class="token expression">CONFIG_ESP32_SPIRAM_SUPPORT</span></span>    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"已启用PSRAM支持\n"</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">else</span></span>    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"未启用PSRAM\n"</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">endif</span></span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="嵌入式" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/"/>
    
    <category term="程序架构" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/%E7%A8%8B%E5%BA%8F%E6%9E%B6%E6%9E%84/"/>
    
    
  </entry>
  
  <entry>
    <title>ESPIDF快速入门</title>
    <link href="https://blog.godmao.top/posts/21459/"/>
    <id>https://blog.godmao.top/posts/21459/</id>
    <published>2026-04-05T09:18:26.000Z</published>
    <updated>2026-04-08T03:43:40.314Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><h2 id="1-安装espidf"><a class="markdownIt-Anchor" href="#1-安装espidf"></a> 1. 安装ESPIDF</h2><p>参见——<a href="https://docs.espressif.com/projects/vscode-esp-idf-extension/zh_CN/latest/installation.html">安装 ESP-IDF 和相关工具 - - — ESP-IDF Extension for VSCode latest 文档</a></p><h2 id="2-cli常用命令"><a class="markdownIt-Anchor" href="#2-cli常用命令"></a> 2. CLI常用命令</h2><p><img src="Snipaste_2026-04-05_17-25-59.png" alt="" /></p><h2 id="3-sdk的配置"><a class="markdownIt-Anchor" href="#3-sdk的配置"></a> 3. SDK的配置</h2><p>最常用的就是配置flash、CPU主频、FreeRTOS的时钟频率。<br />可能还会用到：</p><p>自定义分区、外部ram配置、日志等级、编译优化。</p><p>参见——<a href="https://blog.godmao.top/posts/19167/">ESPIDF-SDK配置</a></p><h2 id="4-分区表详解"><a class="markdownIt-Anchor" href="#4-分区表详解"></a> 4. 分区表详解</h2><p><img src="Snipaste_2026-04-05_20-06-31.png" alt="" /></p><p>分区顾名思义就是给硬盘分区，并告诉系统每个区是用来干嘛的。如果没有分区表，系统就不知道如何操作这个存储器。</p><p>这是我自己自定义的分区表：这是一个适用ota升级的分区</p><pre class="line-numbers language-none"><code class="language-none"># Name,   Type, SubType, Offset,  Size, Flags# 注意：偏移量(Offset)留空，系统在编译时会自动计算并紧凑排列nvs,      data, nvs,     ,        0x4000,otadata,  data, ota,     ,        0x2000,phy_init, data, phy,     ,        0x1000,factory,  app,  factory, ,        1M,ota_0,    app,  ota_0,   ,        1M,ota_1,    app,  ota_1,   ,        1M,spiffs,   data, spiffs,  ,        0xE0000,<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="41-容量怎么算"><a class="markdownIt-Anchor" href="#41-容量怎么算"></a> 4.1 容量怎么算？</h3><p>相信大家第一次看到用16进制表示分区的大小时是一头雾水，不知道这对应的是多少字节。其实我们只需要把16进制转换为十进制就可以算出大小。</p><p>比如 <code>0x1000</code> 用我们小学二年级学过的方法：<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mn>1</mn><mo>×</mo><mn>1</mn><msup><mn>6</mn><mn>3</mn></msup><mo>=</mo><mn>4096</mn></mrow><annotation encoding="application/x-tex">1 \times 16^3 = 4096</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.72777em;vertical-align:-0.08333em;"></span><span class="mord">1</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">×</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.8141079999999999em;vertical-align:0em;"></span><span class="mord">1</span><span class="mord"><span class="mord">6</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8141079999999999em;"><span style="top:-3.063em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">3</span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">4</span><span class="mord">0</span><span class="mord">9</span><span class="mord">6</span></span></span></span>。</p><p>因为 1 KB = 1024 字节，所以： <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mn>4096</mn><mo>÷</mo><mn>1024</mn><mo>=</mo><mrow><mn mathvariant="bold">4</mn><mtext> KB</mtext></mrow></mrow><annotation encoding="application/x-tex">4096 \div 1024 = \mathbf{4 \text{ KB}}</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.72777em;vertical-align:-0.08333em;"></span><span class="mord">4</span><span class="mord">0</span><span class="mord">9</span><span class="mord">6</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">÷</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">1</span><span class="mord">0</span><span class="mord">2</span><span class="mord">4</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0.68333em;vertical-align:0em;"></span><span class="mord"><span class="mord mathbf">4</span><span class="mord text"><span class="mord"> KB</span></span></span></span></span></span></p><p>再如 <code>0xf0000</code><br />F 在十六进制里代表 15。换算成十进制： <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mn>15</mn><mo>×</mo><mn>1</mn><msup><mn>6</mn><mn>4</mn></msup><mo>=</mo><mn>983</mn><mo separator="true">,</mo><mn>040</mn></mrow><annotation encoding="application/x-tex">15 \times 16^4 = 983,040</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.72777em;vertical-align:-0.08333em;"></span><span class="mord">1</span><span class="mord">5</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">×</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.8141079999999999em;vertical-align:0em;"></span><span class="mord">1</span><span class="mord"><span class="mord">6</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8141079999999999em;"><span style="top:-3.063em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">4</span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0.8388800000000001em;vertical-align:-0.19444em;"></span><span class="mord">9</span><span class="mord">8</span><span class="mord">3</span><span class="mpunct">,</span><span class="mspace" style="margin-right:0.16666666666666666em;"></span><span class="mord">0</span><span class="mord">4</span><span class="mord">0</span></span></span></span> 字节。<br />换算成 KB： <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mn>983040</mn><mo>÷</mo><mn>1024</mn><mo>=</mo><mrow><mn mathvariant="bold">960</mn><mtext> KB</mtext></mrow></mrow><annotation encoding="application/x-tex">983040 \div 1024 = \mathbf{960 \text{ KB}}</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.72777em;vertical-align:-0.08333em;"></span><span class="mord">9</span><span class="mord">8</span><span class="mord">3</span><span class="mord">0</span><span class="mord">4</span><span class="mord">0</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">÷</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">1</span><span class="mord">0</span><span class="mord">2</span><span class="mord">4</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0.68333em;vertical-align:0em;"></span><span class="mord"><span class="mord mathbf">9</span><span class="mord mathbf">6</span><span class="mord mathbf">0</span><span class="mord text"><span class="mord"> KB</span></span></span></span></span></span></p><p>总结一下，<code>0x1000</code> 代表 4 KB。<code>0x10000</code> 代表 64 KB。这样我们就可以很直观的换算大小了。</p><p>所以，上面分区表大小对应如下：</p><ul><li><strong>nvs</strong>: 16 KB</li><li><strong>otadata</strong>: 8 KB</li><li><strong>phy_init</strong>: 4 KB</li><li><strong>factory</strong>: 1 MB (1024 KB)</li><li><strong>ota_0</strong>: 1 MB (1024 KB)</li><li><strong>ota_1</strong>: 1 MB (1024 KB)</li><li><strong>spiffs</strong>: 896 KB</li></ul><h3 id="42-分区表各列含义"><a class="markdownIt-Anchor" href="#42-分区表各列含义"></a> 4.2 分区表各列含义</h3><p>esp32的分区表是用csv文件定义的，左到右用逗号隔开了 6 个参数：<br /><code>Name, Type, SubType, Offset, Size, Flags</code></p><ol><li><p><strong>Name (名称)</strong>：这个分区的名字，你自己起的，方便你和系统识别（比如叫 <code>spiffs</code> 或 <code>nvs</code>）。</p></li><li><p><strong>Type (类型)</strong>：大分类。ESP32 只有两种核心类型：</p><ul><li><p><code>app</code>：代表这个分区是用来存<strong>可执行的代码/程序</strong>的。</p></li><li><p><code>data</code>：代表这个分区是用来存<strong>数据</strong>的（比如配置参数、文件、网页等）。</p></li></ul></li><li><p><strong>SubType (子类型)</strong>：小分类，进一步说明这个分区具体干嘛用。比如同样是 <code>app</code>，可以是出厂代码（<code>factory</code>）或者升级用的代码（<code>ota_0</code>）。不可随意起！</p></li><li><p><strong>Offset (偏移量)</strong>：这个分区在芯片物理空间上的“起始地址”。推荐留空。 编译器会把它们紧凑地排列好，省去了我们手动计算地址可能犯的错。</p></li><li><p><strong>Size (大小)</strong>：这个分区的容量（就是我们上面算过的那些数值）。</p></li><li><p><strong>Flags (标志位)</strong>：高级设置，比如是否加密等。绝大多数情况下留空即可。</p></li></ol><h3 id="43-分区解析"><a class="markdownIt-Anchor" href="#43-分区解析"></a> 4.3 分区解析</h3><p>这是一个带 OTA 无线升级功能 + 文件系统的设计。</p><p><strong>1. 系统配置区（nvs, otadata, phy_init）</strong>。<code>nvs</code> 用来存连接过的 WiFi 密码、传感器校准值，这样断电重启后密码还在。<code>phy_init</code> 存射频底层数据。<code>otadata</code> 告诉系统下次开机应该运行哪个盘里的代码。</p><p><strong>2. 核心程序区（factory, ota_0, ota_1）</strong> 为什么要有三个 <code>app</code> 区？这是为了实现 <strong>OTA（Over-The-Air 无线升级）</strong>：</p><ul><li><p><strong>factory</strong>：通过 USB 线烧录的原始固件（出厂设置）。</p></li><li><p><strong>ota_0 和 ota_1</strong>：当你通过 WiFi 远程更新程序时，ESP32 不会直接覆盖正在运行的程序（万一升级到一半断电了，设备就“变砖”彻底死机了）。假设系统现在运行在 <code>ota_0</code>，当你下载新固件时，它会存入 <code>ota_1</code>。存好并校验无误后，修改 <code>otadata</code> ，下次重启就从 <code>ota_1</code> 启动。这就保证了设备永远有一个完整的系统可以运行。</p></li></ul><p><strong>3. 文件系统区（spiffs）</strong> 这部分空间相当于一个小小的 U 盘。如果你用 ESP32 做一个网页控制台，你可以把 HTML 文件、CSS 样式表甚至小的图标图片存在这里，你的代码只需要去读取这些文件发给浏览器即可，而不需要把这些乱七八糟的文本全都硬编码（Hardcode）写在 C 语言代码里。</p><h3 id="44-subtype的官方命名规范"><a class="markdownIt-Anchor" href="#44-subtype的官方命名规范"></a> 4.4 subtype的官方命名规范</h3><!DOCTYPE html> <html lang="zh-CN"> <head> <meta charset="UTF-8"> <title>ESP32 分区表 SubType 说明</title> <style> /* 基础表格样式 */ .esp32-table { width: 100%; border-collapse: collapse; margin: 15px 0; font-size: 14px; text-align: left; } .esp32-table th, .esp32-table td { border: 1px solid #0070c6; padding: 12px 15px; } .esp32-table th { background-color: #f5f5f5; font-weight: bold; } /* 分类行高亮样式 */ .category-row { background-color:  ; font-weight: bold; } /* 警告提示样式 */ .warn { color: #d9534f; font-weight: bold; } </style> </head> <body> <table class="esp32-table"> <thead> <tr> <th>分区类型(Type)</th> <th>子类型(SubType)</th> <th>核心用途说明</th> </tr> </thead> <tbody> <!-- 程序区 app --> <tr> <td rowspan="3"><strong>app<br>(程序区)</strong></td> <td>factory</td> <td>出厂默认程序，ESP32 首次通电优先运行</td> </tr> <tr> <td>ota_0 ~ ota_15</td> <td>OTA 无线升级程序槽位，最多16个，常用 ota_0/ota_1 交替使用</td> </tr> <tr> <td>test</td> <td>工厂生产线测试专用，日常开发极少使用</td> </tr> <!-- 数据区 data 分类 --> <tr class="category-row"> <td rowspan="8"><strong>data<br>(数据区)</strong></td> <td class="warn">【系统核心数据】</td> <td class="warn">⚠️ 禁止随意修改</td> </tr> <tr> <td>nvs</td> <td>非易失性存储，WiFi 库自动存储密码、连接记录</td> </tr> <tr> <td>ota</td> <td>8KB 小分区，记录当前系统运行的 OTA 槽位</td> </tr> <tr> <td>phy</td> <td>物理层初始化数据，存储射频校准等底层参数</td> </tr> <tr class="category-row"> <td>【文件系统】</td> <td>用于存储数据/文件</td> </tr> <tr> <td>spiffs</td> <td>Flash 轻量级文件系统，适合存储网页、图片</td> </tr> <tr> <td>fat</td> <td>通用 FAT 文件系统，适配 SD 卡、USB 存储模拟</td> </tr> <tr> <td>littlefs</td> <td>高性能、高安全性，主流替代 spiffs 的文件系统</td> </tr> <tr class="category-row"> <td></td> <td>【其他高级功能】</td> <td>拓展功能专用分区</td> </tr> <tr> <td></td> <td>coredump</td> <td>程序崩溃时存储内存状态，用于故障排查</td> </tr> <tr> <td></td> <td>nvs_keys</td> <td>Flash 加密功能专用，存储 NVS 分区加密密钥</td> </tr> </tbody> </table> </body> </html><h3 id="45-数据分配规则"><a class="markdownIt-Anchor" href="#45-数据分配规则"></a> 4.5 数据分配规则</h3><ul><li><p><strong>规则一：4KB 对齐原则（最基础的物理铁律）</strong></p><p>Flash 芯片的最小擦除单位是 1 个扇区（4KB）。</p><ul><li><p><strong>规则</strong>：<strong>所有分区的大小（Size）必须是 4KB（<code>0x1000</code>）的整数倍。</strong></p></li><li><p><strong>绝对不要</strong>出现像 <code>0x1500</code> 或者 <code>3KB</code> 这种大小，系统会直接报错。</p></li></ul></li><li><p><strong>规则二：系统关键数据“雷打不动”</strong></p><p>有些底层数据分区，官方有严格的要求，最好不要乱改：</p><ul><li><p><strong><code>otadata</code></strong>：必须是 <strong>8KB</strong>（<code>0x2000</code>）。用于 OTA 双备份切换。</p></li><li><p><strong><code>phy_init</code></strong>：必须是 <strong>4KB</strong>（<code>0x1000</code>）。用于射频参数。</p></li></ul></li><li><p><strong>规则三：NVS 分区“宁大勿小”</strong></p><p><code>nvs</code> 负责存储 WiFi 密码、系统配置等。</p><ul><li><p><strong>底线</strong>：官方底层库（特别是 WiFi 和蓝牙库）在初始化时需要占用一定的 NVS 空间。<strong>最小不要低于 12KB，通常标准推荐值为 16KB（<code>0x4000</code>）。</strong></p></li><li><p><strong>扩展</strong>：如果你要在代码里用 <code>Preferences</code> 库保存大量的个人设置、长字符串，可以酌情增加到 <code>20KB</code> (<code>0x5000</code>) 或更大。</p></li></ul></li><li><p><strong>规则四：APP 程序区“看菜下饭，强迫对称”</strong></p><p>存放代码的区域（<code>factory</code>, <code>ota_0</code>, <code>ota_1</code>）是最占空间的部分。</p><ul><li><p><strong>看菜下饭</strong>：大小取决于你写的代码编译出来有多大。通常引入了 WiFi、蓝牙、显示屏驱动后，固件轻松突破 800KB。所以一般给 <strong>1MB（<code>0x100000</code>）起步</strong>。如果是非常庞大的项目，可能需要分 1.5MB 甚至 2MB。</p></li><li><p><strong>强迫对称</strong>：如果你使用了 OTA 升级，<strong><code>ota_0</code> 和 <code>ota_1</code> 的大小必须完全一致！</strong> 而且它们的大小，决定了你未来能够升级的最大固件体积。</p></li></ul></li><li><p><strong>规则五：文件系统（SPIFFS/FAT）“兜底捡漏”</strong></p><p>像 <code>spiffs</code> 或 <code>littlefs</code> 这样的文件系统，对大小没有特定要求（只要满足规则一的 4KB 对齐即可）。</p><ul><li><strong>规则</strong>：通常把它放在分区表的<strong>最后一行</strong>，大小就是 <strong>“整块芯片总容量 - 其它所有分区占用的容量”</strong>。</li></ul></li></ul><p>假设你手里有一块全新的 Flash 芯片，你可以按照以下 <strong>4 步走</strong>来分配：</p><ol><li><p><strong>先扣除系统预留开销</strong>：芯片最开头有约 <strong>36KB</strong> 的空间是被引导程序（Bootloader）和分区表本身占用的。这部分你看不见，但算总账时要扣掉（0x0000-0x9000）。</p></li><li><p><strong>分配固定小分区</strong>：写上 <code>nvs</code> (16KB)、<code>otadata</code> (8KB)、<code>phy_init</code> (4KB)。这三个一共占 28KB。</p></li><li><p><strong>分配程序大头（App）</strong>：评估你的代码有多大。假设是 4MB 芯片带 OTA，你分给 <code>ota_0</code> 1.2MB，<code>ota_1</code> 1.2MB。（总共用掉 2.4MB）。</p></li><li><p><strong>把剩下的都给文件系统（Data）</strong>：</p><ul><li><p>芯片总容量 - Bootloader预留 - 小分区 - App分区 = 剩下的给 <code>spiffs</code>。</p></li><li><p><em>(注意：偏移量 Offset 全部留空，让系统自动从上到下紧凑排列计算即可。)</em></p></li></ul></li></ol><blockquote><p>需要注意的是，ESP32规定所有 <code>Type</code> 为 <code>app</code>（可执行代码）的分区，它的起始地址（Offset）必须是 64KB（十六进制 <code>0x10000</code>）的整数倍（64字节对齐）。因此上面计算spiffs大小的公式算出来的结果会偏大。需要根据实际情况调小一点。</p></blockquote><p>补充：如果你想把bootloader的存储空间改大一点，只需要更改分区表的起始地址，默认为0x8000。比如改为0x10000，由于分区表默认占4字节，因此分区表的末地址为0x11000，那么更改后芯片开头就被预留了68KB空间。此时重新计算并分配spiffs的空间。</p><h2 id="5-自定义组件和cmake修改"><a class="markdownIt-Anchor" href="#5-自定义组件和cmake修改"></a> 5. 自定义组件和cmake修改</h2><p>ESP-IDF 的核心设计思想就是<strong>组件化 (Component-based)</strong>。系统自带的 FreeRTOS、Wi-Fi、蓝牙，全都是一个个独立的组件。 我们自己写的业务逻辑代码（比如 LED 驱动、传感器读取、MQTT 通信等），最好也抽离成独立的组件。这种方式代码解耦度极高，以后甚至可以直接复制到别的项目里用。</p><p>假设你要写一个专门控制 LED 的模块，你的目录结构应该变成这样：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c">hello_world<span class="token operator">/</span>├── CMakeLists<span class="token punctuation">.</span><span class="token function">txt</span>      <span class="token punctuation">(</span>项目级<span class="token punctuation">)</span>├── components<span class="token operator">/</span>         <span class="token operator">&lt;</span><span class="token operator">--</span> <span class="token number">1.</span> 在项目根目录下手动建一个 components 文件夹 <span class="token punctuation">(</span>名字必须是这个<span class="token punctuation">)</span>│   └── led_ctrl<span class="token operator">/</span>       <span class="token operator">&lt;</span><span class="token operator">--</span> <span class="token number">2.</span> 你的新模块文件夹│       ├── CMakeLists<span class="token punctuation">.</span>txt <span class="token operator">&lt;</span><span class="token operator">--</span> <span class="token number">3.</span> 为这个模块新建一个专属的 CMake 文件│       ├── led_ctrl<span class="token punctuation">.</span>c│       └── include<span class="token operator">/</span>       <span class="token operator">&lt;</span><span class="token operator">--</span> <span class="token number">4.</span> 习惯上把头文件单独放一个 include 文件夹│           └── led_ctrl<span class="token punctuation">.</span>h├── main<span class="token operator">/</span>│   ├── CMakeLists<span class="token punctuation">.</span>txt  │   └── main<span class="token punctuation">.</span>c<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>第 1 步：编写模块专属的 CMake</strong> 在 <code>led_ctrl</code> 文件夹里，新建一个 <code>CMakeLists.txt</code>，里面只需要写这一句：</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token function">idf_component_register</span><span class="token punctuation">(</span>SRCS <span class="token string">"led_ctrl.c"</span>                       INCLUDE_DIRS <span class="token string">"include"</span>                       REQUIRES driver<span class="token punctuation">)</span> <span class="token comment"># 如果你的LED代码用到了GPIO，就需要依赖底层的 driver 组件</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><em>注：<code>REQUIRES</code> 是声明依赖，如果不写，这部分代码就找不到 ESP32 底层的系统函数。</em></p><p><strong>第 2 步：在 main 中引入</strong> 因为你把 <code>led_ctrl</code> 放进了 <code>components</code> 文件夹，ESP-IDF 的 CMake 不同寻常，<strong>它在编译时会自动扫描根目录下的 <code>components</code> 文件夹，并把它注册进系统</strong>。 所以你不需要去修改最外层项目的 CMake，只需要在 <code>main.c</code> 里直接使用即可：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token comment">// 在 main.c 中直接包含即可，CMake 会自动找到它</span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">"led_ctrl.h"</span> </span><span class="token keyword">void</span> <span class="token function">app_main</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span><span class="token punctuation">&#123;</span>    <span class="token comment">// 调用 led_ctrl.c 里的函数</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="6-api"><a class="markdownIt-Anchor" href="#6-api"></a> 6. API</h2><p>——<a href="https://docs.espressif.com/projects/esp-idf/zh_CN/stable/esp32/api-reference/index.html">请参阅官方文档</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="嵌入式" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/"/>
    
    <category term="程序架构" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/%E7%A8%8B%E5%BA%8F%E6%9E%B6%E6%9E%84/"/>
    
    
  </entry>
  
  <entry>
    <title>stupidRTos开发日记</title>
    <link href="https://blog.godmao.top/posts/33566/"/>
    <id>https://blog.godmao.top/posts/33566/</id>
    <published>2026-04-03T07:13:24.000Z</published>
    <updated>2026-04-04T13:01:14.237Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><h1 style="color:blue"> 开发进度 </h1><ul><li>[ ] RTOS最基本的功能<ul><li>[x] 任务静态创建与TCB</li><li>[x] 任务栈的初始化</li><li>[ ] 动态创建任务</li><li>[x] SVC中断启动内核和第一个任务</li><li>[x] 在PendSV中断里上下文切换</li><li>[x] 任务主动发起的上下文切换函数</li><li>[x] Systick中断触发上下文切换</li><li>[x] 中断屏蔽宏函数</li><li>[x] Delay函数</li><li>[x] Idle空闲任务</li></ul></li><li>[ ] 一些基本的进程间同步功能</li></ul><h2 style="color:red"> DAY1 26-03-29 </h2><p>正式开始 suipidRTOS 项目。根据前面学习的FreeRTOS的内部机制进行开发。</p><p>完成了任务创建与TCB、任务栈的初始化、SVC中断的实现。</p><h2 style="color:red"> DAY2 26-03-30 </h2>完成任务主动让出CPU、PendSV中断的汇编逻辑。<h3 id="bug1只会触发一次上下文切换"><a class="markdownIt-Anchor" href="#bug1只会触发一次上下文切换"></a> Bug1：只会触发一次上下文切换</h3><p>问题出在 <strong><code>stm32f4xx_it.c</code> 中的中断函数嵌套调用</strong>。<br />在 <code>stm32f4xx_it.c</code> 中，我是这么写的：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">void</span> <span class="token function">PendSV_Handler</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span><span class="token punctuation">&#123;</span>  <span class="token comment">/* USER CODE BEGIN PendSV_IRQn 0 */</span>  <span class="token function">srtos_PendSV_Handler</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 你在这里调用了汇编函数！</span>  <span class="token comment">/* USER CODE END PendSV_IRQn 0 */</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><code>PendSV_Handler</code> 是一个<strong>标准的 C 函数</strong>。当内核进入这个函数时，编译器为了遵守 C 语言调用约定，会生成压栈指令（Prologue），并用 <code>bl</code>（分支并链接）指令去调用 <code>srtos_PendSV_Handler()</code>。<code>bl</code> 指令<strong>覆盖掉了 <code>LR</code> 寄存器</strong>！此时 <code>r14</code> 不再是 <code>0xFFFFFFFD</code>，而是变成了 <code>PendSV_Handler</code> 函数内部的下一条指令地址。导致PendSV中断不能正常返回到应该被切换到的任务函数，而是返回到 <code>PendSV_Handler(void)</code>，进一步返回到原先的函数。只能切换一次是因为首次运行时手动设置了LR寄存器的值。</p><p><strong>Fix：上下文切换的汇编函数必须是中断向量表的直接入口，绝对不能被其他 C 函数包装或调用。</strong></p><h2 style="color:red"> DAY3 26-04-02 </h2><p>完成Systick和中断屏蔽宏函数。</p><h3 id="q1行为寄存器的写入规则"><a class="markdownIt-Anchor" href="#q1行为寄存器的写入规则"></a> Q1：行为寄存器的写入规则</h3><p>不同于数据寄存器，行为寄存器的写入规则不应该用 <code>|=</code> （或等于）来赋值，而是直接用等于号赋值。例如ARM Cortex-M 内核的 <strong>ICSR（Interrupt Control and State Register，中断控制和状态寄存器）</strong> 就是一个行为寄存器。</p><p>写入规则：“写 0 无效”。硬件会直接无视 0，原来是什么状态，就是什么状态。</p><h3 id="q2终于理解了为什么要为os单独设置一个时基而不是和mcu共用一个时基"><a class="markdownIt-Anchor" href="#q2终于理解了为什么要为os单独设置一个时基而不是和mcu共用一个时基"></a> Q2：终于理解了为什么要为OS单独设置一个时基而不是和MCU共用一个时基</h3><p>OS和MCU时钟（HAL库依赖的时钟）解耦的妙处——确保这两个系统不受对方的影响。两个系统完全控制自己的时钟。</p><p>如果在多任务运行期间，需要临时挂起调度器（<code>vTaskSuspendAll()</code>）去执行一段极其关键的裸机代码，或者发生严重 Fault 需要在不依赖 OS 的情况下用 HAL 库保存错误日志到 Flash，SysTick 此时的状态是不可控的。</p><p><strong>时钟解耦，能保证无论 OS 处于未启动、运行中、还是崩溃状态，MCU（HAL库）永远有一颗正常跳动的心脏。</strong></p><h2 style="color:red"> DAY4 26-04-04 </h2><p>清明前夕苦逼营业中…</p><p>初步实现了Delay函数的逻辑还有空闲任务。一遍调通没啥大问题。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="开发日记" scheme="https://blog.godmao.top/categories/%E5%BC%80%E5%8F%91%E6%97%A5%E8%AE%B0/"/>
    
    
  </entry>
  
  <entry>
    <title>FreeRTOS的内部机制4</title>
    <link href="https://blog.godmao.top/posts/46543/"/>
    <id>https://blog.godmao.top/posts/46543/</id>
    <published>2026-03-28T09:31:19.000Z</published>
    <updated>2026-03-28T10:24:08.351Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><p>这章我们来分析一下PendSV和SwitchContext的实现细节，OS内核中比较难啃的地方。<br />那我们先来看看 <code>/portable/GCC/ARM_CM4F/port.c</code> 对 PendSV_Handler 的实现。</p><h2 id="1-pendsv_handler-的实现带浮点运算单元的版本"><a class="markdownIt-Anchor" href="#1-pendsv_handler-的实现带浮点运算单元的版本"></a> 1. PendSV_Handler 的实现（带浮点运算单元的版本）</h2><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">void</span> <span class="token function">xPortPendSVHandler</span><span class="token punctuation">(</span> <span class="token keyword">void</span> <span class="token punctuation">)</span><span class="token punctuation">&#123;</span>    <span class="token comment">/* This is a naked function. */</span>    __asm <span class="token keyword">volatile</span>    <span class="token punctuation">(</span>        <span class="token string">"   mrs r0, psp                         \n"</span>        <span class="token string">"   isb                                 \n"</span>        <span class="token string">"                                       \n"</span>        <span class="token string">"   ldr r3, =pxCurrentTCB               \n"</span> <span class="token comment">/* Get the location of the current TCB. */</span>        <span class="token string">"   ldr r2, [r3]                        \n"</span>        <span class="token string">"                                       \n"</span>        <span class="token string">"   tst r14, #0x10                      \n"</span> <span class="token comment">/* Is the task using the FPU context?  If so, push high vfp registers. */</span>        <span class="token string">"   it eq                               \n"</span>        <span class="token string">"   vstmdbeq r0!, &#123;s16-s31&#125;             \n"</span>        <span class="token string">"                                       \n"</span>        <span class="token string">"   stmdb r0!, &#123;r4-r11, r14&#125;            \n"</span> <span class="token comment">/* Save the core registers. */</span>        <span class="token string">"   str r0, [r2]                        \n"</span> <span class="token comment">/* Save the new top of stack into the first member of the TCB. */</span>        <span class="token string">"                                       \n"</span>        <span class="token string">"   stmdb sp!, &#123;r0, r3&#125;                 \n"</span>        <span class="token string">"   mov r0, %0                          \n"</span>        <span class="token string">"   msr basepri, r0                     \n"</span>        <span class="token string">"   dsb                                 \n"</span>        <span class="token string">"   isb                                 \n"</span>        <span class="token string">"   bl vTaskSwitchContext               \n"</span>        <span class="token string">"   mov r0, #0                          \n"</span>        <span class="token string">"   msr basepri, r0                     \n"</span>        <span class="token string">"   ldmia sp!, &#123;r0, r3&#125;                 \n"</span>        <span class="token string">"                                       \n"</span>        <span class="token string">"   ldr r1, [r3]                        \n"</span> <span class="token comment">/* The first item in pxCurrentTCB is the task top of stack. */</span>        <span class="token string">"   ldr r0, [r1]                        \n"</span>        <span class="token string">"                                       \n"</span>        <span class="token string">"   ldmia r0!, &#123;r4-r11, r14&#125;            \n"</span> <span class="token comment">/* Pop the core registers. */</span>        <span class="token string">"                                       \n"</span>        <span class="token string">"   tst r14, #0x10                      \n"</span> <span class="token comment">/* Is the task using the FPU context?  If so, pop the high vfp registers too. */</span>        <span class="token string">"   it eq                               \n"</span>        <span class="token string">"   vldmiaeq r0!, &#123;s16-s31&#125;             \n"</span>        <span class="token string">"                                       \n"</span>        <span class="token string">"   msr psp, r0                         \n"</span>        <span class="token string">"   isb                                 \n"</span>        <span class="token string">"                                       \n"</span>        <span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">ifdef</span> <span class="token expression">WORKAROUND_PMU_CM001 </span><span class="token comment">/* XMC4000 specific errata workaround. */</span></span>            <span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">if</span> <span class="token expression">WORKAROUND_PMU_CM001 <span class="token operator">==</span> <span class="token number">1</span></span></span>                <span class="token string">"           push &#123; r14 &#125;                \n"</span>                <span class="token string">"           pop &#123; pc &#125;                  \n"</span>            <span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">endif</span></span>        <span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">endif</span></span>        <span class="token string">"                                       \n"</span>        <span class="token string">"   bx r14                              \n"</span>        <span class="token string">"                                       \n"</span>        <span class="token string">"   .ltorg                              \n"</span>        <span class="token operator">::</span><span class="token string">"i"</span> <span class="token punctuation">(</span> configMAX_SYSCALL_INTERRUPT_PRIORITY <span class="token punctuation">)</span>    <span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>整个过程可以划分为三个阶段：<strong>上半场（保存现场）、中场休息（挑选任务）、下半场（恢复现场）</strong>。</p><h3 id="第一阶段上半场-保存当前任务的现场"><a class="markdownIt-Anchor" href="#第一阶段上半场-保存当前任务的现场"></a> 第一阶段：上半场 —— 保存当前任务的现场</h3><p>当系统触发 PendSV 进入这个中断时，<strong>硬件已经自动帮你把当前任务的 R0-R3, R12, LR, PC, xPSR（如果用了浮点运算，还有 S0-S15）压入了当前任务的栈里</strong>。剩下的活需要这段汇编手动干。</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token string">"   mrs r0, psp                         \n"</span> <span class="token comment">/* 读取当前任务的栈指针 PSP 到 R0 */</span><span class="token string">"   isb                                 \n"</span> <span class="token comment">/* 指令同步屏障，确保流水线执行完毕 */</span><span class="token string">"   ldr r3, =pxCurrentTCB               \n"</span> <span class="token comment">/* R3 获取 pxCurrentTCB 指针的地址 */</span><span class="token string">"   ldr r2, [r3]                        \n"</span> <span class="token comment">/* R2 获取当前 TCB 的真实首地址 */</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>解读</strong>：先把当前任务的栈顶指针 <code>PSP</code> 拿出来放在 <code>R0</code> 里备用。接着，顺藤摸瓜找到当前正在运行的任务控制块 <code>TCB</code>。</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token string">"   tst r14, #0x10                      \n"</span> <span class="token comment">/* 测试 R14(LR) 的第4位 */</span><span class="token string">"   it eq                               \n"</span> <span class="token comment">/* 如果第4位是0 (Equal) */</span><span class="token string">"   vstmdbeq r0!, &#123;s16-s31&#125;             \n"</span> <span class="token comment">/* 把浮点寄存器 s16-s31 压栈 */</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><strong>解读</strong>：<strong>这是带 FPU 芯片的特有。</strong> 在中断里，<code>R14</code>（也就是 <code>LR</code> 寄存器）里面装的不再是返回地址，而是一个叫做 <code>EXC_RETURN</code> 的特殊值。它的<strong>第 4 位</strong>用来标记被中断的任务刚刚有没有使用浮点运算。</p><ul><li><p>如果有用（第4位=0），我们就必须手动把剩下的高位浮点寄存器 <code>s16-s31</code> 也压入当前任务的栈（<code>R0</code>）里。（<code>s0-s15</code> 由硬件压）。</p>  <pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token string">"   stmdb r0!, &#123;r4-r11, r14&#125;            \n"</span> <span class="token comment">/* 将普通寄存器 r4-r11 和 r14(EXC_RETURN) 压栈 */</span><span class="token string">"   str r0, [r2]                        \n"</span> <span class="token comment">/* 【关键！】把最终的栈指针 R0 保存到 TCB 的第一个成员 pxTopOfStack 里 */</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li></ul><p><strong>解读</strong>：接着把普通寄存器 <code>R4-R11</code> 压栈。注意这里连 <code>R14 (EXC_RETURN)</code> 也一并压栈了，因为下一个任务醒来时，也要根据自己的 <code>EXC_RETURN</code> 来决定怎么出栈。<br />压栈结束后，<code>R0</code> 的位置发生移动，成为了新的“栈顶”。 <code>str</code> 则把这个新栈顶记录到任务的“身份证” TCB 里。<strong>当前任务，正式进入休眠！</strong></p><h3 id="第二阶段中场休息-呼叫-c-函数挑选新任务"><a class="markdownIt-Anchor" href="#第二阶段中场休息-呼叫-c-函数挑选新任务"></a> 第二阶段：中场休息 —— 呼叫 C 函数挑选新任务</h3><p>这部分的核心是调用 <code>vTaskSwitchContext</code>，但调用 C 函数之前需要做周密的保护。</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token string">"   stmdb sp!, &#123;r0, r3&#125;                 \n"</span> <span class="token comment">/* 把 r0 和 r3 压入主堆栈 (MSP) 保护起来 */</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><strong>解读</strong>：由于接下来要调用 C 语言函数，C 函数在运行中可能会破坏寄存器的值。由于等会“下半场”我们还要靠 <code>R3</code> 去找下一个任务的 TCB，所以先把它们压入主堆栈（中断专用栈）藏起来。</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token string">"   mov r0, %0                          \n"</span> <span class="token comment">/* %0 是宏传入的参数 configMAX_SYSCALL_INTERRUPT_PRIORITY */</span><span class="token string">"   msr basepri, r0                     \n"</span> <span class="token comment">/* 写入 basepri 寄存器，屏蔽部分中断 */</span><span class="token string">"   dsb                                 \n"</span> <span class="token string">"   isb                                 \n"</span><span class="token string">"   bl vTaskSwitchContext               \n"</span> <span class="token comment">/* 调用 C 函数，挑选最高优先级的就绪任务！ */</span><span class="token string">"   mov r0, #0                          \n"</span> <span class="token string">"   msr basepri, r0                     \n"</span> <span class="token comment">/* 清零 basepri，重新开启中断 */</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>解读</strong>：这就是 FreeRTOS 的<strong>临界区保护机制</strong>。通过配置 <code>BASEPRI</code> 寄存器，屏蔽掉所有优先级低于某个设定的中断，防止在切换任务指针的关键时刻又被打断。安全锁死后，调用调度算法 <code>vTaskSwitchContext</code>。 <strong>函数执行完毕后，全局变量 pxCurrentTCB 则被偷天换日，指向下一个要运行的高优先级任务</strong>。</p><h3 id="第三阶段下半场-恢复新任务的现场"><a class="markdownIt-Anchor" href="#第三阶段下半场-恢复新任务的现场"></a> 第三阶段：下半场 —— 恢复新任务的现场</h3><p>现在，我们要把新任务唤醒，把刚才存入栈里的数据全部弹回 CPU。</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token string">"   ldmia sp!, &#123;r0, r3&#125;                 \n"</span> <span class="token comment">/* 把之前藏在主堆栈里的 R0, R3 拿回来 */</span><span class="token string">"   ldr r1, [r3]                        \n"</span> <span class="token comment">/* 此时 R3 指向的 pxCurrentTCB 已经变了！R1 拿到新任务的 TCB 首地址 */</span><span class="token string">"   ldr r0, [r1]                        \n"</span> <span class="token comment">/* R0 拿到新任务的 pxTopOfStack (栈顶指针) */</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p><strong>解读</strong>：拿到新任务上次休眠时保存的栈顶指针 <code>R0</code>。</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token string">"   ldmia r0!, &#123;r4-r11, r14&#125;            \n"</span> <span class="token comment">/* 从新任务的栈中弹出 r4-r11 和 r14(EXC_RETURN) */</span><span class="token string">"   tst r14, #0x10                      \n"</span> <span class="token comment">/* 再次检查弹出来的 R14 的第 4 位 */</span><span class="token string">"   it eq                               \n"</span><span class="token string">"   vldmiaeq r0!, &#123;s16-s31&#125;             \n"</span> <span class="token comment">/* 如果新任务使用了浮点运算，把浮点寄存器也弹出来 */</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>解读</strong>：<strong>怎么吃的怎么吐出来。</strong> 刚才压栈的顺序反过来执行一次。注意，弹出的 <code>R14</code> 决定了接下来是否要恢复浮点寄存器。</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token string">"   msr psp, r0                         \n"</span> <span class="token comment">/* 把弹完数据后的 R0 重新写回 CPU 的栈指针寄存器 PSP */</span><span class="token string">"   isb                                 \n"</span><span class="token string">"   bx r14                              \n"</span> <span class="token comment">/* 异常返回！硬件接管剩余出栈工作 */</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><strong>解读</strong>：大功告成！把最新的栈位置还给 <code>PSP</code> 。最后一句 <code>bx r14</code> 让 CPU 根据 <code>R14</code> 的值退出中断模式。<strong>硬件会自动从 PSP 指向的栈里弹出 R0-R3, R12, LR, PC, xPSR。</strong> 伴随着 <code>PC</code> 寄存器的更新，CPU 已经置身于全新的任务现场中了！</p><h3 id="如若不需浮点运算"><a class="markdownIt-Anchor" href="#如若不需浮点运算"></a> 如若不需浮点运算</h3><p>如果不搞浮点运算，只用最普通的 Cortex-M3，完全可以删掉所有带 <code>vstmdbeq</code> 和 <code>vldmiaeq</code> 的浮点指令，以及测试 <code>R14</code> 的逻辑，那么你的代码只剩下最核心的：</p><ol><li><code>MRS r0, PSP</code></li><li>推入 <code>r4-r11</code></li><li>存入旧 TCB</li><li>调用 <code>vTaskSwitchContext</code></li><li>读取新 TCB</li><li>弹出 <code>r4-r11</code></li><li><code>MSR PSP, r0</code></li><li><code>BX LR</code></li></ol><h2 id="2-vtaskswitchcontext-的实现细节"><a class="markdownIt-Anchor" href="#2-vtaskswitchcontext-的实现细节"></a> 2. vTaskSwitchContext 的实现细节</h2><p>如果说 <code>PendSV</code> 是负责干苦力的“搬运工”（把寄存器压栈出栈），那么 <code>vTaskSwitchContext</code> 就是坐在调度室里的“总调度长”。</p><p>在 <code>PendSV</code> 的纯汇编代码中，有一句极其关键的调用：<code>bl vTaskSwitchContext</code>。搬运工把旧任务打包好之后，跑来问总调度长：“下一个该让谁上？”总调度长查阅一番后，在小黑板上写下新任务的名字（更新 <code>pxCurrentTCB</code> 指针），然后搬运工再去把新任务解包运行。</p><h3 id="1-核心使命更新-pxcurrenttcb"><a class="markdownIt-Anchor" href="#1-核心使命更新-pxcurrenttcb"></a> 1. 核心使命：更新 pxCurrentTCB</h3><p>这个函数存在的终极目的只有一个：<strong>找到当前就绪链表中优先级最高的任务，并将全局指针 pxCurrentTCB 指向它的任务控制块（TCB）。</strong></p><h3 id="2-附加工作推荐开启"><a class="markdownIt-Anchor" href="#2-附加工作推荐开启"></a> 2. 附加工作（推荐开启）</h3><p>在去挑下一个任务之前，调度长会顺手做两件附加工作：</p><ul><li><p><strong>运行时间统计 (Run Time Stats)：</strong> 如果你的系统开启了 <code>configGENERATE_RUN_TIME_STATS</code>，调度长会记录一下上一个任务总共跑了多长时间。你平时在终端里敲 <code>top</code> 命令看到的 CPU 占用率，底层数据就是在这里收集的。</p></li><li><p><strong>栈溢出检查 (Stack Overflow Checking)：</strong> 如果你开启了 <code>configCHECK_FOR_STACK_OVERFLOW</code>。调度长会去检查刚刚被暂停的那个任务的栈。它会去看栈底的最后几个字节（也就是我们之前提到过的魔术字，通常是 <code>0xA5A5A5A5</code>）有没有被改写。如果被改写了，说明任务的局部变量开得太大，把栈底击穿了！调度长会立刻触发 <code>vApplicationStackOverflowHook()</code> 函数，通常是直接死机报错，以确保系统不带病运行。</p></li></ul><h3 id="3-挑选最高优先级任务"><a class="markdownIt-Anchor" href="#3-挑选最高优先级任务"></a> 3. 挑选最高优先级任务</h3><p>附加工作做完，进入核心环节。在源码中，有一个宏： <code>taskSELECT_HIGHEST_PRIORITY_TASK()</code></p><p>这是 FreeRTOS 神奇的地方：<strong>O(1) 时间复杂度的调度算法。</strong> 无论你创建了 5 个任务还是 50 个任务，它找出最高优先级任务消耗的时间是绝对恒定的。这是怎么做到的呢？这取决于你的架构配置。</p><h4 id="方法-a通用-c-语言法"><a class="markdownIt-Anchor" href="#方法-a通用-c-语言法"></a> 方法 A：通用 C 语言法</h4><p>—— 硬件不支持时的备选</p><p>如果是比较低端的 8 位单片机（没有专门的硬件指令），FreeRTOS 只能用一个 <code>while</code> 循环，从最高优先级的就绪链表往下遍历，直到找到第一个里面有任务的链表。这种方法虽然简单，但每次耗时不一样（O(n)），不够实时。</p><p>FreeRTOS会维护一个<strong>uxTopReadyPriority</strong>的变量记录就绪链表数，也是优先级数。</p><h4 id="方法-b硬件指令加速法"><a class="markdownIt-Anchor" href="#方法-b硬件指令加速法"></a> 方法 B：硬件指令加速法</h4><p>对于ARM架构芯片，FreeRTOS 采用了硬件优化方案：<strong>前导零计数指令（CLZ - Count Leading Zeros）</strong>。</p><p>FreeRTOS 维护了一个 32 位的无符号整数变量，也叫做 <strong>uxTopReadyPriority</strong>。不同的方法<strong>uxTopReadyPriority</strong>的初始化不同，是条件编译实现的。</p><ul><li>它的每一位（Bit）代表一个优先级。</li><li>如果优先级 3 的就绪链表里有任务，就把 Bit 3 置为 1。如果没有，就置 0。 当调度长需要找最高优先级时，只需执行一条汇编指令： <code>__asm (&quot;clz r0, r1&quot;)</code></li></ul><p>这条指令会在 1 个时钟周期内，直接告诉这个 32 位变量里，从左往右数有几个 0，从而<strong>反推出最高位是 1 的是在第几位</strong>。 这就等于瞬间知道了当前系统中有就绪任务的最高优先级是多少（比如优先级 5）。</p><h3 id="4-同级轮转再复习"><a class="markdownIt-Anchor" href="#4-同级轮转再复习"></a> 4. 同级轮转再复习</h3><p>找到了最高优先级（假设是 5），接下来就要从 <code>pxReadyTasksLists[5]</code> 这个链表里挑任务了。</p><p>如果这个链表里有多个任务（比如任务 A 和任务 B 都是优先级 5，并且都在就绪状态），怎么做到公平的时间片轮转呢？</p><p>在 <code>taskSELECT_HIGHEST_PRIORITY_TASK</code> 宏的内部，有这样一句代码：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token function">listGET_OWNER_OF_NEXT_ENTRY</span><span class="token punctuation">(</span> pxCurrentTCB<span class="token punctuation">,</span> <span class="token operator">&amp;</span><span class="token punctuation">(</span> pxReadyTasksLists<span class="token punctuation">[</span> uxTopPriority <span class="token punctuation">]</span> <span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>就绪链表不仅记录了头部，还记录了一个<strong>索引指针（Index）</strong>。</li><li>每次调用这个函数，索引指针就会往后移动一格。</li><li>第一次挑出任务 A 给 <code>pxCurrentTCB</code>，索引指向 B。下一个 SysTick 到来触发切换时，再调用这个函数，就会挑出任务 B，索引又绕回 A。</li><li>这就完美实现了同级优先级的<strong>公平时间片轮转（Round-Robin）</strong></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="RTOS" scheme="https://blog.godmao.top/categories/RTOS/"/>
    
    
  </entry>
  
  <entry>
    <title>FreeRTOS的内部机制3</title>
    <link href="https://blog.godmao.top/posts/30606/"/>
    <id>https://blog.godmao.top/posts/30606/</id>
    <published>2026-03-26T14:36:50.000Z</published>
    <updated>2026-04-03T07:17:11.690Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><h2 id="1-甘-文-崔三个重要的中断"><a class="markdownIt-Anchor" href="#1-甘-文-崔三个重要的中断"></a> 1. 甘、文、崔：三个重要的中断</h2><p>在FreeRTOS中，使用三个宏重写了中断，他们一般定义在与硬件相关的 <code>port.c</code> 中。</p><p>例如 <code>/portable/GCC/ARM_CM4F/port.c</code>。</p><blockquote><p>注意还有一个文件夹叫 <code>/portable/GCC/ARM_CM4_MPU/port.c</code>。这是带内存保护单元MPU的版本，由于添加了内存保护功能，实现较为复杂，上下文开销也大。一般用在对安全性要求极高的产品上，比如医疗和车规级产品。一般产品不建议使用。</p></blockquote><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">void</span> <span class="token function">xPortPendSVHandler</span><span class="token punctuation">(</span> <span class="token keyword">void</span> <span class="token punctuation">)</span> <span class="token keyword">__attribute__</span><span class="token punctuation">(</span> <span class="token punctuation">(</span> naked <span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token keyword">void</span> <span class="token function">xPortSysTickHandler</span><span class="token punctuation">(</span> <span class="token keyword">void</span> <span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token keyword">void</span> <span class="token function">vPortSVCHandler</span><span class="token punctuation">(</span> <span class="token keyword">void</span> <span class="token punctuation">)</span> <span class="token keyword">__attribute__</span><span class="token punctuation">(</span> <span class="token punctuation">(</span> naked <span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><blockquote><p><code>__attribute__( ( naked ) )</code> 告知编译器这个函数是“裸体”的，不要给他“穿衣服”（即给他添加额外的汇编代码）</p></blockquote><h3 id="11-svc_handler"><a class="markdownIt-Anchor" href="#11-svc_handler"></a> 1.1 SVC_Handler</h3><p>—— Supervisor Call，系统服务调用。</p><p>它在RTOS中仅被调用一次，用来启动第一个任务。</p><p>芯片复位上电，默认运行在特权模式下，并且使用的是 <strong>MSP（主栈指针）</strong>。（我们在1里讲的双栈模式）但我们前面说过，RTOS 的任务必须使用自己的私有栈，也就是必须切换到 <strong>PSP（进程栈指针）</strong> 去运行。</p><p>标准做法就是使用SVC。省流：<code>vTaskStartScheduler()</code> 通过触发 <code>svc 0</code> 指令进入到了SVC中断。</p><details><summary> 如果对具体的调用链感兴趣可点击展开查看细节，篇幅原因折叠了 </summary><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">void</span> <span class="token function">vTaskStartScheduler</span><span class="token punctuation">(</span> <span class="token keyword">void</span> <span class="token punctuation">)</span><span class="token punctuation">&#123;</span>BaseType_t xReturn<span class="token punctuation">;</span>xReturn <span class="token operator">=</span> <span class="token function">prvCreateIdleTasks</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">if</span> <span class="token expression"><span class="token punctuation">(</span> configUSE_TIMERS <span class="token operator">==</span> <span class="token number">1</span> <span class="token punctuation">)</span></span></span>    <span class="token punctuation">&#123;</span>        <span class="token keyword">if</span><span class="token punctuation">(</span> xReturn <span class="token operator">==</span> pdPASS <span class="token punctuation">)</span>        <span class="token punctuation">&#123;</span>            xReturn <span class="token operator">=</span> <span class="token function">xTimerCreateTimerTask</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token punctuation">&#125;</span>        <span class="token keyword">else</span>        <span class="token punctuation">&#123;</span>            <span class="token function">mtCOVERAGE_TEST_MARKER</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token punctuation">&#125;</span>    <span class="token punctuation">&#125;</span>    <span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">endif</span> <span class="token comment">/* configUSE_TIMERS */</span></span>    <span class="token comment">/* The return value for xPortStartScheduler is not required         * hence using a void datatype. */</span>        <span class="token punctuation">(</span> <span class="token keyword">void</span> <span class="token punctuation">)</span> <span class="token function">xPortStartScheduler</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>可以看出，开始调度函数一般就做这些事：</p><ol><li>创建空闲任务</li><li>如果使用了定时器，则创建定时器守护函数</li><li>调用 <code>xPortStartScheduler()</code>，这个函数又与硬件强相关</li></ol><p>我们来看 <code>/portable/GCC/ARM_CM4F/port.c</code> 中的 <code>xPortStartScheduler()</code></p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token comment">/* Start the first task. */</span>    <span class="token function">prvPortStartFirstTask</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>再看 <code>prvPortStartFirstTask()</code>，可以看到已经到汇编了，最重点的就是 <code> svc 0</code> 这个指令，直接触发了SVC中断。</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">prvPortStartFirstTask</span><span class="token punctuation">(</span> <span class="token keyword">void</span> <span class="token punctuation">)</span><span class="token punctuation">&#123;</span>    <span class="token comment">/* Start the first task.  This also clears the bit that indicates the FPU is     * in use in case the FPU was used before the scheduler was started - which     * would otherwise result in the unnecessary leaving of space in the SVC stack     * for lazy saving of FPU registers. */</span>    __asm <span class="token keyword">volatile</span> <span class="token punctuation">(</span>        <span class="token string">" ldr r0, =0xE000ED08   \n"</span> <span class="token comment">/* Use the NVIC offset register to locate the stack. */</span>        <span class="token string">" ldr r0, [r0]          \n"</span>        <span class="token string">" ldr r0, [r0]          \n"</span>        <span class="token string">" msr msp, r0           \n"</span> <span class="token comment">/* Set the msp back to the start of the stack. */</span>        <span class="token string">" mov r0, #0            \n"</span> <span class="token comment">/* Clear the bit that indicates the FPU is in use, see comment above. */</span>        <span class="token string">" msr control, r0       \n"</span>        <span class="token string">" cpsie i               \n"</span> <span class="token comment">/* Globally enable interrupts. */</span>        <span class="token string">" cpsie f               \n"</span>        <span class="token string">" dsb                   \n"</span>        <span class="token string">" isb                   \n"</span>        <span class="token string">" svc 0                 \n"</span> <span class="token comment">/* System call to start first task. */</span>        <span class="token string">" nop                   \n"</span>        <span class="token string">" .ltorg                \n"</span>        <span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></details><p>那在SVC中断里具体做什么呢？我们查看SVC回调函数的实现细节：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">void</span> <span class="token function">vPortSVCHandler</span><span class="token punctuation">(</span> <span class="token keyword">void</span> <span class="token punctuation">)</span> <span class="token keyword">__attribute__</span> <span class="token punctuation">(</span><span class="token punctuation">(</span> naked <span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token keyword">void</span> <span class="token function">vPortSVCHandler</span><span class="token punctuation">(</span> <span class="token keyword">void</span> <span class="token punctuation">)</span><span class="token punctuation">&#123;</span>    __asm <span class="token keyword">volatile</span> <span class="token punctuation">(</span>        <span class="token comment">/* 1. 找到当前最高优先级任务的 TCB 指针 */</span>        <span class="token string">"   ldr r3, pxCurrentTCBConst2      \n"</span> <span class="token comment">/* r3 = &amp;pxCurrentTCB (获取指针变量本身的物理地址) */</span>        <span class="token string">"   ldr r1, [r3]                    \n"</span> <span class="token comment">/* r1 = pxCurrentTCB (解引用，拿到当前 TCB 的首地址) */</span>                <span class="token comment">/* 2. 读取 TCB 的第一个成员：pxTopOfStack */</span>        <span class="token string">"   ldr r0, [r1]                    \n"</span> <span class="token comment">/* r0 = TCB的首个4字节数据，即之前伪造好的栈顶指针 */</span>                <span class="token comment">/* 3. 手动出栈：恢复属于软件管理的寄存器 (R4-R11) 以及 EXC_RETURN (R14) */</span>        <span class="token string">"   ldmia r0!, &#123;r4-r11, r14&#125;        \n"</span> <span class="token comment">/* 从 r0 存储的地址开始，连续弹出数据到 r4-r11 和 r14， 同时r0的值递增。 */</span>                <span class="token comment">/* 4. 移交栈指针给硬件 PSP */</span>        <span class="token string">"   msr psp, r0                     \n"</span> <span class="token comment">/* 将 r0 赋值给 CPU 的进程栈指针 (PSP) */</span>        <span class="token string">"   isb                             \n"</span> <span class="token comment">/* 指令同步屏障：清空流水线，强制 CPU 确认 PSP 已经更新 */</span>                <span class="token comment">/* 5. 打开中断总开关 */</span>        <span class="token string">"   mov r0, #0                      \n"</span>         <span class="token string">"   msr basepri, r0                 \n"</span> <span class="token comment">/* 清零 BASEPRI 寄存器，允许所有优先级的中断触发 */</span>                <span class="token comment">/* 6. 神奇的异空间跳跃：触发硬件自动出栈 */</span>        <span class="token string">"   bx r14                          \n"</span> <span class="token comment">/* 跳转到 r14 (LR) 寄存器里的特殊地址，触发中断返回 */</span>        <span class="token string">"                                   \n"</span>        <span class="token string">"   .align 4                        \n"</span>        <span class="token string">"pxCurrentTCBConst2: .word pxCurrentTCB \n"</span>    <span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>简单来说，就是做了这么几件事：</p><ul><li>通过 <code>pxCurrentTCB</code> 找到第一个任务的栈顶指针。</li><li>将 CPU 的栈指针（PSP）指向这个栈顶。</li><li>执行出栈操作（POP），把我们在 <code>pxPortInitialiseStack</code> 里伪造的 <code>PC</code>、<code>R0</code> 等寄存器弹到 CPU 里。</li><li>汇编执行 <code>bx r14</code>（返回指令）。跳到了第一个任务的代码去执行了。</li></ul><blockquote><p>新入栈的元素总是在栈顶，出栈总是栈顶元素出，是曰“后进先出”。</p></blockquote><p>一些细节的说明：</p><ol><li>对于汇编指令的含义，这里就不多赘述。</li><li>关于硬件自动出栈，第一节已有讲解</li></ol><p><img src="Snipaste_2026-03-27_18-00-41.png" alt="" /></p><p>至此，第一个任务的运行现场已准备就绪，开始运行了！</p><h3 id="12-systick_handler"><a class="markdownIt-Anchor" href="#12-systick_handler"></a> 1.2 SysTick_Handler</h3><p>——决策者。</p><p>SysTick 是 RTOS 的心跳，通常每 1 毫秒触发一次。它的核心职责是<strong>处理时间逻辑</strong>并<strong>做出调度决策</strong>，但<strong>绝对不亲自执行上下文切换</strong>。来看看其核心代码：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">void</span> <span class="token function">SysTick_Handler</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span><span class="token comment">/*The SysTick runs at the lowest interrupt priority*/</span><span class="token punctuation">&#123;</span>    <span class="token comment">// 1. 屏蔽中断，保护 OS 内部数据结构</span>    <span class="token class-name">uint32_t</span> ulPreviousMask <span class="token operator">=</span> <span class="token function">portSET_INTERRUPT_MASK_FROM_ISR</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token comment">// 2. 更新系统时间 (xTickCount++)，检查有没有延时到期的任务</span>    <span class="token keyword">if</span><span class="token punctuation">(</span> <span class="token function">xTaskIncrementTick</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">!=</span> pdFALSE <span class="token punctuation">)</span>    <span class="token punctuation">&#123;</span>        <span class="token comment">//向硬件寄存器（ICSR）的特定位写 1，请求触发 PendSV 中断。</span>        portNVIC_INT_CTRL_REG <span class="token operator">=</span> portNVIC_PENDSVSET_BIT<span class="token punctuation">;</span>     <span class="token punctuation">&#125;</span>    <span class="token comment">// 4. 恢复中断屏蔽</span>    <span class="token function">portCLEAR_INTERRUPT_MASK_FROM_ISR</span><span class="token punctuation">(</span> ulPreviousMask <span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>就一个核心函数 <code>xTaskIncrementTick()</code> 做了很多事：</p><ol><li><p><strong>维持系统时间：</strong> 每次触发，将 FreeRTOS 的全局系统节拍计数器（<code>xTickCount</code>）加 1。</p></li><li><p><strong>唤醒阻塞任务：</strong> 检查 <code>pxDelayedTaskList</code>（延时链表）。如果有任务的等待时间到了（比如 <code>vTaskDelay(100)</code> 结束，或者等待的超时时间到了），SysTick 负责把它从阻塞链表移到就绪链表（<code>pxReadyTasksLists</code>）。</p></li><li><p><strong>时间片轮转裁决：</strong> 如果开启了时间片，SysTick 会检查当前运行任务的同优先级下，是否有其他任务也在就绪状态。如果有，说明当前任务的“1 毫秒配额”用完了，该换人了。</p></li></ol><p>这个函数比较长，我们还是看一些核心的部分:</p><details><summary>1. 增加计时器值，以及判断时钟是否溢出，若溢出则切换阻塞链表指针（第2节讲的）的部分</summary><pre><code><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token comment">/* Tick increment should occur on every kernel timer event. Core 0 has the     * responsibility to increment the tick, or increment the pended ticks if the     * scheduler is suspended.  If pended ticks is greater than zero, the core that     * calls xTaskResumeAll has the responsibility to increment the tick. */</span>    <span class="token keyword">if</span><span class="token punctuation">(</span> uxSchedulerSuspended <span class="token operator">==</span> <span class="token punctuation">(</span> UBaseType_t <span class="token punctuation">)</span> <span class="token number">0U</span> <span class="token punctuation">)</span>    <span class="token punctuation">&#123;</span>        <span class="token comment">/* Minor optimisation.  The tick count cannot change in this         * block. */</span>  <span class="token comment">//1，增加计数</span>        <span class="token keyword">const</span> TickType_t xConstTickCount <span class="token operator">=</span> xTickCount <span class="token operator">+</span> <span class="token punctuation">(</span> TickType_t <span class="token punctuation">)</span> <span class="token number">1</span><span class="token punctuation">;</span>        <span class="token comment">/* Increment the RTOS tick, switching the delayed and overflowed         * delayed lists if it wraps to 0. */</span>        xTickCount <span class="token operator">=</span> xConstTickCount<span class="token punctuation">;</span>        <span class="token keyword">if</span><span class="token punctuation">(</span> xConstTickCount <span class="token operator">==</span> <span class="token punctuation">(</span> TickType_t <span class="token punctuation">)</span> <span class="token number">0U</span> <span class="token punctuation">)</span>        <span class="token punctuation">&#123;</span>            <span class="token function">taskSWITCH_DELAYED_LISTS</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token comment">//2，切换链表指针</span>        <span class="token punctuation">&#125;</span>        <span class="token keyword">else</span>        <span class="token punctuation">&#123;</span>            <span class="token function">mtCOVERAGE_TEST_MARKER</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></code></pre></details><details><summary>2. 唤醒阻塞任务的部分</summary><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token comment">/* See if this tick has made a timeout expire.  Tasks are stored in        * the  queue in the order of their wake time - meaning once one task        * has been found whose block time has not expired there is no need to        * look any further down the list. */</span>       <span class="token keyword">if</span><span class="token punctuation">(</span> xConstTickCount <span class="token operator">>=</span> xNextTaskUnblockTime <span class="token punctuation">)</span>       <span class="token punctuation">&#123;</span>           <span class="token keyword">for</span><span class="token punctuation">(</span> <span class="token punctuation">;</span> <span class="token punctuation">;</span> <span class="token punctuation">)</span>           <span class="token punctuation">&#123;</span>               <span class="token keyword">if</span><span class="token punctuation">(</span> <span class="token function">listLIST_IS_EMPTY</span><span class="token punctuation">(</span> pxDelayedTaskList <span class="token punctuation">)</span> <span class="token operator">!=</span> pdFALSE <span class="token punctuation">)</span>               <span class="token punctuation">&#123;</span>                   <span class="token comment">/* The delayed list is empty.  Set xNextTaskUnblockTime                    * to the maximum possible value so it is extremely                    * unlikely that the                    * if( xTickCount >= xNextTaskUnblockTime ) test will pass                    * next time through. */</span>                      <span class="token comment">//如果没有阻塞任务，则将其设为最大值，以便不进入大if判断中，而这个xNextTaskUnblockTime是个全局变量，有阻塞任务时会更新</span>                   xNextTaskUnblockTime <span class="token operator">=</span> portMAX_DELAY<span class="token punctuation">;</span>                   <span class="token keyword">break</span><span class="token punctuation">;</span>               <span class="token punctuation">&#125;</span>               <span class="token keyword">else</span>               <span class="token punctuation">&#123;</span>                   <span class="token comment">/* The delayed list is not empty, get the value of the                    * item at the head of the delayed list.  This is the time                    * at which the task at the head of the delayed list must                    * be removed from the Blocked state. */</span>                   <span class="token comment">/* MISRA Ref 11.5.3 [Void pointer assignment] */</span>                   <span class="token comment">/* More details at: https://github.com/FreeRTOS/FreeRTOS-Kernel/blob/main/MISRA.md#rule-115 */</span>                   <span class="token comment">/* coverity[misra_c_2012_rule_11_5_violation] */</span>                   pxTCB <span class="token operator">=</span> <span class="token function">listGET_OWNER_OF_HEAD_ENTRY</span><span class="token punctuation">(</span> pxDelayedTaskList <span class="token punctuation">)</span><span class="token punctuation">;</span>                   xItemValue <span class="token operator">=</span> <span class="token function">listGET_LIST_ITEM_VALUE</span><span class="token punctuation">(</span> <span class="token operator">&amp;</span><span class="token punctuation">(</span> pxTCB<span class="token operator">-></span>xStateListItem <span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>                                      <span class="token keyword">if</span><span class="token punctuation">(</span> xConstTickCount <span class="token operator">&lt;</span> xItemValue <span class="token punctuation">)</span>                   <span class="token punctuation">&#123;</span>                       <span class="token comment">/* It is not time to unblock this item yet, but the                        * item value is the time at which the task at the head                        * of the blocked list must be removed from the Blocked                        * state -  so record the item value in                        * xNextTaskUnblockTime. */</span>                          <span class="token comment">//记录所有任务中离就绪时间最短的</span>                       xNextTaskUnblockTime <span class="token operator">=</span> xItemValue<span class="token punctuation">;</span>                       <span class="token keyword">break</span><span class="token punctuation">;</span>                   <span class="token punctuation">&#125;</span>                   <span class="token keyword">else</span>                   <span class="token punctuation">&#123;</span>                       <span class="token function">mtCOVERAGE_TEST_MARKER</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>                   <span class="token punctuation">&#125;</span>                   <span class="token comment">/* It is time to remove the item from the Blocked state. */</span>                   <span class="token function">listREMOVE_ITEM</span><span class="token punctuation">(</span> <span class="token operator">&amp;</span><span class="token punctuation">(</span> pxTCB<span class="token operator">-></span>xStateListItem <span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>                   <span class="token comment">/* Is the task waiting on an event also?  If so remove                    * it from the event list. */</span>                   <span class="token keyword">if</span><span class="token punctuation">(</span> <span class="token function">listLIST_ITEM_CONTAINER</span><span class="token punctuation">(</span> <span class="token operator">&amp;</span><span class="token punctuation">(</span> pxTCB<span class="token operator">-></span>xEventListItem <span class="token punctuation">)</span> <span class="token punctuation">)</span> <span class="token operator">!=</span> <span class="token constant">NULL</span> <span class="token punctuation">)</span>                   <span class="token punctuation">&#123;</span>                       <span class="token function">listREMOVE_ITEM</span><span class="token punctuation">(</span> <span class="token operator">&amp;</span><span class="token punctuation">(</span> pxTCB<span class="token operator">-></span>xEventListItem <span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>                   <span class="token punctuation">&#125;</span>                   <span class="token keyword">else</span>                   <span class="token punctuation">&#123;</span>                       <span class="token function">mtCOVERAGE_TEST_MARKER</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>                   <span class="token punctuation">&#125;</span>                   <span class="token comment">/* Place the unblocked task into the appropriate ready                    * list. */</span>                      <span class="token comment">//加入就绪链表</span>                   <span class="token function">prvAddTaskToReadyList</span><span class="token punctuation">(</span> pxTCB <span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></details></details><details><summary>3. 同优先级的时间片轮询</summary><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token comment">/* Tasks of equal priority to the currently running task will share         * processing time (time slice) if preemption is on, and the application         * writer has not explicitly turned time slicing off. */</span>        <span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">if</span> <span class="token expression"><span class="token punctuation">(</span> <span class="token punctuation">(</span> configUSE_PREEMPTION <span class="token operator">==</span> <span class="token number">1</span> <span class="token punctuation">)</span> <span class="token operator">&amp;&amp;</span> <span class="token punctuation">(</span> configUSE_TIME_SLICING <span class="token operator">==</span> <span class="token number">1</span> <span class="token punctuation">)</span> <span class="token punctuation">)</span></span></span>        <span class="token comment">//本质就是判断链表长度嘛</span>        <span class="token punctuation">&#123;</span>            <span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">if</span> <span class="token expression"><span class="token punctuation">(</span> configNUMBER_OF_CORES <span class="token operator">==</span> <span class="token number">1</span> <span class="token punctuation">)</span></span></span>            <span class="token punctuation">&#123;</span>                <span class="token keyword">if</span><span class="token punctuation">(</span> <span class="token function">listCURRENT_LIST_LENGTH</span><span class="token punctuation">(</span> <span class="token operator">&amp;</span><span class="token punctuation">(</span> pxReadyTasksLists<span class="token punctuation">[</span> pxCurrentTCB<span class="token operator">-></span>uxPriority <span class="token punctuation">]</span> <span class="token punctuation">)</span> <span class="token punctuation">)</span> <span class="token operator">></span> <span class="token number">1U</span> <span class="token punctuation">)</span>                <span class="token punctuation">&#123;</span>                    xSwitchRequired <span class="token operator">=</span> pdTRUE<span class="token punctuation">;</span>                <span class="token punctuation">&#125;</span>                <span class="token keyword">else</span>                <span class="token punctuation">&#123;</span>                    <span class="token function">mtCOVERAGE_TEST_MARKER</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>                <span class="token punctuation">&#125;</span>            <span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></details><p>最后：</p><ul><li><p><strong>下达切换指令（悬起 PendSV）：</strong> 当 SysTick 经过上述计算，发现<strong>需要</strong>进行任务切换时（无论是同级轮转，还是唤醒了更高优先级的任务），它只会做一件事：向硬件寄存器（ICSR）的特定位写 1，<strong>请求触发 PendSV 中断</strong>。然后 SysTick 就直接返回退出了。</p></li><li><p><strong>如果高优先级IRQ经常打断systick，会不会导致时钟不准？</strong></p><p>只要你的IRQ写的符合标准，比如不要做长时间的逻辑，一般不会，只会有时间抖动。因为我们有硬件计数器兜底。硬件计数器的时间是绝对准确的，这次systick中断稍微延后了，下一次依然会准时触发。</p></li></ul><h3 id="13-pendsv_handler"><a class="markdownIt-Anchor" href="#13-pendsv_handler"></a> 1.3 ⭐PendSV_Handler</h3><p>——可悬起的系统调用。上下文切换的核心</p><p>PendSV（可悬起的系统调用），它是一种CPU系统级别的异常，它可以像普通外设中断一样被悬起，而不会像SVC服务那样，因为没有及时响应处理，而触发Fault。所以PendSV的最大特点就是，它是系统级别的异常，但它又支持挂起。</p><ul><li><strong>触发时机</strong>：被 SysTick 触发，或者任务调用 <code>TaskYield()</code>、<code>TaskDelay()</code> 时主动触发。</li><li><strong>存在的意义</strong>： <strong>保存上一个任务的现场，恢复下一个任务的现场。</strong></li></ul><p>here are some question，hope they can help you understand PendSV better：</p><ul><li><p><strong>为什么使用PendSV</strong>？</p><p>因为优先级最低，如果此时系统中有任何其他硬件外设中断（如串口接收、定时器、ADC）正在发生，PendSV 会排队等待。它保证了“任务切换”绝对不会抢占“硬件中断响应”。</p></li><li><p><strong>用systick中断将优先级设为最低，然后在systick中断里切换上下文，是不是也可以达到同样的效果？</strong></p><p>我认为是不可以的，因为systick最主要的任务是记录系统节拍，它的触发是有严格规律的（比如严格每 1 毫秒触发一次），但 RTOS 中的任务切换，绝大多数时候是<strong>异步的、突发的</strong>。我们总不可能在需要上下文切换的时候强制触发一次systick中断吧？这样时钟节拍不就乱了吗？</p></li><li><p><strong>在上下文切换时，一般要关闭中断响应，此时如果来了紧急的中断怎么办？</strong></p><p>额，会屏蔽低于某个优先级的中断，而不是所有中断，如果是很紧急的中断，把他优先级设高一点，就算在任务切换也能紧急响应。这叫<strong>零延迟中断（Zero Latency Interrupts）</strong></p></li></ul><blockquote><p><strong>课后小科普：什么是中断丢失？</strong><br />NVIC挂起寄存器（ISPR）中，每一个中断源只有 1 个 Bit 的记录空间。所以该寄存器只能记录“谁来找过”，不能记录“来了几次”。所以某个中断被挂起期间，无论这个中断又触发了几次，最后恢复的时候也只会执行一次，除了最后一次其余中断全部丢失了。避免这种情况的方法就是遵循中断“快进快出”原则。</p></blockquote><p>要想复刻一个RTOS，PendSV的实现是不得不品的一环。关于PendSV的源码，我们下一章继续！</p><h2 id="2-调度的本质是链表操作"><a class="markdownIt-Anchor" href="#2-调度的本质是链表操作"></a> 2. 调度的本质是链表操作</h2><p>在上一节我们已经略微领悟到了三个中断的妙处，可以说是从微观角度来理解RTOS，下面我们从宏观角度继续看看RTOS的调度。正如本章标题所说，调度的本质就是链表操作，我们结合具体的场景来看看调度是怎么回事。</p><p>首先，在 FreeRTOS 中，所有的任务在任何时刻都必然存在于以下三种链表之一：</p><ol><li><strong>就绪链表数组</strong> (<code>pxReadyTasksLists</code>)：随时准备运行的任务。</li><li><strong>阻塞/延时链表</strong> (<code>xDelayedTaskList</code>)：在睡觉，或者在等某个事件的任务。</li><li><strong>挂起链表</strong> (<code>xPendingReadyList</code> / <code>xSuspendedTaskList</code>)：被强制暂停的特殊情况。</li></ol><h3 id="场景一同级任务的时间片轮转"><a class="markdownIt-Anchor" href="#场景一同级任务的时间片轮转"></a> 场景一：同级任务的时间片轮转</h3><p><strong>触发时机</strong>：硬件的 SysTick 定时器（比如每 1ms）产生一次中断。</p><p><strong>链表操作</strong>：</p><ol><li>硬件中断打断当前运行的 Task A。</li><li>系统进入中断服务函数，调用内核的 <code>xTaskIncrementTick()</code>，将全局时间 <code>xTickCount</code> 加 1。</li><li>检查当前优先级下，是否还有其他就绪任务？如果有，触发底层 <code>PendSV</code> 异常</li><li>在切换函数 <code>vTaskSwitchContext</code> 中，调用 <code>listGET_OWNER_OF_NEXT_ENTRY</code>。</li><li><strong>核心动作</strong>：将 <code>pxReadyTasksLists[TaskA的优先级]</code> 这个链表的 <code>pxIndex</code> 游标往后移动一格，获取到 Task B 的 TCB。</li><li>将全局指针 <code>pxCurrentTCB</code> 指向 Task B。完成切换！</li></ol><blockquote><p>时间片轮转的实现方法，可以类比排队打饭，打完饭的人还想打，需要到队尾去排队。任务a运行1个时钟，就被移到队尾，让下一个任务运行，该任务又运行1个时钟，也被移到队尾，以此类推，循环往复。而我们的任务链表是环形的，它不需要执行耗时的“拔出再插入”操作，只是遍历环形链表就可以达到这种目的。还记得我们在 <code>List_t</code> 结构体里看到的那个 <code>pxIndex</code>（游标指针）吗？想想你是怎么遍历环形链表的？<code>pxIndex = pxIndex-&gt;pxNext</code>。游标往后挪一格，下一个任务自然就变成了队伍的第一个。</p></blockquote><h3 id="场景二任务主动睡觉-调用-vtaskdelay"><a class="markdownIt-Anchor" href="#场景二任务主动睡觉-调用-vtaskdelay"></a> 场景二：任务主动“睡觉” (调用 <code>vTaskDelay</code>)</h3><p><strong>触发时机</strong>：Task A 运行到一半，调用了 <code>vTaskDelay(100)</code>，想休息 100 个 tick。 <strong>链表操作</strong>：</p><ol><li><strong>计算唤醒时间</strong>：系统把当前的 <code>xTickCount</code> + 100，算出一个“闹钟时间”（ItemValue），存入 Task A TCB 的状态钩子 <code>xStateListItem.xItemValue</code> 中。</li><li><strong>拔出</strong>：把 Task A 从它所在的就绪链表 <code>pxReadyTasksLists</code> 中 <code>List_Remove</code> 拔出来。</li><li><strong>插入等待</strong>：把 Task A 插入到延时链表 <code>xDelayedTaskList</code> 中。<strong>注意：延时链表是按唤醒时间升序排列的！</strong> 最早醒来的任务永远排在链表最前面。（这就是 <code>vListInsert</code> 按值插入的作用）。</li><li><strong>让出 CPU</strong>：Task A 调用 <code>portYIELD()</code>，主动触发上下文切换。调度器会在剩下的就绪任务里挑一个最高优先级的去运行。</li></ol><h3 id="场景三闹钟响了任务唤醒-滴答中断触发"><a class="markdownIt-Anchor" href="#场景三闹钟响了任务唤醒-滴答中断触发"></a> 场景三：闹钟响了，任务唤醒 (滴答中断触发)</h3><p><strong>触发时机</strong>：又是 SysTick 中断。 <strong>链表操作</strong>：</p><ol><li><code>xTaskIncrementTick()</code> 除了增加时间，还会死死盯着 <code>xDelayedTaskList</code> 的<strong>第一个节点</strong>（因为它是最早醒的）。</li><li>判断第一个节点的闹钟时间（<code>xItemValue</code>）是否等于或者小于当前的 <code>xTickCount</code>？</li><li>如果时间到了（比如 Task A 睡够了 100 毫秒）：<ul><li><strong>拔出</strong>：把它从 <code>xDelayedTaskList</code> 拔出。</li><li><strong>插回</strong>：重新把它插回对应优先级的 <code>pxReadyTasksLists</code> 的尾部。</li></ul></li><li><strong>抢占判定</strong>：如果刚刚醒来的 Task A 的优先级，<strong>大于或等于</strong>当前正在运行的 Task B 的优先级，立马触发上下文切换！Task B 瞬间被挂起，Task A 抢回 CPU。</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="RTOS" scheme="https://blog.godmao.top/categories/RTOS/"/>
    
    
  </entry>
  
  <entry>
    <title>FreeRTOS的内部机制2</title>
    <link href="https://blog.godmao.top/posts/46927/"/>
    <id>https://blog.godmao.top/posts/46927/</id>
    <published>2026-03-26T08:11:02.000Z</published>
    <updated>2026-04-14T14:45:25.261Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><h2 id="1-freertos中的链表结构"><a class="markdownIt-Anchor" href="#1-freertos中的链表结构"></a> 1. FreeRTOS中的链表结构</h2><p>上一节我们介绍了任务创建的部分，但对于链表部分没有深入分析，这章我们就来分析一下这个最核心的数据结构，FreeRTOS底层逻辑。理解FreeRTOS的链表，是理解任务调度、时间管理以及各种通信机制（队列、信号量）的基础。</p><blockquote><p>任务的状态切换，本质上就是把任务的TCB从一个链表拔出来，插进另一个链表里。<br />——鲁迅</p></blockquote><h3 id="11-链表结构详解"><a class="markdownIt-Anchor" href="#11-链表结构详解"></a> 1.1 链表结构详解</h3><p>FreeRTOS 使用的是<strong>双向环形链表</strong>。在 <code>include/list.h</code> 中，有两个最核心的结构体。</p><ol><li><p>链表节点：<code>ListItem_t</code> (挂在TCB身上的钩子)</p> <pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">struct</span> <span class="token class-name">xLIST_ITEM</span><span class="token punctuation">&#123;</span>    TickType_t xItemValue<span class="token punctuation">;</span>             <span class="token comment">/* 节点的值。用于排序（比如延时阻塞时，按唤醒时间点排序） */</span>    <span class="token keyword">struct</span> <span class="token class-name">xLIST_ITEM</span> <span class="token operator">*</span> pxNext<span class="token punctuation">;</span>        <span class="token comment">/* 指向下一个节点 */</span>    <span class="token keyword">struct</span> <span class="token class-name">xLIST_ITEM</span> <span class="token operator">*</span> pxPrevious<span class="token punctuation">;</span>    <span class="token comment">/* 指向上一个节点 */</span>    <span class="token keyword">void</span> <span class="token operator">*</span> pvOwner<span class="token punctuation">;</span>                    <span class="token comment">/* 核心！指向这个节点的主人（通常是 TCB 的指针） */</span>    <span class="token keyword">struct</span> <span class="token class-name">xLIST</span> <span class="token operator">*</span> pvContainer<span class="token punctuation">;</span>        <span class="token comment">/* 指向这个节点当前所在的链表（用来检查任务到底在哪） */</span><span class="token punctuation">&#125;</span><span class="token punctuation">;</span><span class="token keyword">typedef</span> <span class="token keyword">struct</span> <span class="token class-name">xLIST_ITEM</span> ListItem_t<span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>还记得我们在 TCB 结构体里看到的 <code>xStateListItem</code> 和 <code>xEventListItem</code> 吗？它们就是这个结构体。 可以把 <code>ListItem_t</code> 想象成 TCB 伸出来的“钩子”。RTOS 调度器从来不直接把 TCB 放进链表，而是把 TCB 上的这根“钩子”挂进链表。</p><ul><li><code>pvOwner</code> 的设计：如何通过子结构体成员反向寻回其父结构体？你可能会想到Linux里的 <code>container_of</code> 宏。不过更有效的方法就是直接在子结构体成员中直接嵌入父结构体的地址，这样就能快速寻回。简单来说，当调度器在链表里找到一个节点时，通过 <code>pvOwner</code> 就能顺藤摸瓜，直接找到这个节点所属的 TCB。</li></ul></li></ol><hr /><ol start="2"><li><p>链表根节点：<code>List_t</code></p> <pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">typedef</span> <span class="token keyword">struct</span> <span class="token class-name">xLIST</span><span class="token punctuation">&#123;</span>    UBaseType_t uxNumberOfItems<span class="token punctuation">;</span>    <span class="token comment">/* 链表里现在有几个节点 */</span>    ListItem_t <span class="token operator">*</span> pxIndex<span class="token punctuation">;</span>           <span class="token comment">/* 游标指针：用于遍历链表（同优先级时间片轮转就靠它） */</span>    MiniListItem_t xListEnd<span class="token punctuation">;</span>        <span class="token comment">/* 链表的尾部/锚点（为了节省一点点内存，去掉了Owner属性的精简版节点） */</span><span class="token punctuation">&#125;</span> List_t<span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>如果 <code>ListItem_t</code> 是挂在衣服（TCB）上的钩子，那 <code>List_t</code> 就是衣柜里的“衣架横杆”。系统里定义了各种各样的“衣架”，在 <code>task.c</code> 中定义：</p> <pre class="line-numbers language-c" data-language="c"><code class="language-c">PRIVILEGED_DATA <span class="token keyword">static</span> List_t pxReadyTasksLists<span class="token punctuation">[</span> configMAX_PRIORITIES <span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token comment">/**&lt; Prioritised ready tasks. */</span>PRIVILEGED_DATA <span class="token keyword">static</span> List_t xDelayedTaskList1<span class="token punctuation">;</span>                         <span class="token comment">/**&lt; Delayed tasks. */</span>PRIVILEGED_DATA <span class="token keyword">static</span> List_t xDelayedTaskList2<span class="token punctuation">;</span>                         <span class="token comment">/**&lt; Delayed tasks (two lists are used - one for delays that have overflowed the current tick count. */</span>PRIVILEGED_DATA <span class="token keyword">static</span> List_t <span class="token operator">*</span> <span class="token keyword">volatile</span> pxDelayedTaskList<span class="token punctuation">;</span>              <span class="token comment">/**&lt; Points to the delayed task list currently being used. */</span>PRIVILEGED_DATA <span class="token keyword">static</span> List_t <span class="token operator">*</span> <span class="token keyword">volatile</span> pxOverflowDelayedTaskList<span class="token punctuation">;</span>      <span class="token comment">/**&lt; Points to the delayed task list currently being used to hold tasks that have overflowed the current tick count. */</span>PRIVILEGED_DATA <span class="token keyword">static</span> List_t xPendingReadyList<span class="token punctuation">;</span>                         <span class="token comment">/**&lt; Tasks that have been readied while the scheduler was suspended.  They will be moved to the ready list when the scheduler is resumed. */</span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">if</span> <span class="token expression"><span class="token punctuation">(</span> INCLUDE_vTaskSuspend <span class="token operator">==</span> <span class="token number">1</span> <span class="token punctuation">)</span></span></span>    PRIVILEGED_DATA <span class="token keyword">static</span> List_t xSuspendedTaskList<span class="token punctuation">;</span> <span class="token comment">/**&lt; Tasks that are currently suspended. */</span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">endif</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><p>**<em>pxReadyTasksLists[configMAX_PRIORITIES]</em></p><p>用于挂载就绪任务。<br />这是一个链表数组。<code>configMAX_PRIORITIES</code> 定义的是最大的优先级，默认应该是5。<br />说明 FreeRTOS 为每一个可能设定的优先级都单独准备了一个链表，优先级相同则在同一链表，调度时根据优先级查表，需要注意的是FreeRTOS 等级数字大的优先级高（5最优先）。</p></li><li><p><em><strong>xDelayedTaskList1 &amp; 2</strong></em></p><p>用于挂载阻塞和延时任务。<br />在注释中也说了，两个链表是为了防止系统节拍溢出的问题。下面是一个例子：<br />首先系统节拍（Tick）是用一个整型变量记录的（比如 32 位）。假设当前 Tick 是 <code>0xFFFFFFF0</code>，任务 A 调用了 <code>vTaskDelay(100)</code>。任务 A 应该在 <code>0xFFFFFFF0 + 100</code> = <code>0x00000054</code> （溢出后）醒来。如果只有一个延时链表，并且按照唤醒时间从小到大排序，调度器在比较时间时就会彻底混乱：<code>0x00000054</code> 在数值上比当前的 <code>0xFFFFFFF0</code> 还要小，这该怎么比？<br />对此的解决办法是采用双链表：</p><ol><li><p><strong>当前链表 (由<code>pxDelayedTaskList</code>指针指向的链表)：</strong> 存放那些在Tick 溢出前就会醒来的任务。</p></li><li><p><strong>溢出链表 (由<code>pxOverflowDelayedTaskList</code>指针指向的链表)：</strong> 存放那些唤醒时间越过了溢出界限（也就是唤醒时间在数值上小于当前时间）的任务，比如刚才的任务 A。</p></li></ol></li><li><p><em><strong>pxDelayedTaskList</strong></em>、<em><strong>pxOverflowDelayedTaskList</strong></em></p><p>注意看这两个并不是链表类型，而是指针类型，指向上面提到的两个双delay链表。它由一个非常巧妙的运用：<br />当系统 Tick 计满溢出变为 0 时，内核<strong>直接把这两个指针互换</strong>。溢出链表变成了当前链表，无缝衔接，继续运行。</p></li><li><p><em><strong>xPendingReadyList</strong></em></p><p>用于暂存“刚刚解除阻塞，但由于特殊原因目前还不能放入 Ready 链表”的任务。<br />运行的任务 A 正好在修改就绪链表（比如它刚好在创建一个新任务，或者动态改变某个任务的优先级）它通常会调用 <code>vTaskSuspendAll()</code>让调度器挂起（不能进行任务切换），以保证修改就绪链表不被打断或就绪链表被其他任务修改。</p><p>但注意：<strong>此时硬件中断是依然开启的</strong>。假设调度器正挂起时，一个串口接收中断触发了。ISR（中断服务函数）释放了一个信号量，这个信号量恰好唤醒了一个高优先级任务 B。<br />此时的情况：</p><ol><li>由于调度器被挂起了，内核不能现在就把任务 B 放进前面讲的<code>pxReadyTasksLists</code> 里去打乱正在进行的内核逻辑。如若直接强行把任务 B 塞进就绪链表，就会发生严重的数据竞争——同时修改同一个双向链表的指针。</li><li>任务 B 确实已经不属于 Blocked 状态了，不能继续待在 Delayed 链表里。</li></ol><p>此时内核会把任务 B 暂时挂到 <code>xPendingReadyList</code>。代码执行到 <code>xTaskResumeAll()</code> 恢复调度器时，内核会检查这个 Pending 链表。如果里面有任务，就会把它们全部转移到对应的 <code>pxReadyTasksLists</code> 中，然后触发一次上下文切换。</p></li><li><p><em><strong>xSuspendedTaskList</strong></em></p><p>用于挂载被挂起的任务。这里只需注意区分挂起和阻塞。</p><ul><li><p>阻塞是因为<strong>等待某个具体事件</strong>（等待信号量、队列有数据）或者<strong>等待时间到达</strong>（<code>vTaskDelay</code>）而进入<strong>被动</strong>休眠。一旦外部因素解除则会加入就绪链表。</p></li><li><p>而挂起则是<strong>主动</strong>的休眠（用户程序主动调用了<code>vTaskSuspend( xTaskHandle )</code>），进入挂起态后，没有运行资格，或者说系统直接无视这个任务，要想重新运行，只能是系统里的另一个活跃任务显式调用<code>vTaskResume(TaskHandle)</code>或其衍生函数。</p></li></ul></li></ul></li></ol><p>双向环形链表：<br /><img src="Snipaste_2026-03-26_18-57-25.png" alt="" /></p><h2 id="2-创建任务函数对链表的操作"><a class="markdownIt-Anchor" href="#2-创建任务函数对链表的操作"></a> 2. 创建任务函数对链表的操作</h2><ol><li><p>初始化</p> <pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token function">vListInitialiseItem</span><span class="token punctuation">(</span> <span class="token operator">&amp;</span><span class="token punctuation">(</span> pxNewTCB<span class="token operator">-></span>xStateListItem <span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这个函数只做两件事：</p><ol><li><p><code>pvContainer = NULL;</code> （标记这个钩子目前哪儿也没挂）。</p></li><li><p><code>pvOwner = pxNewTCB;</code> （把钩子的主人指向刚刚创建的这个 TCB。以后谁拿到这个钩子，谁就能找到这个 TCB）。</p></li></ol></li><li><p>挂入就绪链表 (<code>prvAddTaskToReadyList</code>)<br />需要注意这个函数的这个地方：</p> <pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token comment">/* If the scheduler is not already running, make this task the* current task if it is the highest priority task to be created* so far. */</span><span class="token keyword">if</span><span class="token punctuation">(</span> pxCurrentTCB<span class="token operator">-></span>uxPriority <span class="token operator">&lt;=</span> pxNewTCB<span class="token operator">-></span>uxPriority <span class="token punctuation">)</span><span class="token punctuation">&#123;</span>pxCurrentTCB <span class="token operator">=</span> pxNewTCB<span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span class="token keyword">else</span><span class="token punctuation">&#123;</span><span class="token function">mtCOVERAGE_TEST_MARKER</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ol><p>pxCurrentTCB是一个全局变量，指向当前运行的TCB。当调度器首次调度时，最先执行的任务设定为<strong>最高优先级，最后创建的任务。</strong></p><h2 id="3-链表的操作"><a class="markdownIt-Anchor" href="#3-链表的操作"></a> 3. 链表的操作</h2><p>此节需要你理解环形链表的基本操作（如插入节点）</p><h3 id="31-链表节点按值排序插入"><a class="markdownIt-Anchor" href="#31-链表节点按值排序插入"></a> 3.1 <strong>链表节点按值排序插入</strong></h3><p>这里讲一个FreeRTOS中比较重要的一个链表操作——<strong>链表节点按值排序插入</strong>。也就是通过比较链表结构体中的 <code>xItemValue</code> 的值将节点按序插入。</p><h4 id="311-作用"><a class="markdownIt-Anchor" href="#311-作用"></a> 3.1.1 作用</h4><p>在阻塞链表中，<code>xItemValue</code> 指代的是阻塞时间，按序插入，我们就能快速判断任务是否到达延时（如果第一个任务都没有到达延时，那后面的也肯定不会到达），以及快速取出第一个到达延时的任务（就是第一个任务）。非常的高效</p><h4 id="312-流程"><a class="markdownIt-Anchor" href="#312-流程"></a> 3.1.2 流程</h4><p>我复刻了一下，大致是这样的：<br />其中 <code>MultiUseValue</code> 即 <code>xItemValue</code></p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">void</span> <span class="token function">ListInsertNodeAndSort</span><span class="token punctuation">(</span>List_t <span class="token operator">*</span><span class="token keyword">const</span> theList<span class="token punctuation">,</span> ListNode_t <span class="token operator">*</span>theNodeToInsert<span class="token punctuation">)</span><span class="token punctuation">&#123;</span>    ListNode_t <span class="token operator">*</span>Iterator<span class="token punctuation">;</span>   <span class="token comment">// 迭代器指针，用来记录位置</span>    <span class="token keyword">const</span> <span class="token class-name">uint32_t</span> ValueOfInsertion <span class="token operator">=</span> theNodeToInsert<span class="token operator">-></span>MultiUseValue<span class="token punctuation">;</span>    <span class="token keyword">if</span> <span class="token punctuation">(</span> ValueOfInsertion <span class="token operator">==</span> <span class="token number">0xffffffff</span> <span class="token punctuation">)</span>    <span class="token punctuation">&#123;</span>        <span class="token comment">// 直接插入到尾节点。即HeadNode的前节点。</span>        <span class="token comment">// 此时Iterator 指向当前尾节点（也就是要插入节点的上一个节点）</span>        Iterator <span class="token operator">=</span> theList<span class="token operator">-></span>HeadNode<span class="token punctuation">.</span>PrevNode<span class="token punctuation">;</span>    <span class="token punctuation">&#125;</span><span class="token keyword">else</span>    <span class="token punctuation">&#123;</span>        <span class="token comment">// 正常遍历：从头节点开始往下找</span>        <span class="token comment">// 用一个 for 循环，只要下一个节点的值 &lt;= valueOfInsertion，Iterator 就继续往下走</span>        <span class="token comment">// 由于是先比较，后更新节点位置，所以当循环结束时，Iterator 也是我们要插入节点的前一个节点</span>        <span class="token keyword">for</span> <span class="token punctuation">(</span> Iterator <span class="token operator">=</span> <span class="token punctuation">(</span>ListNode_t <span class="token operator">*</span><span class="token punctuation">)</span> <span class="token operator">&amp;</span><span class="token punctuation">(</span> theList<span class="token operator">-></span>HeadNode <span class="token punctuation">)</span><span class="token punctuation">;</span>              Iterator<span class="token operator">-></span>NextNode<span class="token operator">-></span>MultiUseValue <span class="token operator">&lt;=</span> ValueOfInsertion<span class="token punctuation">;</span>              Iterator <span class="token operator">=</span> Iterator<span class="token operator">-></span>NextNode <span class="token punctuation">)</span>        <span class="token punctuation">&#123;</span>            <span class="token comment">// 空循环，靠 for 括号里的逻辑推进即可</span>        <span class="token punctuation">&#125;</span>    <span class="token punctuation">&#125;</span>    <span class="token comment">// 下面开始链表节点移植手术</span>    <span class="token comment">// 请注意顺序！由于我们要靠Iterator->NextNode寻找要插入节点的后节点，所以Iterator->NextNode的值应该最后改变，</span>    <span class="token comment">// 因此先链接后节点再链接前节点</span>    <span class="token comment">// 推荐的顺序：</span>    <span class="token comment">//1. 先把要插入节点的前后节点指定，形成单向链</span>    theNodeToInsert<span class="token operator">-></span>NextNode <span class="token operator">=</span> Iterator<span class="token operator">-></span>NextNode<span class="token punctuation">;</span>    theNodeToInsert<span class="token operator">-></span>PrevNode <span class="token operator">=</span> Iterator<span class="token punctuation">;</span>    <span class="token comment">//2. 把要插入节点的后节点PrevNode更新，补全后节点的双向链</span>    Iterator<span class="token operator">-></span>NextNode<span class="token operator">-></span>PrevNode <span class="token operator">=</span> theNodeToInsert<span class="token punctuation">;</span>    <span class="token comment">//3. 最后更新插入节点的前节点NextNode，补全前节点的双向链</span>    Iterator<span class="token operator">-></span>NextNode <span class="token operator">=</span> theNodeToInsert<span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="RTOS" scheme="https://blog.godmao.top/categories/RTOS/"/>
    
    
  </entry>
  
  <entry>
    <title>FreeRTOS的内部机制1</title>
    <link href="https://blog.godmao.top/posts/46607/"/>
    <id>https://blog.godmao.top/posts/46607/</id>
    <published>2026-03-24T10:03:11.000Z</published>
    <updated>2026-03-30T08:35:20.997Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><h2 id="1-从栈的角度来看rtos整体"><a class="markdownIt-Anchor" href="#1-从栈的角度来看rtos整体"></a> 1. 从栈的角度来看RTOS整体</h2><p>从 RTOS 的宏观视角来看，栈的核心作用可以归结为以下四个维度：</p><h3 id="11-任务独立性的根本"><a class="markdownIt-Anchor" href="#11-任务独立性的根本"></a> 1.1 任务独立性的根本</h3><p>——一任务一栈。</p><p>在裸机系统里，整个系统只有一个大循环，所有的函数共享同一个巨大的系统栈。 但在 RTOS 中，最大的特点是“多任务并发”。为了让每个任务都感觉自己独占了整个 CPU，RTOS 必须为每一个创建的任务分配一块<strong>完全独立、私有的栈内存。</strong></p><ul><li>当你在 FreeRTOS 中调用 <code>xTaskCreate()</code> 时，传入的参数之一就是“栈深度（Stack Size）”。</li><li>任务的局部变量、嵌套调用的函数现场，全都保存在它自己的这块私有领地里。没有这块私有栈，所谓的“多任务”就无从谈起。</li></ul><h3 id="12-上下文切换的载体"><a class="markdownIt-Anchor" href="#12-上下文切换的载体"></a> 1.2 上下文切换的载体</h3><p>——最核心的作用。</p><p><strong>当发生任务切换时（例如经过了一个 SysTick，或者任务 A 主动调用 <code>vTaskDelay</code>）：</strong></p><ol><li><p><strong>冻结现场（压栈）：</strong> 硬件和 RTOS 的底层汇编代码，会强行把当前 CPU 里的所有通用寄存器（R0-R12, LR, PC, xPSR 等）全部一股脑压入 <strong>任务 A 的私有栈</strong> 中。</p></li><li><p><strong>保存指针：</strong> RTOS 把当前任务 A 压栈后最新的栈指针（SP）的值，保存到任务 A 的 <strong>任务控制块（TCB, Task Control Block）</strong> 里。</p></li><li><p><strong>切换指针：</strong> RTOS 找到优先级最高的就绪任务 B，从任务 B 的 TCB 中取出它上次保存的栈指针（SP）值，强行塞给 CPU 的 SP 寄存器。</p></li><li><p><strong>解冻现场（出栈）：</strong> CPU 现在指向了任务 B 的私有栈。执行一次出栈操作，把栈里保存的旧寄存器值全部弹出到 CPU 核心里。</p></li><li><p><strong>恢复执行：</strong> PC（程序计数器）被恢复，系统瞬间穿越回了任务 B 上次被暂停的地方继续执行。</p></li></ol><h3 id="13-中断栈与任务栈的物理隔离"><a class="markdownIt-Anchor" href="#13-中断栈与任务栈的物理隔离"></a> 1.3 中断栈与任务栈的物理隔离</h3><p>——双栈机制，节省 RAM。Cortex-M使用了这种机制。记住要考</p><ul><li><p>让所有的普通任务使用 <strong>PSP（进程栈）</strong>。一旦发生硬件中断，CPU 自动切换到 <strong>MSP（主栈）</strong>。</p></li><li><p><strong>好处：</strong> 中断服务函数（ISR）运行产生的局部变量和嵌套压栈，全部消耗在 MSP 对应的那块系统全局栈上。因此，你给任务分配栈大小时，<strong>只需要考虑该任务自身的业务逻辑深度，完全不需要为任何中断预留空间。</strong> 这极大地节省了总内存。</p></li></ul><h3 id="14-检测系统稳定性"><a class="markdownIt-Anchor" href="#14-检测系统稳定性"></a> 1.4 检测系统稳定性</h3><p>由于每个任务的栈大小在创建时就被写死了（静态分配或从 RTOS 堆中分配固定大小），如果任务内部定义了太大的局部数组（比如 <code>char buf[2048];</code>），或者递归调用太深，就会导致<strong>栈溢出（Stack Overflow）</strong>。 溢出的数据会直接篡改相邻任务的栈或者系统数据，导致极其诡异的 HardFault（硬件错误）。</p><ul><li>RTOS 通常会利用栈来做健康监测。比如 FreeRTOS 会在任务栈的末尾填充特定的魔术字（如 <code>0xA5</code>）。RTOS 在空闲时会去检查这个魔术字有没有被覆盖。如果被覆盖了，说明该任务踩界了，立刻触发 <code>vApplicationStackOverflowHook</code> 报错死机，以防系统带病运行。</li></ul><h2 id="2-创建任务的内部细节"><a class="markdownIt-Anchor" href="#2-创建任务的内部细节"></a> 2. 创建任务的内部细节</h2><h3 id="21-任务的实体tcb-task-control-block"><a class="markdownIt-Anchor" href="#21-任务的实体tcb-task-control-block"></a> 2.1 任务的实体（TCB -Task Control Block）</h3><p>任务在 FreeRTOS的体现就是一个结构体，也可以说是一个任务类（面向对象的说法）。</p><p>在 <code>tasks.c</code> 的头部，你可以找到 <code>tskTCB</code> 结构体的定义。把那些杂七杂八的条件编译宏全删了，核心只剩下这几行：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">typedef</span> <span class="token keyword">struct</span> <span class="token class-name">tskTaskControlBlock</span><span class="token punctuation">&#123;</span>    <span class="token keyword">volatile</span> StackType_t <span class="token operator">*</span>pxTopOfStack<span class="token punctuation">;</span> <span class="token comment">/* 指向任务栈的当前栈顶。绝对的核心！并且必须放在结构体的第一个位置 */</span>    ListItem_t xStateListItem<span class="token punctuation">;</span>          <span class="token comment">/* 状态节点：用来把任务挂到就绪链表或阻塞链表 */</span>    ListItem_t xEventListItem<span class="token punctuation">;</span>          <span class="token comment">/* 事件节点：用来把任务挂到事件（如队列、信号量）等待链表 */</span>        UBaseType_t uxPriority<span class="token punctuation">;</span>             <span class="token comment">/* 任务优先级 */</span>    StackType_t <span class="token operator">*</span>pxStack<span class="token punctuation">;</span>               <span class="token comment">/* 指向任务栈的起始地址（用于销毁任务时释放内存，或栈溢出检测） */</span>    <span class="token keyword">char</span> pcTaskName<span class="token punctuation">[</span> configMAX_TASK_NAME_LEN <span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token comment">/* 任务名字，主要用于调试 */</span><span class="token punctuation">&#125;</span> tskTCB<span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>此节我们聚焦于 <code>pxTopOfStack</code> 和 <code>pxStack</code>。</p><h3 id="22-当我们在创建任务时我们在做什么"><a class="markdownIt-Anchor" href="#22-当我们在创建任务时我们在做什么"></a> 2.2 当我们在创建任务时我们在做什么？</h3><p>在 <code>tasks.c</code> 找到创建任务函数原型：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c">BaseType_t <span class="token function">xTaskCreate</span><span class="token punctuation">(</span> TaskFunction_t pxTaskCode<span class="token punctuation">,</span>                            <span class="token keyword">const</span> <span class="token keyword">char</span> <span class="token operator">*</span> <span class="token keyword">const</span> pcName<span class="token punctuation">,</span>                            <span class="token keyword">const</span> configSTACK_DEPTH_TYPE uxStackDepth<span class="token punctuation">,</span>                            <span class="token keyword">void</span> <span class="token operator">*</span> <span class="token keyword">const</span> pvParameters<span class="token punctuation">,</span>                            UBaseType_t uxPriority<span class="token punctuation">,</span>                            TaskHandle_t <span class="token operator">*</span> <span class="token keyword">const</span> pxCreatedTask <span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>官方文档对参数的释义如下：</p><ul><li><p><em>pvTaskCode</em></p><p>指向<strong>任务入口函数的指针</strong>（即实现任务的函数名称，请参阅如下示例）。 任务通常<a href="https://www.freertos.org/Documentation/02-Kernel/02-Kernel-features/01-Tasks-and-co-routines/05-Implementing-a-task">以无限循环的形式实现</a>；实现任务的函数 绝不能尝试返回或退出。但是，任务可以 <a href="https://www.freertos.org/Documentation/02-Kernel/04-API-references/01-Task-creation/03-vTaskDelete/">自行删除</a>。</p></li><li><p><em>pcName</em></p><p><strong>任务的描述性名称</strong>。此参数主要用于方便调试，但也可用于 <a href="https://www.freertos.org/Documentation/02-Kernel/04-API-references/03-Task-utilities/00-Task-utilities/#xtaskgethandle">获取任务句柄</a>。任务名称的最大长度 由 <a href="https://www.freertos.org/Documentation/02-Kernel/03-Supported-devices/02-Customization">FreeRTOSConfig.h</a> 中的 configMAX_TASK_NAME_LEN 定义。</p></li><li><p><em>uxStackDepth</em></p><p>要<a href="https://www.freertos.org/Documentation/02-Kernel/02-Kernel-features/09-Memory-management/01-Memory-management">分配</a><strong>用作任务堆栈的字数（不是字节数！）</strong>。例如，如果 堆栈宽度为 16 位，uxStackDepth 为 100，则将分配 200 字节用作任务 堆栈。再举一例，如果堆栈宽度为 32 位，uxStackDepth 为 400， 则将分配 1600 字节用作任务堆栈。堆栈深度与堆栈宽度的乘积不得超过 size_t 类型变量所能包含的最大值。请参阅 常见问题：<a href="https://www.freertos.org/Why-FreeRTOS/FAQs/Memory-usage-boot-times-context#how-big-should-the-stack-be">堆栈应该多大？</a>。</p></li><li><p><em>pvParameters</em></p><p><strong>作为参数传递给所创建任务的值</strong>。如果 pvParameters 设置为某变量的地址， 则在创建的任务执行时，该变量必须仍然存在， 因此，不能传递堆栈变量的地址。</p></li><li><p><em>uxPriority</em></p><p>创建的任务将以该指定<a href="https://www.freertos.org/Documentation/02-Kernel/02-Kernel-features/01-Tasks-and-co-routines/03-Task-priorities"><strong>优先级</strong></a>执行。支持 MPU 的系统 可以通过在 uxPriority 中设置 portPRIVILEGE_BIT 位来选择以特权（系统）模式创建任务。 例如，要创建优先级为 2 的特权任务，请将 uxPriority 设置为 ( 2 | portPRIVILEGE_BIT )。应断言优先级 低于 configMAX_PRIORITIES。如果 configASSERT 未定义，则优先级默认上限为 (configMAX_PRIORITIES - 1)。</p></li><li><p><em>pxCreatedTask</em></p><p>用于将句柄传递至由 xTaskCreate() 函数创建的任务。pxCreatedTask 是可选参数， 可设置为 NULL。</p></li></ul><p><strong>返回：</strong></p><ul><li>如果任务创建成功，则返回 pdPASS，</li><li>否则返回 errCOULD_NOT_ALLOCATE_REQUIRED_MEMORY。</li></ul><blockquote><p>“任务栈” 本质是在堆上，这何尝不是一种NTR呢？&gt;_&gt;</p></blockquote><p>我们查看它的调用层次结构如下：</p><p><img src="Snipaste_2026-03-24_22-02-21.png" alt="" /></p><p>在我们调用它时，直接进入到了一个私有函数，也就是静态函数（<code>prvCreatetask</code>），在<code>prvCreatetask</code> 里，核心就做两件事： malloc 一块内存给 TCB，并初始化为0x00；malloc 另一块内存作为任务的栈。</p><p>ok，TCB分配好内存后，结构体成员尚未初始化，填写结构体成员则由 <code>prvInitialiseNewTask()</code> 这个静态函数来完成：</p><ol><li><p><strong>清理栈内存</strong>：把分配到的栈空间全刷成 <code>0xA5</code>（可配置，为了调试时看栈最高使用了多少）。</p> <pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">if</span> <span class="token expression"><span class="token punctuation">(</span> tskSET_NEW_STACKS_TO_KNOWN_VALUE <span class="token operator">==</span> <span class="token number">1</span> <span class="token punctuation">)</span></span></span><span class="token punctuation">&#123;</span><span class="token comment">/* Fill the stack with a known value to assist debugging. */</span><span class="token punctuation">(</span> <span class="token keyword">void</span> <span class="token punctuation">)</span> <span class="token function">memset</span><span class="token punctuation">(</span> pxNewTCB<span class="token operator">-></span>pxStack<span class="token punctuation">,</span> <span class="token punctuation">(</span> <span class="token keyword">int</span> <span class="token punctuation">)</span> tskSTACK_FILL_BYTE<span class="token punctuation">,</span> <span class="token punctuation">(</span> <span class="token class-name">size_t</span> <span class="token punctuation">)</span> uxStackDepth <span class="token operator">*</span> <span class="token keyword">sizeof</span><span class="token punctuation">(</span> StackType_t <span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">endif</span> <span class="token comment">/* tskSET_NEW_STACKS_TO_KNOWN_VALUE */</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><code>tskSTACK_FILL_BYTE</code> 被定义为0xA5。</p></li><li><p><strong>初始化链表节点</strong>：调用 <code>vListInitialiseItem</code>，把 TCB 里的 <code>xStateListItem</code> 初始化，并把这个节点的主人（owner）设置为当前的 TCB。此节我们略过。</p></li><li><p><strong>伪造栈帧</strong>：调用 <code>pxPortInitialiseStack()</code> 来伪造栈帧。</p></li></ol><p>我们注意到，在TCB中，并没有函数入口（创建函数的参数 <em>pvTaskCode</em>）和函数参数（创建函数的参数 <em>pvParameters</em>）相关成员，难道传入这两个参数是没有意义的吗？如此重要的两个信息记录在哪呢？</p><p>——答案是栈。</p><p>在 <code>prvInitialiseNewTask()</code> 中我们提到其调用 <code>pxPortInitialiseStack()</code>。顾名思义，也就是初始化任务栈。它负责初始化任务控制块（TCB）的各个字段，设置任务名称、优先级、列表项，并初始化任务堆栈。在这里TCB从一块内存转变为一个完全初始化、准备好可被调度的任务控制块。</p><p>这里不得不提到这个函数也关系到任务如何正确地被加载进CPU执行。</p><p>我们还没有讲调度器，但是我们可以先说一下调度器（Scheduler）的工作逻辑：当它决定要运行某个任务时，会从该任务的 TCB（任务控制块）中取出之前保存的栈指针（<em>pxTopOfStack</em>）（保存现场）然后执行一系列出栈（POP）操作，把栈里的数据恢复到 CPU 的寄存器中，最后修改 PC 指针让程序跑起来。</p><p>这就产生了一个问题：如果是一个刚创建的全新任务，程序没运行过，连栈都是空的，何谈现场？如果栈里啥都没有，CPU 的 PC（程序计数器）怎么知道要跳转到你的任务函数？CPU 怎么知道你的任务函数的参数是什么？</p><p><code>pxPortInitialiseStack</code> 的任务之一，就是在任务刚刚创建、还未执行之前，在它的私有栈顶布置好一个“程序运行现场”。伪造它曾经运行过。</p><blockquote><p><code>pxPortInitialiseStack()</code> 在 <code>portable.h</code> 中声明，但实现在具体硬件对应的移植文件中，因为如何初始化栈和硬件强相关(比如栈的增长方向）。</p></blockquote><p>以最常见的 ARM Cortex-M3/M4 为例，伪造逻辑如下：注意寄存器的顺序不能乱！</p><pre class="line-numbers language-c" data-language="c"><code class="language-c">StackType_t <span class="token operator">*</span><span class="token function">pxPortInitialiseStack</span><span class="token punctuation">(</span> StackType_t <span class="token operator">*</span>pxTopOfStack<span class="token punctuation">,</span> TaskFunction_t pxCode<span class="token punctuation">,</span> <span class="token keyword">void</span> <span class="token operator">*</span>pvParameters <span class="token punctuation">)</span><span class="token punctuation">&#123;</span>    <span class="token comment">/* 1. 栈顶指针先向下偏移，留出存放一套 CPU 寄存器的空间+1个EXC_RETURN的位置 */</span>    pxTopOfStack <span class="token operator">-=</span> <span class="token number">17</span><span class="token punctuation">;</span> <span class="token comment">/* Cortex-M 压栈需要保存 16 个寄存器 (XPSR, PC, LR, R12, R3-R0, R11-R4) +1个EXC_RETURN */</span>    <span class="token comment">/* 2. 开始往栈里填入伪造数据 */</span>    pxTopOfStack<span class="token punctuation">[</span> <span class="token number">16</span> <span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token number">0x01000000UL</span><span class="token punctuation">;</span>      <span class="token comment">/* xPSR: 必须设置 Thumb 状态位，否则一运行就硬件错误 */</span>    pxTopOfStack<span class="token punctuation">[</span> <span class="token number">15</span> <span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">(</span> <span class="token class-name">uint32_t</span> <span class="token punctuation">)</span> pxCode<span class="token punctuation">;</span> <span class="token comment">/* PC: 任务函数的入口地址！CPU 出栈后就会直接跳到这里执行 */</span>    pxTopOfStack<span class="token punctuation">[</span> <span class="token number">14</span> <span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">(</span> <span class="token class-name">uint32_t</span> <span class="token punctuation">)</span> prvTaskExitError<span class="token punctuation">;</span> <span class="token comment">/* LR: 返回地址。任务是不允许 return 的，万一 return 了就跳到错误处理函数 */</span>        <span class="token comment">/* R12, R3, R2, R1 随便填，或者清零 */</span>        pxTopOfStack<span class="token punctuation">[</span> <span class="token number">9</span> <span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">(</span> <span class="token class-name">uint32_t</span> <span class="token punctuation">)</span> pvParameters<span class="token punctuation">;</span> <span class="token comment">/* R0: 按照 C 语言调用约定，R0 保存函数的第一个参数 */</span>    pxTopOfStack<span class="token punctuation">[</span> <span class="token number">8</span> <span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token number">0xfffffffd</span><span class="token punctuation">;</span> <span class="token comment">/* EXC_RETURN */</span>    <span class="token comment">/* R11 - R4 随便填，一般填个固定特征值方便调试 (如 0x11111111, 0x04040404 等) */</span>    <span class="token comment">/* 3. 返回最终的栈顶指针，这个指针会被存入 TCB 的 pxTopOfStack 中 */</span>    <span class="token keyword">return</span> pxTopOfStack<span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>该函数执行完后，我们的创建工作也就接近尾声了，最终会形成这样一块内存布局：</p><p><img src="Snipaste_2026-03-24_22-55-57.png" alt="" /></p><p>关于 <code>EXC_RETURN</code>：</p><ul><li><p>在 Cortex-M 架构中，当进入异常时，<code>r14</code>（即 LR 寄存器）会被硬件自动赋一个叫做 <code>EXC_RETURN</code> 的特殊值。</p></li><li><p>这个值决定了 CPU 退出异常时要回到什么模式、使用哪个栈。比如：</p><ul><li><p><code>0xFFFFFFFD</code>：返回线程模式，使用 PSP 栈（普通任务）。</p></li><li><p><code>0xFFFFFFED</code>：返回线程模式，使用 PSP 栈，<strong>并且说明这个任务使用了浮点运算（FPU），硬件需要额外恢复浮点寄存器</strong>。</p></li></ul></li></ul><p>要兼容带 FPU 的 Cortex-M4F 芯片，它不能在汇编里把 <code>r14</code> 写死。它的巧妙做法是：<strong>在初始化任务栈（<code>port_TaskStackInit</code>）的时候，就把对应任务的 <code>EXC_RETURN</code> 值提前存到栈里。</strong></p><p>这就是最终的“任务”。一定注意它的顺序！后面要考。</p><p><strong>当 RTOS 调度器第一次挑中这个任务，执行汇编的上下文切换（出栈操作）时：</strong> 栈里预先布置好的 <code>pxCode</code> 被弹入了 CPU 的 <code>PC</code> 寄存器，<code>pvParameters</code> 被弹入了 <code>R0</code> 寄存器。 CPU 无缝跳转到了你的任务代码，并且完美接收了传进来的参数。任务就此拥有了生命。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="RTOS" scheme="https://blog.godmao.top/categories/RTOS/"/>
    
    
  </entry>
  
  <entry>
    <title>PCB学习笔记2</title>
    <link href="https://blog.godmao.top/posts/13427/"/>
    <id>https://blog.godmao.top/posts/13427/</id>
    <published>2026-03-20T13:28:17.000Z</published>
    <updated>2026-04-03T07:14:22.326Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><h2 id="1-绘制138译码电路"><a class="markdownIt-Anchor" href="#1-绘制138译码电路"></a> 1. 绘制138译码电路</h2><h3 id="11-138译码器ic器件标准"><a class="markdownIt-Anchor" href="#11-138译码器ic器件标准"></a> 1.1 138译码器IC器件标准</h3><p><img src="Snipaste_2026-03-20_21-48-47.png" alt="" /></p><h3 id="12-网络的概念"><a class="markdownIt-Anchor" href="#12-网络的概念"></a> 1.2 网络的概念</h3><p>简单来说，<strong>网络是指电路中所有在电气上相互连接在一起的引脚（Pins）的集合</strong>。你可以把它理解为一个“逻辑上的导电节点”。在这个节点上的任何一点，<strong>理论上电压都是相等的</strong>（忽略导线的微小电阻）。</p><p>当你把原理图导入到 PCB 界面时，元件起初是散乱放置的。此时，属于同一个网络的引脚之间，会有一根根细细的、交叉的直线连接着。</p><p>这就是“飞线”（Ratsnest）。飞线不是实际的铜箔，它只是软件在提示你：“嘿，这两个点在逻辑上属于同一个网络，你一会儿布线的时候记得把它们连起来。”</p><p><img src="Snipaste_2026-03-20_21-56-53.png" alt="" /></p><p>放置网络 <code>M-N</code></p><h3 id="13-自锁开关符号的讲解"><a class="markdownIt-Anchor" href="#13-自锁开关符号的讲解"></a> 1.3 自锁开关符号的讲解</h3><p><img src="Snipaste_2026-03-20_22-07-39.png" alt="" /></p><p>箭头是指按下的方向，松开按钮时，5、6号触点连通；开关按下时，4、5号触点连通。</p><h3 id="14-跳线帽的使用"><a class="markdownIt-Anchor" href="#14-跳线帽的使用"></a> 1.4 跳线帽的使用</h3><p><img src="Snipaste_2026-03-20_22-21-53.png" alt="" /></p><p>如图，排针的2号引脚连接IC器件的PIN。如果没有跳线帽，则2号引脚浮空。若跳线帽连接3、2，则引脚为高电平。</p><h3 id="15-批量修改位号"><a class="markdownIt-Anchor" href="#15-批量修改位号"></a> 1.5 批量修改位号</h3><p>对于同一类型元件，我们可以批量分配/修改位号</p><p>先选中所有要分配/修改位号的元器件，然后把他们的位号都修改为 元器件符号+?（例如 <code>R?</code>) 这个“?”就相当于通配符。</p><p>然后点击设计——分配位号</p><p><img src="Snipaste_2026-03-20_22-57-59.png" alt="" /></p><h3 id="16-对齐工具-长度工具"><a class="markdownIt-Anchor" href="#16-对齐工具-长度工具"></a> 1.6 对齐工具、长度工具</h3><p>对齐工具和长度工具帮助我们规整化PCB。</p><h3 id="17-交叉选择"><a class="markdownIt-Anchor" href="#17-交叉选择"></a> 1.7 交叉选择</h3><p>从原理图对应PCB里的元器件</p><p>选中原理图中的元器件后 <code>Shift+X</code>，则会同步选择出PCB里对应的元器件。</p><h3 id="18-布线的原则"><a class="markdownIt-Anchor" href="#18-布线的原则"></a> 1.8 布线的原则</h3><h4 id="181-从电源开始"><a class="markdownIt-Anchor" href="#181-从电源开始"></a> 1.8.1 从电源开始</h4><ul><li><p>考虑载流能力。线宽能否承载相应电流。可以使用 PCB导线宽度-载流工具 来估算 <strong>建议使用用IPC2152标准</strong>。<br /><a href="https://bbs.eda365.com/article-12-1.html">PCB走线载流计算器-EDA365电子论坛通信数码-人工智能-计算机-半导体-手机家电消费电子硬件门户网站</a></p><p><img src="Snipaste_2026-03-22_16-08-15.png" alt="" /></p><p><strong>温升</strong>：指导线允许的上升温度<br /><strong>内层线宽</strong>：多层板中的内层线。因为内层线在内部，不好散热，温升不好控制。</p><p>该图的含义是，想要承载1A电流，在铜厚1oz、容许温升10度的情况下，需要至少导线宽11.83mill。按照该图，我们可以算出10密尔导线大约能承载885mA电流。</p></li></ul><h4 id="182-不要走直角线-锐角线"><a class="markdownIt-Anchor" href="#182-不要走直角线-锐角线"></a> 1.8.2 不要走直角线、锐角线</h4><p>在同一层（同一过孔）引出的线不要走直角、锐角线。<strong>T型分线除外</strong>。</p><p>如果同一过孔想要引出多条线，可以以135度引。</p><ul><li><p>错误示范——同层锐角线：</p><p><img src="Snipaste_2026-03-22_17-14-10.png" alt="" /></p></li><li><p>建议的引线方式——以135度引出（或T型引出，呈90度。低速信号与电源线适用），不同层的两条线（红和蓝）则可以为锐角：</p><p><img src="Snipaste_2026-03-22_17-14-19.png" alt="" /></p></li></ul><p>不同层则没有限制，甚至鼓励相邻两层的走线走向应当互相垂直以避免电磁耦合。</p><blockquote><p><strong>为什么过孔可以T型分线</strong>？</p><ul><li><strong>过孔是一个大铜盘：</strong> 过孔本身是一个圆形的金属环。当两条线成 180° 对穿，或者成 90° 像 T 字一样从过孔中心向外引出时，它们实际上是分别连接在这个大圆盘的边缘上。</li></ul></blockquote><ul><li><strong>消灭了锐角死胡同：</strong> 因为有中间那个巨大的圆形焊盘作为“缓冲地带”，两条导线之间并没有直接形成一个狭窄的“V”型或“L”型内角。因此，蚀刻药水不会卡在这里，完美避开了“酸角”效应和铜皮剥离的机械应力问题。（如果配合泪滴，就更加平滑牢固了）。</li></ul><h3 id="19-持续高亮选中的网络"><a class="markdownIt-Anchor" href="#19-持续高亮选中的网络"></a> 1.9 持续高亮选中的网络</h3><p><code>Shift+H</code>。</p><h3 id="110-铺铜"><a class="markdownIt-Anchor" href="#110-铺铜"></a> 1.10 铺铜</h3><p>用于连接GND网络。</p><p>两种与铜的连接方式 点开设计——设计规则<br /><img src="Snipaste_2026-03-22_18-16-37.png" alt="" /></p><p>单层焊盘：贴片式的就是单层<br />多层：多见于插式元器件</p><ul><li>发散<br /><img src="Snipaste_2026-03-22_18-20-21.png" alt="" /></li><li>直连：适合大电流<br /><img src="Snipaste_2026-03-22_18-20-35.png" alt="" /></li></ul><p>改完后需要点击PCB铺铜区域——<strong>重建铺铜区</strong>。</p><h3 id="111-泪滴"><a class="markdownIt-Anchor" href="#111-泪滴"></a> 1.11 泪滴</h3><p>点开工具——泪滴</p><p><img src="Snipaste_2026-03-22_18-24-23.png" alt="" /></p><p>它会在导线和过孔连接的过渡区域，自动铺设一层平滑的水滴状铜皮。不仅能完美消除应力集中，还能加固连接点，彻底避免钻孔偏差导致的断线问题。</p><h3 id="112-丝印"><a class="markdownIt-Anchor" href="#112-丝印"></a> 1.12 丝印</h3><p>略</p><h3 id="113-usb供电"><a class="markdownIt-Anchor" href="#113-usb供电"></a> 1.13 USB供电</h3><p>简单介绍<br /><img src="Snipaste_2026-03-22_21-12-37.png" alt="" /></p><p>OTG引脚，若接地，则表明该设备为主设备，向外部供电；若接VCC或高阻态则表明该设备为从设备，向外部索要电源供给。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="嵌入式" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/"/>
    
    <category term="PCB" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/PCB/"/>
    
    
  </entry>
  
  <entry>
    <title>PCB学习笔记1</title>
    <link href="https://blog.godmao.top/posts/13619/"/>
    <id>https://blog.godmao.top/posts/13619/</id>
    <published>2026-03-19T08:57:38.000Z</published>
    <updated>2026-03-28T08:22:02.402Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><h2 id="1-从点灯开始"><a class="markdownIt-Anchor" href="#1-从点灯开始"></a> 1. 从点灯开始</h2><h3 id="11-led的选型及电路原理"><a class="markdownIt-Anchor" href="#11-led的选型及电路原理"></a> 1.1 LED的选型及电路原理。</h3><p>开始绘制原理图之前，首先对元器件进行选型，在<a href="https://www.szlcsc.com/">立创商城</a>或者在原理图绘制界面使用快捷键 <strong><code>shift+F</code></strong> 打开元器件选型界面选型。</p><p>比如这款LED：<br /><img src="Snipaste_2026-03-19_17-05-11.png" alt="" /></p><p>编号是 C2936518，这是供应商编号，也是器件的唯一标识。</p><p>我们可以查看它的参数——正向电流是20mA（20mAMAX），压降是2V。</p><p>选型可不是乱选，我们要计算。我们使用3V的纽扣电池点亮它的话，需要什么条件呢？</p><p>首先它吃掉2v的电压，还剩1v，如果我们什么都不接的话，1V 几乎全部加在极小的导线电阻 + 电源内阻上，电流会非常大，远超 LED 正常工作电流20mA。所以我们必需要接一个限流电阻，分担这1v。那我们简单计算一下就知道可以选择50欧姆电阻，这样整个串联电路电流就是20mA，刚好满足led的最大工作电流。那我们保守一点，用10mA的电流使LED工作也是可以的，就是暗一点而已，这样我们就选择100欧姆的限流电阻。</p><p>接下来就是画原理图了很简单的：<br /><img src="Snipaste_2026-03-19_17-27-57.png" alt="" /></p><p>主要用到的快捷键：</p><table><thead><tr><th>快捷键</th><th>含义</th></tr></thead><tbody><tr><td>M-W</td><td>画线</td></tr><tr><td>SHI-F</td><td>搜索在线元件库</td></tr><tr><td>鼠标右键</td><td>取消操作/长按拖动</td></tr><tr><td>选中元器件+space</td><td>旋转</td></tr><tr><td>选中元器件+X/Y</td><td>左右反转/上下反转</td></tr><tr><td>DEL</td><td>删除</td></tr></tbody></table><p>涉及位号缩写：</p><table><thead><tr><th>缩写</th><th>全称</th></tr></thead><tbody><tr><td>BAT</td><td>Battery</td></tr><tr><td>SW</td><td>Switch</td></tr><tr><td>U</td><td>Integrated Circuit（集成电路I/C）</td></tr><tr><td>R</td><td>Resistor</td></tr></tbody></table><h3 id="12-drc检查"><a class="markdownIt-Anchor" href="#12-drc检查"></a> 1.2 DRC检查</h3><p>画完原理图后，要进行DRC检查，看看有没有什么错误。</p><p><img src="Snipaste_2026-03-19_18-11-38.png" alt="" /></p><p>发现警告：<br /><img src="Snipaste_2026-03-19_17-32-40.png" alt="" /></p><p><strong>警告1</strong>是因为之前我们改了电源的名字属性为CR2032，软件判断属性与它元件库里的默认元件属性不符，如果你直接使用这个封装，系统无法直接识别你的元器件是啥，但是这个元器件要是你熟悉的并且是自己买元器件焊接那其实只要他的封装能用那么无所谓。</p><p>解决方法是不要修改元器件的属性或者器件标准化，使用在线元件库里的元件替换你的元件。<br /><img src="Snipaste_2026-03-19_18-16-35.png" alt="" /></p><p><strong>警告2</strong>则是画图标准的问题，一般悬空引脚都要有标识（非连接标识<br /><img src="Snipaste_2026-03-19_18-18-24.png" alt="" /></p><p>也可以化成如下：<br /><img src="Snipaste_2026-03-19_19-08-26.png" alt="" /></p><h3 id="13-绘制pcb"><a class="markdownIt-Anchor" href="#13-绘制pcb"></a> 1.3 绘制PCB</h3><p>双击PCB项，使用 <code>M-I</code> 导入原理图/导入原理图变更。</p><p>切换层级略。</p><p>同样 <code>M-W</code> 布线。</p><p><img src="Snipaste_2026-03-19_19-32-25.png" alt="" /></p><p><code>M-V</code> 放置过孔。</p><p>关于单位：<br />- 密尔：英制单位，<strong>应默认使用</strong><br />- 毫米：一般在机械部分用（例如板框边长）<br />- 切换快捷键：<code>Q</code></p><p>总结：</p><table><thead><tr><th>快捷键</th><th>描述</th></tr></thead><tbody><tr><td>M-I</td><td>导入原理图/导入原理图变更</td></tr><tr><td>M-V</td><td>放置过孔</td></tr><tr><td>Q</td><td>切换单位</td></tr></tbody></table><h3 id="14-drc检查pcb"><a class="markdownIt-Anchor" href="#14-drc检查pcb"></a> 1.4 DRC检查PCB</h3><p>画完PCB同样要DRC检查。</p><p>关于设计规则。</p><p><img src="Snipaste_2026-03-19_19-24-28.png" alt="" /></p><p>安全间距中的两个选项 <code>...1oz</code> <code>...2oz</code> 实际上就是指的不同铜厚的间距规则，1盎司铜大概相当于铜厚0.35mm，对于初学者，很少主动修改设计规则，但是我们需要知道这些设计规则的决定因素。比如这个间距的规则就和加工PCB厂家的工艺能力有关，例如JLC的工艺能力就可以在官网找到：<a href="https://www.jlc.com/portal/vtechnology.html?spm=PCB.Homepage.functionbar.1015">嘉立创PCB工艺加工能力范围说明</a></p><p>除此之外，还有电气因素的影响，例如电压越高，导线间隙应越远；电流越大，导线应越粗等。</p><h3 id="15-导出文件"><a class="markdownIt-Anchor" href="#15-导出文件"></a> 1.5 导出文件</h3><p>在上方菜单栏选择 导出——嘉立创PCB下单，会生成三个文件：</p><p><img src="Snipaste_2026-03-19_19-34-14.png" alt="" /></p><ul><li>Gerber：涉及PCB的导线、丝印等</li><li>坐标：一般是PCB上要打的孔</li><li>BOM：Bill of MAterials 物料清单，记录要用到的元器件。</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="嵌入式" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/"/>
    
    <category term="PCB" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/PCB/"/>
    
    
  </entry>
  
  <entry>
    <title>Project001-开发日记</title>
    <link href="https://blog.godmao.top/posts/50539/"/>
    <id>https://blog.godmao.top/posts/50539/</id>
    <published>2026-03-17T11:28:32.000Z</published>
    <updated>2026-04-03T07:20:36.081Z</updated>
    
    <content type="html"><![CDATA[<div class="hbe hbe-container" id="hexo-blog-encrypt" data-wpm="ERROR。" data-whm="这个文章不能被校验, 不过你还是能看看解密后的内容。">  <script id="hbeData" type="hbeData" data-hmacdigest="70ac6fa47f1030502f795377b0517158655770e3cd4de0c53967dc76eb3e3747"></script>  <div class="hbe hbe-content">    <div class="hbe hbe-input hbe-input-up">      <input class="hbe hbe-input-field hbe-input-field-up" type="password" id="hbePass">      <label class="hbe hbe-input-label hbe-input-label-up" for="hbePass">        <span class="hbe hbe-input-label-content hbe-input-label-content-up">文章已被隐藏，可能为私密内容或还未完成。</span>      </label>    </div>  </div></div><script data-pjax src="/lib/hbe.js"></script><link href="/css/hbe.style.css" rel="stylesheet" type="text/css">]]></content>
    
    
    <summary type="html">???</summary>
    
    
    
    <category term="开发日记" scheme="https://blog.godmao.top/categories/%E5%BC%80%E5%8F%91%E6%97%A5%E8%AE%B0/"/>
    
    
  </entry>
  
  <entry>
    <title>通信协议：SPI</title>
    <link href="https://blog.godmao.top/posts/23186/"/>
    <id>https://blog.godmao.top/posts/23186/</id>
    <published>2026-03-14T07:57:32.000Z</published>
    <updated>2026-03-14T10:33:57.228Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><h2 id="1-什么是spi"><a class="markdownIt-Anchor" href="#1-什么是spi"></a> 1. 什么是SPI？</h2><p>SPI（Serial Peripheral Interface，串行外设接口）是由摩托罗拉（Motorola）公司开发的一种<strong>同步、全双工、单主多从式</strong>的通信协议。</p><p>不用被这些名词吓到，我们拆解来看：</p><ul><li><strong>同步：</strong> 意味着通信双方有一个共同的时钟（节拍器）来指挥数据的收发，步调一致。</li><li><strong>全双工：</strong> 数据可以同时双向传输。就像你在打电话，你可以一边说话一边听对方说话。</li><li><strong>单主多从式：</strong> 网络中有且只有一个“老大”（Master），其他都是“小弟”（Slave）。老大负责提供时钟节拍，并决定什么时候和哪个小弟说话（CS）。</li></ul><h2 id="2-工作原理"><a class="markdownIt-Anchor" href="#2-工作原理"></a> 2. 工作原理</h2><p>它的底层硬件逻辑其实极其简单粗暴——核心就是<strong>移位寄存器（Shift Register）</strong>。</p><p>在STM32（主机）和你的屏幕或传感器（从机）内部，各有一个8位（或16位）的移位寄存器。这两个寄存器通过 MOSI 和 MISO 两根线首尾相连，形成了一个<strong>环形结构</strong>。</p><p><strong>工作过程就是转圈交换数据：</strong></p><p>主机将 CS 线拉低。</p><ol><li><p><strong>准备阶段：</strong> 主机把要发送的数据放到自己的移位寄存器里；从机也把要返回的数据放到自己的移位寄存器里。</p></li><li><p><strong>时钟启动：</strong> 主机的 SCLK 引脚开始发出时钟脉冲。</p></li><li><p><strong>环形移位：</strong> 每来一个时钟脉冲的边沿（取决于SPI的工作模式）：</p><ul><li>主机寄存器里的最高位（MSB）通过 MOSI 线被挤出去，跑进从机寄存器的最低位。</li><li>同时，从机寄存器里的最高位（MSB）通过 MISO 线被挤出去，跑进主机寄存器的最低位。</li></ul></li><li><p><strong>完成交换：</strong> 经过连续的8个时钟脉冲，主从机内部的8位数据刚好完整地转了一圈。主机的发出去的数据到了从机，从机的数据也到了主机。</p></li><li><p><strong>锁存</strong>：当CS线再次被拉高，数据被锁存。就可以开始解析数据了。</p></li></ol><p>因此，SPI的发送和接收是<strong>绑定在一起同时发生的</strong>。</p><ul><li>主机哪怕只是纯发送，它的MISO引脚也会在底层同步接收从机发来的数据（虽然你可以忽略这些垃圾数据）。</li><li>反之，如果主机只想读数据，也必须主动发送一个“伪数据”（通常是<code>0xFF</code>或<code>0x00</code>），产生8个时钟节拍，才能把从机里的数据给“挤”回来。</li></ul><p><img src="Snipaste_2026-03-14_17-02-54.png" alt="" /></p><p><img src="Snipaste_2026-03-14_17-15-17.png" alt="" /></p><h2 id="3-标准spi信号线"><a class="markdownIt-Anchor" href="#3-标准spi信号线"></a> 3. 标准SPI信号线</h2><p>SPI接口一般使用四条信号线通信：<strong>SDI（数据输入），SDO（数据输出），SCK（时钟），CS（片选）</strong></p><p>亦即：</p><ol><li><p><strong>SCLK (Serial Clock) - 串行时钟线：</strong> 由主机产生，就像乐队指挥的指挥棒。时钟的频率决定了通信的速度。</p></li><li><p><strong>MOSI (Master Output Slave Input) - 主出从入：</strong> 主机发送数据，从机接收数据的通道。</p></li><li><p><strong>MISO (Master Input Slave Output) - 主入从出：</strong> 主机接收数据，从机发送数据的通道。</p></li><li><p><strong>CS/NSS (Chip Select / Slave Select) - 片选线：</strong> 由主机控制，低电平有效。当主机想要和某个特定的从机通信时，就会把连接那个从机的CS线拉低（变成0V）。这就好比上课时老师点名：“那个CS线被拉低的同学，站起来回答问题！”</p></li></ol><blockquote><p>因为每个从机都需要一根独立的CS线，所以如果从机太多，会占用主机较多的GPIO引脚。</p></blockquote><h2 id="4-spi工作模式"><a class="markdownIt-Anchor" href="#4-spi工作模式"></a> 4. SPI工作模式</h2><p>SPI允许配置时钟的极性和相位，组合出四种工作模式。</p><ul><li><p><strong>CPOL (Clock Polarity - 时钟极性)：</strong> 决定了SPI空闲时（没有数据传输时），SCLK线是高电平还是低电平。</p><ul><li><p><code>CPOL = 0</code>：空闲时SCLK为低电平。</p></li><li><p><code>CPOL = 1</code>：空闲时SCLK为高电平。</p></li></ul></li><li><p><strong>CPHA (Clock Phase - 时钟相位)：</strong> 决定了数据是在时钟的第几个跳变沿（上升沿或下降沿）被采样（读取）。</p><ul><li><p><code>CPHA = 0</code>：在第1个跳变沿采样数据。</p></li><li><p><code>CPHA = 1</code>：在第2个跳变沿采样数据。</p></li></ul></li></ul><p><img src="Pasted%20image%2020260314162232.png" alt="" /></p><p><strong>怎么选？</strong> 具体使用哪种模式，完全取决于你要连接的<strong>从机芯片手册</strong>怎么写。绝大多数市面上的传感器和屏幕驱动芯片，默认使用的是 <strong>Mode 0</strong> (CPOL=0, CPHA=0) 或 <strong>Mode 3</strong> (CPOL=1, CPHA=1)。</p><h2 id="5-spi-多从设备的连接"><a class="markdownIt-Anchor" href="#5-spi-多从设备的连接"></a> 5. SPI 多从设备的连接</h2><p>主机连接从机的方法一般有两种——直连式和菊花链式</p><p><strong>直连：</strong></p><p>很直观。</p><p><img src="Snipaste_2026-03-14_17-01-58.png" alt="" /></p><p><strong>菊花链</strong>：</p><p>可以看出菊花链可以一定程度上节省GPIO资源。</p><p>菊花链的最大缺点是因为是信号串行传输，所以一旦数据链路中的某设备发生故障的时候，它下面优先级较低的设备就不可能得到服务了；</p><p>另一方面，距离主机越远的从机，获得服务的优先级越低，所以需要安排好从机的优先级，并且设置总线检测器，如果某个从机超时，则对该从机进行短路，防止单个从机损坏造成整个链路崩溃的情况；</p><p><img src="Snipaste_2026-03-14_16-44-59.png" alt="" /></p><p>具体的数据流向：</p><p><img src="Snipaste_2026-03-14_16-49-40.png" alt="" /></p><p>D为有效数据，X为无效数据（无操作指令NOP），可以看出，离主机越远，主机要给该从机发送消息耗时越长。</p><blockquote><p>假设你有 3 个设备组成菊花链（从机 1、从机 2、从机 3，从机 3 在最末端）。你想让<strong>从机 3</strong> 执行指令 <code>0xAA</code>，同时让从机 1 和 2 保持原样。主机不能只发一个 <code>0xAA</code>，它必须一次性发送 <strong>3 个字节（24 bits）</strong> 的数据。数据包的构造顺序是：<br />第 1 个字节：给从机 3 的目标指令 <code>0xAA</code><br />第 2 个字节：给从机 2 的 <strong>NOP（无操作）指令</strong>（通常是 <code>0x00</code> 或 <code>0xFF</code>，具体看芯片手册）<br />第 3 个字节：给从机 1 的 <strong>NOP（无操作）指令</strong></p></blockquote>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="嵌入式" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/"/>
    
    <category term="通信协议" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/"/>
    
    
  </entry>
  
  <entry>
    <title>Cmake简易使用文档</title>
    <link href="https://blog.godmao.top/posts/35476/"/>
    <id>https://blog.godmao.top/posts/35476/</id>
    <published>2026-03-14T07:10:53.000Z</published>
    <updated>2026-03-14T07:52:24.497Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><p>*<em><strong>AI生成内容</strong></em></p><hr /><p>这是一份为有一定基础的 C/C++ 开发者准备的现代 CMake 实战指南。相比于早期 CMake 满天飞的全局变量（如 <code>include_directories</code>），现代 CMake（3.0+）的核心哲学是<strong>基于目标（Target-based）</strong>。</p><p>理解并坚持“基于目标”的原则，能极大提升软件架构的清晰度，尤其是在开发包含多个模块或硬件抽象层的复杂项目时。</p><h2 id="1-现代-cmake-的核心target-与属性"><a class="markdownIt-Anchor" href="#1-现代-cmake-的核心target-与属性"></a> 1. 现代 CMake 的核心：Target 与属性</h2><p>在现代 CMake 中，一切皆为 Target（可执行文件或库）。你不需要全局设置包含路径或编译选项，而是将它们绑定到特定的 Target 上。</p><p>核心命令只有这几个家族：</p><ul><li><p><code>add_executable()</code> / <code>add_library()</code>：创建 Target。</p></li><li><p><code>target_include_directories()</code>：给 Target 添加头文件搜索路径。</p></li><li><p><code>target_link_libraries()</code>：给 Target 链接其他库。</p></li><li><p><code>target_compile_options()</code> / <code>target_compile_definitions()</code>：给 Target 添加编译选项或宏定义。</p></li></ul><p><strong>关键概念：PRIVATE, PUBLIC, INTERFACE</strong>。：</p><p>当你为一个 Target 指定包含路径、编译选项或链接库时，必须使用这三个关键字来声明作用域：</p><ul><li><p><strong><code>PRIVATE</code></strong>：该属性仅供本 Target 内部编译使用。使用者（依赖该 Target 的其他目标）不需要知道它。</p></li><li><p><strong><code>INTERFACE</code></strong>：该属性本 Target 编译时不需要，但任何使用该 Target 的使用者都需要它（通常见于接口库（ or Header-Only））。</p></li><li><p><strong><code>PUBLIC</code></strong>：等于 <code>PRIVATE</code> + <code>INTERFACE</code>。本 Target 编译时需要，使用者也需要。</p></li></ul><p>示例：假设我们正在编写一个传感器硬件抽象层（<code>sensor_hal</code>）库，并且它内部调用了底层的内核级字符串处理函数（比如内核版的 <code>string.h</code>），但不对外暴露这些底层细节。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token comment"># 创建一个静态库 Target</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>sensor_hal <span class="token namespace">STATIC</span> hal_core.c hal_utils.c<span class="token punctuation">)</span><span class="token comment"># PUBLIC: 使用 sensor_hal 的上层应用也需要找到这些头文件</span><span class="token keyword">target_include_directories</span><span class="token punctuation">(</span>sensor_hal <span class="token namespace">PUBLIC</span> <span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_CURRENT_SOURCE_DIR</span><span class="token punctuation">&#125;</span>/include<span class="token punctuation">)</span><span class="token comment"># PRIVATE: 只有 sensor_hal 内部编译时需要这些特定的编译选项（如独立环境）</span><span class="token keyword">target_compile_options</span><span class="token punctuation">(</span>sensor_hal <span class="token namespace">PRIVATE</span> -Wall -Wextra -ffreestanding<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="2-标准的单模块项目模板"><a class="markdownIt-Anchor" href="#2-标准的单模块项目模板"></a> 2. 标准的单模块项目模板</h2><p>适用于快速编写测试、脚本管理工具或小型服务端程序。</p><pre class="line-numbers language-CMake" data-language="CMake"><code class="language-CMake"># 1. 指定最低版本要求 (建议 3.15 以上，支持更多现代特性)cmake_minimum_required(VERSION 3.15)# 2. 定义项目名称、版本和语言project(SmartTerminal VERSION 1.0.0 LANGUAGES C CXX)# 3. 设置全局 C&#x2F;C++ 标准 (少数推荐的全局变量之一)set(CMAKE_C_STANDARD 11)set(CMAKE_C_STANDARD_REQUIRED ON)set(CMAKE_CXX_STANDARD 17)set(CMAKE_CXX_STANDARD_REQUIRED ON)# 4. 收集源文件 (推荐明确列出，而不是用 file(GLOB) 避免缓存问题)set(SOURCES    src&#x2F;main.c    src&#x2F;utils.c)# 5. 定义可执行文件 Targetadd_executable($&#123;PROJECT_NAME&#125; $&#123;SOURCES&#125;)# 6. 指定头文件目录# $&#123;CMAKE_CURRENT_SOURCE_DIR&#125; 指向当前 CMakeLists.txt 所在目录target_include_directories($&#123;PROJECT_NAME&#125; PRIVATE $&#123;CMAKE_CURRENT_SOURCE_DIR&#125;&#x2F;include)# 7. 添加特定宏定义 (如开启调试日志)target_compile_definitions($&#123;PROJECT_NAME&#125; PRIVATE ENABLE_DEBUG_LOG&#x3D;1)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="3-多模块架构与库的管理"><a class="markdownIt-Anchor" href="#3-多模块架构与库的管理"></a> 3. 多模块架构与库的管理</h2><p>对于工业级项目（如网关、复杂控制系统），通常会划分为核心业务、硬件抽象层（HAL）、第三方组件等。这就需要用到 <code>add_subdirectory()</code>。</p><blockquote><p><strong>拒绝 file GLOB</strong>。很多旧教程会教你使用 <code>file(GLOB SRC_FILES &quot;*.c&quot;)</code> 来批量添加源码。<strong>这是一个非常危险的反模式</strong>，因为当你新增或删除 <code>.c</code> 文件时，CMake 往往无法察觉，导致不会重新生成构建树。<strong>最佳实践是使用分治法、显式列出源码</strong>。</p></blockquote><p><strong>目录结构示例：</strong></p><pre class="line-numbers language-Plaintext" data-language="Plaintext"><code class="language-Plaintext">├── CMakeLists.txt         # 顶层 CMake├── app&#x2F;│   ├── CMakeLists.txt     # 业务层 CMake│   └── main.c└── components&#x2F;    └── hal&#x2F;        ├── CMakeLists.txt # 硬件驱动层 CMake        ├── include&#x2F;hal_gpio.h        └── src&#x2F;hal_gpio.c<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>底层模块 (components/hal/CMakeLists.txt):</strong></p><pre class="line-numbers language-CMake" data-language="CMake"><code class="language-CMake"># 编译为一个静态库add_library(hal STATIC src&#x2F;hal_gpio.c)# 任何链接了 &#39;hal&#39; 的 Target，都会自动获得 include 目录的搜索路径！# 这就是 Modern CMake 的魅力，不需要在上层重复写 include_directoriestarget_include_directories(hal PUBLIC $&#123;CMAKE_CURRENT_SOURCE_DIR&#125;&#x2F;include)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>顶层 (CMakeLists.txt):</strong></p><pre class="line-numbers language-CMake" data-language="CMake"><code class="language-CMake">cmake_minimum_required(VERSION 3.15)project(IoTGateway LANGUAGES C)# 引入子目录add_subdirectory(components&#x2F;hal)add_subdirectory(app)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>应用层 (app/CMakeLists.txt):</strong></p><pre class="line-numbers language-CMake" data-language="CMake"><code class="language-CMake">add_executable(gateway_app main.c)# 链接底层库，自动获取其 PUBLIC 暴露的头文件路径和宏定义target_link_libraries(gateway_app PRIVATE hal)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h2 id="4-引入外部依赖"><a class="markdownIt-Anchor" href="#4-引入外部依赖"></a> 4. 引入外部依赖</h2><p>日常开发中经常需要引入外部库（如多线程、或者像 LVGL 这样的 GUI 库）。</p><h3 id="方式-a系统已安装的库-find_package"><a class="markdownIt-Anchor" href="#方式-a系统已安装的库-find_package"></a> 方式 A：系统已安装的库 (find_package)</h3><p>适用于标准库或通过包管理器安装的库。</p><pre class="line-numbers language-CMake" data-language="CMake"><code class="language-CMake"># 寻找系统的线程库find_package(Threads REQUIRED)add_executable(my_app main.c)target_link_libraries(my_app PRIVATE Threads::Threads)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="方式-b源码级拉取-fetchcontent"><a class="markdownIt-Anchor" href="#方式-b源码级拉取-fetchcontent"></a> 方式 B：源码级拉取 (FetchContent)</h3><p>这是现代 CMake 管理第三方源码依赖的利器，非常适合嵌入式或没有包管理器的环境。</p><pre class="line-numbers language-CMake" data-language="CMake"><code class="language-CMake">include(FetchContent)# 自动从 Git 拉取依赖并加入构建树FetchContent_Declare(  cJSON  GIT_REPOSITORY https:&#x2F;&#x2F;github.com&#x2F;DaveGamble&#x2F;cJSON.git  GIT_TAG        v1.7.15)FetchContent_MakeAvailable(cJSON)add_executable(my_app main.c)# 直接链接拉取下来的 targettarget_link_libraries(my_app PRIVATE cjson) <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="5-交叉编译基础-cross-compiling"><a class="markdownIt-Anchor" href="#5-交叉编译基础-cross-compiling"></a> 5. 交叉编译基础 (Cross-compiling)</h2><p>如果在 WSL2 或普通 Linux 环境下为特定的 MCU 或嵌入式板子编译代码，不要在 CMakeLists.txt 里硬编码编译器路径，而是使用 <strong>Toolchain File (工具链文件)</strong>。</p><p><strong>1. 创建一个 arm-gcc-toolchain.cmake:</strong></p><pre class="line-numbers language-CMake" data-language="CMake"><code class="language-CMake">set(CMAKE_SYSTEM_NAME Generic) # Generic 通常用于裸机&#x2F;RTOS，如果是嵌入式 Linux 则写 Linuxset(CMAKE_SYSTEM_PROCESSOR arm)# 指定编译器set(CMAKE_C_COMPILER arm-none-eabi-gcc)set(CMAKE_CXX_COMPILER arm-none-eabi-g++)# 指定一些硬件相关的强制编译选项 (例如 Cortex-M4)set(CMAKE_C_FLAGS &quot;-mcpu&#x3D;cortex-m4 -mthumb -mfloat-abi&#x3D;hard -mfpu&#x3D;fpv4-sp-d16&quot; CACHE INTERNAL &quot;C Compiler options&quot;)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>2. 在命令行使用该工具链配置项目：</strong></p><pre class="line-numbers language-Bash" data-language="Bash"><code class="language-Bash"># 在 build 目录下执行cmake -DCMAKE_TOOLCHAIN_FILE&#x3D;..&#x2F;arm-gcc-toolchain.cmake -DCMAKE_BUILD_TYPE&#x3D;Release ..make -j4<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h2 id="6-日常高频命令汇总备忘"><a class="markdownIt-Anchor" href="#6-日常高频命令汇总备忘"></a> 6. 日常高频命令汇总备忘</h2><ul><li><p><strong>构建类型控制</strong>：通常通过命令行传入 <code>-DCMAKE_BUILD_TYPE=Debug</code> 或 <code>Release</code>。</p>  <pre class="line-numbers language-CMake" data-language="CMake"><code class="language-CMake"># 在 CMake 中针对 Debug 模式添加特殊选项if(CMAKE_BUILD_TYPE STREQUAL &quot;Debug&quot;)  target_compile_options($&#123;PROJECT_NAME&#125; PRIVATE -g -O0 -Wall -Wextra)else()  target_compile_options($&#123;PROJECT_NAME&#125; PRIVATE -O3)endif()<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><strong>生成 Compile Commands</strong>：对使用 <code>clangd</code> 或 VSCode 插件进行代码跳转非常关键。</p>  <pre class="line-numbers language-CMake" data-language="CMake"><code class="language-CMake"># 执行 cmake 时加上，或在文件头部设置：set(CMAKE_EXPORT_COMPILE_COMMANDS ON) # 这会在 build 目录下生成 compile_commands.json<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li></ul><h2 id="7-高频预定义变量-predefined-variables"><a class="markdownIt-Anchor" href="#7-高频预定义变量-predefined-variables"></a> 7. 高频预定义变量 (Predefined Variables)</h2><p>这部分相当于 CMake 给我们提供的系统级环境变量，直接决定了构建路径和编译参数。</p><h3 id="71-路径与目录相关模块化开发必备"><a class="markdownIt-Anchor" href="#71-路径与目录相关模块化开发必备"></a> 7.1. 路径与目录相关（模块化开发必备）</h3><p>在多目录项目中，极容易把这几个路径变量搞混：</p><ul><li><p><strong><code>CMAKE_SOURCE_DIR</code></strong>：<strong>顶层</strong> <code>CMakeLists.txt</code> 所在的绝对路径。不管你在哪个子目录里调用，它永远指向最外层。</p></li><li><p><strong><code>CMAKE_BINARY_DIR</code></strong>：顶层构建目录的绝对路径（通常就是你执行 <code>cmake ..</code> 所在的 <code>build</code> 文件夹）。</p></li><li><p><strong><code>CMAKE_CURRENT_SOURCE_DIR</code></strong>：<strong>当前</strong>正在处理的 <code>CMakeLists.txt</code> 所在的目录。</p><ul><li><em>实战用法</em>：在写子模块（如驱动层或工具库）时，包含当前目录下的头文件：<code>target_include_directories(my_target PRIVATE $&#123;CMAKE_CURRENT_SOURCE_DIR&#125;/include)</code>。</li></ul></li><li><p><strong><code>PROJECT_SOURCE_DIR</code></strong>：最近一次调用 <code>project()</code> 命令的 <code>CMakeLists.txt</code> 所在的路径。如果你的工程嵌套了其他独立的 CMake 项目，这个变量会比 <code>CMAKE_SOURCE_DIR</code> 更安全。</p></li></ul><h3 id="72-构建与编译控制"><a class="markdownIt-Anchor" href="#72-构建与编译控制"></a> 7.2. 构建与编译控制</h3><ul><li><p><strong><code>CMAKE_BUILD_TYPE</code></strong>：控制构建类型。常用值：<code>Debug</code>（带调试信息，<code>-g</code>）、<code>Release</code>（最高优化，<code>-O3</code>）、<code>MinSizeRel</code>（最小体积优化，<code>-Os</code>，<strong>这在 Flash 资源紧张的 MCU 裸机或 RTOS 开发中非常常用</strong>）。</p></li><li><p><strong><code>CMAKE_C_FLAGS</code> / <code>CMAKE_CXX_FLAGS</code></strong>：全局的 C/C++ 编译选项。</p><ul><li><em>注</em>：现代 CMake 更推荐使用 <code>target_compile_options</code> 针对具体 Target 设置，但如果你要全局强加一些架构相关的硬性 flag（比如硬件浮点运算开启），仍会在这里设置。</li></ul></li><li><p><strong><code>BUILD_SHARED_LIBS</code></strong>：如果你使用 <code>add_library(lib_name src.c)</code> 没有指定 <code>STATIC</code> 或 <code>SHARED</code>，这个变量为 <code>ON</code> 时默认编译动态库，为 <code>OFF</code> 时默认编译静态库。</p></li></ul><h3 id="73-系统与平台判定交叉编译必备"><a class="markdownIt-Anchor" href="#73-系统与平台判定交叉编译必备"></a> 7.3. 系统与平台判定（交叉编译必备）</h3><p>当你的代码需要在不同平台（比如 Linux 服务器环境和 ARM 嵌入式设备）之间切换时：</p><ul><li><p><strong><code>CMAKE_SYSTEM_NAME</code></strong>：目标系统的名称。常用的有 <code>Linux</code>、<code>Windows</code>、<code>Darwin</code>（macOS）。如果是编译裸机程序或 RTOS，通常会被工具链文件设置为 <code>Generic</code>。</p></li><li><p><strong><code>UNIX</code> / <code>WIN32</code> / <code>APPLE</code></strong>：快捷布尔变量。如果目标平台是 Unix-like 系统（包括 Linux 和 macOS），<code>UNIX</code> 的值为 1。</p></li></ul><pre class="line-numbers language-CMake" data-language="CMake"><code class="language-CMake"># 实战示例：跨平台链接特定库if(UNIX AND NOT APPLE)    target_link_libraries(my_app PRIVATE pthread)elseif(WIN32)    target_link_libraries(my_app PRIVATE wsock32)endif()<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="项目管理" scheme="https://blog.godmao.top/categories/%E9%A1%B9%E7%9B%AE%E7%AE%A1%E7%90%86/"/>
    
    
  </entry>
  
  <entry>
    <title>使用openOCD进行调试</title>
    <link href="https://blog.godmao.top/posts/37312/"/>
    <id>https://blog.godmao.top/posts/37312/</id>
    <published>2026-03-14T04:43:51.000Z</published>
    <updated>2026-04-03T07:24:36.675Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><p>OpenOCD（<strong>Open On-Chip Debugger</strong>）是一个极其强大的开源工具，主要用于硬件调试、在线编程（ISP）和边界扫描测试。</p><h2 id="1-openocd-的工作逻辑"><a class="markdownIt-Anchor" href="#1-openocd-的工作逻辑"></a> 1. OpenOCD 的工作逻辑</h2><p>在动手之前，我们需要理解 OpenOCD 在整个开发链路中扮演的角色：</p><ol><li><p><strong>主机 (Host PC)：</strong> 运行 OpenOCD 软件，以及 GDB 调试器。</p></li><li><p><strong>调试适配器 (Adapter/Interface)：</strong> 连接电脑和目标芯片的硬件（例如 ST-Link、J-Link、DAPLink 等）。</p></li><li><p><strong>目标芯片 (Target)：</strong> 你要调试或烧录的微控制器（例如 STM32、ESP32 等）。</p></li></ol><p>OpenOCD 的作用就是<strong>作为桥梁</strong>。它向左通过网络端口（Telnet/GDB）与你的电脑软件通信，向右通过 USB 驱动控制你的调试器硬件，最终操作芯片。</p><h2 id="2-安装"><a class="markdownIt-Anchor" href="#2-安装"></a> 2. 安装</h2><ul><li><p><strong>Windows:</strong> 官方仓库提供预编译版本： <a href="https://github.com/openocd-org/openocd/releases">Releases · openocd-org/openocd</a>。解压后，将 <code>bin</code> 文件夹的路径添加到系统的环境变量 <code>PATH</code> 中。</p></li><li><p><strong>Linux (Ubuntu/Debian):</strong></p>  <pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">sudo</span> <span class="token function">apt</span> update<span class="token function">sudo</span> <span class="token function">apt</span> <span class="token function">install</span> openocd<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li></ul><p><strong>测试安装：</strong> 在终端输入 <code>openocd --version</code>，如果能看到版本号说明安装成功。</p><h2 id="3-启动-openocd-服务"><a class="markdownIt-Anchor" href="#3-启动-openocd-服务"></a> 3. 启动 OpenOCD 服务</h2><p>启动 OpenOCD 的核心在于<strong>告诉它你用了什么调试器，以及你要连什么芯片</strong>。这通过指定配置文件（<code>.cfg</code>）来实现。OpenOCD 自带了大量的标准配置文件（通常存放在它安装目录的 <code>scripts</code> 文件夹下）。</p><p><strong>基础启动命令格式：</strong></p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">openocd <span class="token parameter variable">-f</span> interface/<span class="token operator">&lt;</span>调试器名称<span class="token operator">></span>.cfg <span class="token parameter variable">-f</span> target/<span class="token operator">&lt;</span>芯片名称<span class="token operator">></span>.cfg<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><strong>常见组合</strong>：</p><ul><li><p><strong>ST-Link 烧录 STM32F4 系列：</strong></p>  <pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">openocd <span class="token parameter variable">-f</span> interface/stlink.cfg <span class="token parameter variable">-f</span> target/stm32f4x.cfg<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>DAPLink 烧录 STM32F1 系列：</strong></p>  <pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">openocd <span class="token parameter variable">-f</span> interface/cmsis-dap.cfg <span class="token parameter variable">-f</span> target/stm32f1x.cfg<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>J-Link 烧录树莓派 RP2040：</strong></p>  <pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">openocd <span class="token parameter variable">-f</span> interface/jlink.cfg <span class="token parameter variable">-f</span> target/rp2040.cfg<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ul><p>如果终端输出 <code>Info : Listening on port 3333 for gdb connections</code> 和 <code>Info : Listening on port 4444 for telnet connections</code>，说明连接成功，服务已经跑起来了。<strong>此时不要关闭这个终端窗口。</strong></p><h2 id="4-烧录与控制"><a class="markdownIt-Anchor" href="#4-烧录与控制"></a> 4. 烧录与控制</h2><p>OpenOCD 启动后，它会在后台运行并开放了几个网络端口。你可以重新打开一个新的终端窗口，通过 <strong>Telnet (端口 4444)</strong> 直接发送命令给它。</p><h3 id="41-连接到-openocd-控制台"><a class="markdownIt-Anchor" href="#41-连接到-openocd-控制台"></a> <strong>4.1. 连接到 OpenOCD 控制台</strong></h3><p>在新终端输入：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">telnet localhost <span class="token number">4444</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="42-常用交互指令"><a class="markdownIt-Anchor" href="#42-常用交互指令"></a> 4.2 常用交互指令</h3><p>连接成功后，你会看到 <code>&gt;</code> 提示符。以下是标准的操作流程：</p><ol><li><p>暂停芯片运行：</p> <pre class="line-numbers language-none"><code class="language-none">&gt; halt<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>解锁/擦除并写入固件（支持 .hex / .bin / .elf）：<br />这里假设你要烧录的文件名为 <code>firmware.hex</code>，<strong>请使用绝对路径或确保在当前工作目录下。</strong></p> <pre class="line-numbers language-none"><code class="language-none">&gt; flash write_image erase firmware.hex<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>复位并运行芯片：</p> <pre class="line-numbers language-none"><code class="language-none">&gt; reset run<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>退出 Telnet：</p> <pre class="line-numbers language-none"><code class="language-none">&gt; exit<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ol><p>如果你不想每次都敲这么多命令，可以直接在启动 OpenOCD 时附带执行命令：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">openocd <span class="token parameter variable">-f</span> interface/stlink.cfg <span class="token parameter variable">-f</span> target/stm32f4x.cfg <span class="token parameter variable">-c</span> <span class="token string">"program firmware.hex verify reset exit"</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这条命令会自动完成：连接 -&gt; 烧录 -&gt; 校验 -&gt; 复位运行 -&gt; 退出。非常适合自动化脚本！</p><h2 id="5-结合gdb进行代码调试"><a class="markdownIt-Anchor" href="#5-结合gdb进行代码调试"></a> 5. 结合GDB进行代码调试</h2><p>OpenOCD 最强大的地方在于配合 GDB 进行单步调试。</p><ol><li><p><strong>保持 OpenOCD 服务运行（参考第三部分）。</strong></p></li><li><p><strong>打开新终端，启动 GDB：</strong>（需要使用对应架构的 gdb，比如 ARM）</p> <pre class="line-numbers language-none"><code class="language-none">arm-none-eabi-gdb your_program.elf<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>在 GDB 中连接到 OpenOCD：</strong></p> <pre class="line-numbers language-none"><code class="language-none">(gdb) target remote localhost:3333<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>复位并暂停芯片（OpenOCD 特定指令在 GDB 中需要加 <code>monitor</code> 前缀）：</strong></p> <pre class="line-numbers language-none"><code class="language-none">(gdb) monitor reset halt<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>加载程序（烧录）：</strong></p> <pre class="line-numbers language-none"><code class="language-none">(gdb) load<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>开始常规 GDB 调试：</strong></p><ul><li><p><code>b main</code> (在 main 函数打断点)</p></li><li><p><code>c</code> (Continue，运行到断点)</p></li><li><p><code>n</code> (Next，单步步过)</p></li><li><p><code>s</code> (Step，单步步入)</p></li></ul></li></ol><h2 id="6-常见问题排查"><a class="markdownIt-Anchor" href="#6-常见问题排查"></a> 6. 常见问题排查</h2><table><thead><tr><th><strong>错误现象</strong></th><th><strong>可能的原因与解决方案</strong></th></tr></thead><tbody><tr><td><code>Error: open failed</code></td><td>找不到调试器硬件。请检查 USB 是否插好，虚拟机是否勾选了 USB 直通，或者 Windows 下是否缺少 WinUSB 驱动（可以使用 Zadig 工具安装驱动）。</td></tr><tr><td><code>Error: init mode failed</code></td><td>调试器与芯片通信失败。检查 SWD/JTAG 连线（GND, SWDIO, SWCLK）是否接错、松动，或者芯片是否供电正常。</td></tr><tr><td><code>Error: timed out while waiting for target halted</code></td><td>芯片可能进入了深度休眠或禁用了调试端口。尝试按住开发板上的 Reset 按钮，运行命令，然后瞬间松开 Reset。</td></tr></tbody></table><h2 id="7-wsl下的openocd最佳实践"><a class="markdownIt-Anchor" href="#7-wsl下的openocd最佳实践"></a> 7. WSL下的OpenOCD最佳实践</h2><p><strong>核心思路：</strong> 把 OpenOCD 运行在 Windows 上直接接管 USB 硬件，把交叉编译工具链和 GDB 运行在 WSL 中。两者通过 TCP 网络端口进行通信。</p><p>由于 OpenOCD 本质上就是一个 GDB Server，这种 C/S（客户端/服务端）架构完美契合 WSL 的网络特性。</p><p><strong>操作步骤：</strong></p><ol><li><strong>在 Windows 端启动 OpenOCD：</strong></li></ol><ul><li>下载 Windows 版的 OpenOCD。</li><li>插上你的调试器（确保 Windows 下已安装对应驱动，如 WinUSB）。</li><li>在 Windows 的 PowerShell 或 CMD 中启动 OpenOCD，并<strong>强制绑定 IP 为全网段</strong>，以便 WSL 可以访问：  <pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">openocd <span class="token parameter variable">-f</span> interface/stlink.cfg <span class="token parameter variable">-f</span> target/stm32f4x.cfg <span class="token parameter variable">-c</span> <span class="token string">"bindto 0.0.0.0"</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ul><ol start="2"><li><strong>在 WSL 端获取 Windows 主机 IP：</strong><br />WSL2 和 Windows 不在同一个网段。你需要在 WSL 终端中获取 Windows 宿主机的 IP 地址（通常是虚拟网关）： <pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token comment"># 获取 Windows 宿主机的 IP</span><span class="token function">cat</span> /etc/resolv.conf <span class="token operator">|</span> <span class="token function">grep</span> nameserver <span class="token operator">|</span> <span class="token function">awk</span> <span class="token string">'&#123;print $2&#125;'</span> <span class="token comment"># 假设输出为 172.28.16.1</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li></ol><blockquote><p>如果你使用的是 Windows 11 且开启了 WSL 的 <code>networkingMode=mirrored</code>，你可以直接使用 <code>localhost</code>)</p></blockquote><ol start="3"><li><p><strong>在 WSL 端启动 GDB 并连接:</strong><br />编译出 <code>.elf</code> 固件后，在 WSL 中启动 GDB：</p> <pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">arm-none-eabi-gdb your_firmware.elf<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>在 GDB 命令行中，连接到 Windows 上的 OpenOCD：</p> <pre class="line-numbers language-none"><code class="language-none">(gdb) target remote 172.28.16.1:3333(gdb) load   (gdb) monitor reset halt(gdb) continue<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li></ol><p>通过 GDB 的 <code>load</code> 命令，固件数据是通过 TCP 传给 OpenOCD 的，完美规避了跨系统的文件路径问题。</p><blockquote><p>还有一种方案是将USB透传给WSL，这样WSL就拥有这个usb设备，可正常使用Linux下的openOCD，透传USB参见：<a href="https://blog.godmao.top/posts/13715/">WSL下连接USB设备-GoDm@'s blog</a></p></blockquote><h2 id="8-基于-vscode-图形化调试"><a class="markdownIt-Anchor" href="#8-基于-vscode-图形化调试"></a> 8. 基于 vscode 图形化调试</h2><p>在 VS Code 中进行图形化调试是目前嵌入式开发最高效的方式。借助强大的 <strong>Cortex-Debug</strong> 插件，你可以完美地将代码编辑、编译（在 WSL 中）和底层硬件调试结合在一起，甚至可以直接在可视化界面中查看外设寄存器和 RTOS 的任务栈。</p><p>在 VS Code 中（如果是WSL，则在WSL环境下）安装了以下插件：</p><ol><li><strong>C/C++</strong> (Microsoft) - 提供代码补全和基础 GDB 支持。</li><li><strong>Cortex-Debug</strong> (marus25) - <strong>最核心的插件</strong>，专门针对 ARM Cortex-M 内核优化，支持 OpenOCD 桥接、寄存器查看和 RTOS 线程解析。</li></ol><h3 id="81-核心配置编写-launchjson"><a class="markdownIt-Anchor" href="#81-核心配置编写-launchjson"></a> 8.1. 核心配置：编写 launch.json</h3><p><code>launch.json</code> 核心作用是告诉 VS Code 如何启动和配置调试会话。</p><p>当你编译出 <code>.elf</code> 文件后，你需要把它烧录进芯片，并且监控程序的运行。这就是 <code>launch.json</code> 要干的。它定义了 VS Code 应该怎么去调用底层的调试工具。</p><p>在这个文件里，你需要向 VS Code 交代清楚：</p><ul><li><em>用什么调试插件</em>？ （比如我们用的 <code>cortex-debug</code>）</li><li><em>程序文件在哪里</em>？ （指路刚才编译出来的 <code>.elf</code> 文件）</li><li><em>用什么硬件接口</em>？ （ST-Link 还是 DAPLink？）</li><li><em>目标芯片是什么</em>？ （比如 STM32F4 系列）</li><li><em>调试服务器在哪</em>？ （是我们之前说的 WSL 内部启动，还是连接到 Windows 宿主机？）</li></ul><p>有了 <code>launch.json</code>，VS Code 的图形化调试界面（左侧的变量窗口、调用堆栈、断点控制）才知道要去哪里获取数据。当你打下一个断点时，VS Code 会通过 <code>launch.json</code> 里的配置，把这个“暂停”指令一路传达到物理芯片的硬件断点寄存器上。</p><p>在你的工程根目录下，创建或打开 <code>.vscode/launch.json</code> 文件。针对我们在上一回提到的两种 WSL 开发架构，这里提供两套配置。你可以直接将以下代码复制进去，根据你的实际架构选择对应的调试配置启动。</p><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span>    <span class="token property">"version"</span><span class="token operator">:</span> <span class="token string">"0.2.0"</span><span class="token punctuation">,</span>    <span class="token property">"configurations"</span><span class="token operator">:</span> <span class="token punctuation">[</span>        <span class="token punctuation">&#123;</span>            <span class="token comment">// 【配置一：混合架构】OpenOCD 运行在 Windows 端，GDB 在 WSL 端</span>            <span class="token property">"name"</span><span class="token operator">:</span> <span class="token string">"Debug (Windows OpenOCD)"</span><span class="token punctuation">,</span>            <span class="token property">"type"</span><span class="token operator">:</span> <span class="token string">"cortex-debug"</span><span class="token punctuation">,</span>            <span class="token property">"request"</span><span class="token operator">:</span> <span class="token string">"launch"</span><span class="token punctuation">,</span>            <span class="token property">"servertype"</span><span class="token operator">:</span> <span class="token string">"external"</span><span class="token punctuation">,</span> <span class="token comment">// 关键：告诉插件 OpenOCD 已经在外部启动了</span>            <span class="token property">"gdbTarget"</span><span class="token operator">:</span> <span class="token string">"172.28.16.1:3333"</span><span class="token punctuation">,</span> <span class="token comment">// 替换为你的 Windows 宿主机 IP</span>            <span class="token property">"executable"</span><span class="token operator">:</span> <span class="token string">"$&#123;workspaceFolder&#125;/build/your_program.elf"</span><span class="token punctuation">,</span> <span class="token comment">// 替换为你的实际 elf 路径</span>            <span class="token property">"cwd"</span><span class="token operator">:</span> <span class="token string">"$&#123;workspaceRoot&#125;"</span><span class="token punctuation">,</span>            <span class="token property">"runToEntryPoint"</span><span class="token operator">:</span> <span class="token string">"main"</span><span class="token punctuation">,</span> <span class="token comment">// 启动后自动在 main 函数停下</span>                        <span class="token comment">// 进阶功能（可选但强烈推荐）</span>            <span class="token property">"svdFile"</span><span class="token operator">:</span> <span class="token string">"$&#123;workspaceRoot&#125;/STM32F4xx.svd"</span><span class="token punctuation">,</span> <span class="token comment">// 替换为你的芯片 SVD 文件路径</span>            <span class="token property">"rtos"</span><span class="token operator">:</span> <span class="token string">"FreeRTOS"</span> <span class="token comment">// 如果你跑了 RTOS，开启此项可以查看所有线程状态</span>        <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>        <span class="token punctuation">&#123;</span>            <span class="token comment">// 【配置二：USB 穿透】OpenOCD 和 GDB 都运行在 WSL 内部</span>            <span class="token property">"name"</span><span class="token operator">:</span> <span class="token string">"Debug (WSL Local OpenOCD)"</span><span class="token punctuation">,</span>            <span class="token property">"type"</span><span class="token operator">:</span> <span class="token string">"cortex-debug"</span><span class="token punctuation">,</span>            <span class="token property">"request"</span><span class="token operator">:</span> <span class="token string">"launch"</span><span class="token punctuation">,</span>            <span class="token property">"servertype"</span><span class="token operator">:</span> <span class="token string">"openocd"</span><span class="token punctuation">,</span> <span class="token comment">// 关键：由插件全权负责在 WSL 启动和关闭 OpenOCD</span>            <span class="token property">"executable"</span><span class="token operator">:</span> <span class="token string">"$&#123;workspaceFolder&#125;/build/your_program.elf"</span><span class="token punctuation">,</span>            <span class="token property">"cwd"</span><span class="token operator">:</span> <span class="token string">"$&#123;workspaceRoot&#125;"</span><span class="token punctuation">,</span>            <span class="token property">"configFiles"</span><span class="token operator">:</span> <span class="token punctuation">[</span>                <span class="token string">"interface/stlink.cfg"</span><span class="token punctuation">,</span> <span class="token comment">// 你的调试器配置文件</span>                <span class="token string">"target/stm32f4x.cfg"</span>   <span class="token comment">// 你的目标芯片配置文件</span>            <span class="token punctuation">]</span><span class="token punctuation">,</span>            <span class="token property">"runToEntryPoint"</span><span class="token operator">:</span> <span class="token string">"main"</span><span class="token punctuation">,</span>                        <span class="token comment">// 进阶功能</span>            <span class="token property">"svdFile"</span><span class="token operator">:</span> <span class="token string">"$&#123;workspaceRoot&#125;/STM32F4xx.svd"</span><span class="token punctuation">,</span>            <span class="token property">"rtos"</span><span class="token operator">:</span> <span class="token string">"FreeRTOS"</span>        <span class="token punctuation">&#125;</span>    <span class="token punctuation">]</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="82-cortex-debug-进阶"><a class="markdownIt-Anchor" href="#82-cortex-debug-进阶"></a> 8.2. Cortex-Debug 进阶</h3><p>配置好 <code>launch.json</code> 并按下 <code>F5</code> 启动调试后，除了常规的单步运行（F10）、进入函数（F11）、打断点，你还可以利用 Cortex-Debug 提供的强大面板：</p><ol><li><p><strong>外设寄存器可视化 (XPERIPHERALS 面板)</strong></p><p><strong>作用：</strong> 你可以直接在图形界面点开 <code>GPIOA</code>、<code>TIM1</code> 等外设，实时查看甚至<strong>动态修改</strong>底层寄存器的值（比如直接勾选某个 bit 来翻转引脚电平），这在排查底层驱动或协议栈问题时很好用。</p><blockquote><p>SVD 文件通常可以从芯片厂商官网（如 STMicroelectronics）或 Keil 的 Pack 包中提取。</p></blockquote></li><li><p><strong>RTOS 线程感知 (CALL STACK 面板)</strong></p><p>如果你在项目中使用了 RTOS（如 FreeRTOS、RT-Thread），在 <code>launch.json</code> 中添加 <code>&quot;rtos&quot;: &quot;FreeRTOS&quot;</code> 字段。</p><p><strong>作用：</strong> Cortex-Debug 会自动解析内存，在左侧的“调用堆栈 (Call Stack)”中列出当前系统中所有的 RTOS 任务（Thread）。你可以随时点击任意一个被挂起或阻塞的任务，查看它在切换上下文之前的局部变量和运行轨迹。这对于排查死锁或时序问题极为有效。</p></li><li><p><strong>内存查看器 (Memory View)</strong></p><p>按下 <code>Ctrl+Shift+P</code>，输入 <code>Cortex-Debug: View Memory</code>。</p><p><strong>作用：</strong> 输入一个内存地址（例如缓冲区的首地址 <code>0x20001000</code>）和长度，你可以像使用 Hex Editor 一样直接观察裸机内存中的数据变化。</p></li></ol><h4 id="补充从keil-pack包获取svd"><a class="markdownIt-Anchor" href="#补充从keil-pack包获取svd"></a> 补充：从keil pack包获取svd</h4><ol><li><strong>访问 Keil Pack 官网：</strong> 打开 <a href="https://www.keil.com/dd2/pack/">Keil CMSIS-Pack 搜索页</a>。</li><li><strong>下载 Pack 包：</strong> 搜索你的芯片系列（例如 <code>STM32G4</code>），找到 <code>Keil.STM32G4xx_DFP</code> 并下载。你会得到一个后缀为 <code>.pack</code> 的文件。</li><li><strong>解压提取：</strong> <strong><code>.pack</code> 文件本质上就是一个 <code>.zip</code> 压缩包。</strong> 将它的后缀改成 <code>.zip</code> 然后解压。</li><li><strong>找到 SVD：</strong> 在解压后的文件夹中，依次进入 <code>CMSIS</code> -&gt; <code>SVD</code> 目录，里面有 <code>.svd</code> 文件。</li></ol><p>建议直接把下载好的 <code>.svd</code> 文件扔到你的 VS Code 工程根目录下（和 <code>.vscode</code> 文件夹平级）然后修改 <code>launch.json</code>。</p><h3 id="83-自动化编译任务"><a class="markdownIt-Anchor" href="#83-自动化编译任务"></a> 8.3 自动化编译任务</h3><p>当你写完代码后，通常需要在终端里输入类似 <code>make -j8</code> 或者 <code>cmake --build build</code> 的命令。每次修改代码都要敲一次命令太麻烦了。我们可以把这些命令封装为一个Task。实现 <strong>“按 F5 自动编译 -&gt; 自动烧录 -&gt; 自动进入调试”</strong> 的一键全自动流水线。</p><p>在工程的 <code>.vscode</code> 目录下创建一个名为 <code>tasks.json</code> 的文件。根据你的构建工具，选择以下其中一段配置填入：</p><ul><li><p><strong>makefile</strong>：<br />如果你的工程根目录下有一个 <code>Makefile</code>，请使用这个配置：</p>  <pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span>    <span class="token property">"version"</span><span class="token operator">:</span> <span class="token string">"2.0.0"</span><span class="token punctuation">,</span>    <span class="token property">"tasks"</span><span class="token operator">:</span> <span class="token punctuation">[</span>        <span class="token punctuation">&#123;</span>            <span class="token property">"label"</span><span class="token operator">:</span> <span class="token string">"Build Project"</span><span class="token punctuation">,</span> <span class="token comment">// 任务名称，可以自定义</span>            <span class="token property">"type"</span><span class="token operator">:</span> <span class="token string">"shell"</span><span class="token punctuation">,</span>            <span class="token property">"command"</span><span class="token operator">:</span> <span class="token string">"make"</span><span class="token punctuation">,</span>            <span class="token property">"args"</span><span class="token operator">:</span> <span class="token punctuation">[</span>                <span class="token string">"-j$(nproc)"</span>             <span class="token punctuation">]</span><span class="token punctuation">,</span>            <span class="token property">"options"</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span>                <span class="token property">"cwd"</span><span class="token operator">:</span> <span class="token string">"$&#123;workspaceFolder&#125;"</span>            <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>            <span class="token property">"group"</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span>                <span class="token property">"kind"</span><span class="token operator">:</span> <span class="token string">"build"</span><span class="token punctuation">,</span>                <span class="token property">"isDefault"</span><span class="token operator">:</span> <span class="token boolean">true</span>            <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>            <span class="token property">"problemMatcher"</span><span class="token operator">:</span> <span class="token punctuation">[</span>                <span class="token string">"$gcc"</span> <span class="token comment">// 自动解析 GCC 编译器的报错并在 VSCode 源码中高亮提示</span>            <span class="token punctuation">]</span>        <span class="token punctuation">&#125;</span>    <span class="token punctuation">]</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><strong>Cmake：</strong><br />如果你的工程是基于 CMake 的（即根目录下有 <code>CMakeLists.txt</code>），请使用这个配置。<em>（假设你已经执行过 <code>cmake -B build</code> 生成了构建目录）</em>：</p>  <pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span>    <span class="token property">"version"</span><span class="token operator">:</span> <span class="token string">"2.0.0"</span><span class="token punctuation">,</span>    <span class="token property">"tasks"</span><span class="token operator">:</span> <span class="token punctuation">[</span>        <span class="token punctuation">&#123;</span>            <span class="token property">"label"</span><span class="token operator">:</span> <span class="token string">"Build Project"</span><span class="token punctuation">,</span>            <span class="token property">"type"</span><span class="token operator">:</span> <span class="token string">"shell"</span><span class="token punctuation">,</span>            <span class="token property">"command"</span><span class="token operator">:</span> <span class="token string">"cmake"</span><span class="token punctuation">,</span>            <span class="token property">"args"</span><span class="token operator">:</span> <span class="token punctuation">[</span>                <span class="token string">"--build"</span><span class="token punctuation">,</span>                <span class="token string">"build"</span><span class="token punctuation">,</span> <span class="token comment">// 假设你的编译输出目录叫 build</span>                <span class="token string">"-j$(nproc)"</span>            <span class="token punctuation">]</span><span class="token punctuation">,</span>            <span class="token property">"options"</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span>                <span class="token property">"cwd"</span><span class="token operator">:</span> <span class="token string">"$&#123;workspaceFolder&#125;"</span>            <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>            <span class="token property">"group"</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span>                <span class="token property">"kind"</span><span class="token operator">:</span> <span class="token string">"build"</span><span class="token punctuation">,</span>                <span class="token property">"isDefault"</span><span class="token operator">:</span> <span class="token boolean">true</span>            <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>            <span class="token property">"problemMatcher"</span><span class="token operator">:</span> <span class="token punctuation">[</span>                <span class="token string">"$gcc"</span>            <span class="token punctuation">]</span>        <span class="token punctuation">&#125;</span>    <span class="token punctuation">]</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><blockquote><p>参数中加了 <strong><code>-j$(nproc)</code></strong>。在 Linux/WSL 环境下，这代表调用 CPU 的所有核心进行并发编译。如果你在工程中引入了像 LVGL 这样的图形库，或者包含了复杂的微内核和协议栈，几百上千个 C 文件的编译时间会非常长。加上这个参数，编译速度能提升好几倍，极大地改善调试体验。</p></blockquote><blockquote><p><strong>task.json 工作逻辑</strong>： 创建一个<code>Build Project</code> 的任务，告诉 VS Code：“当你执行这个任务时，就在终端里帮我自动运行 <code>make</code> 命令”。</p></blockquote><blockquote><p><strong>不仅仅是编译：</strong> 你甚至可以写一个清空编译产物的任务（执行 <code>make clean</code>），或者写一个自动打包固件的任务。只要是能在终端里敲的命令，都可以写进 <code>tasks.json</code> 里让它一键代劳。</p></blockquote><p>现在任务写好了，我们需要告诉 VSCode 在启动 OpenOCD 调试之前，请先运行这个编译任务</p><p>打开你之前的 <code>.vscode/launch.json</code>，在你的调试配置项(“configurations”)中，添加一行 <code>preLaunchTask</code>，它的值必须和你刚才在 <code>tasks.json</code> 中写的 <code>label</code> 完全一致。</p><p>完成。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="记录" scheme="https://blog.godmao.top/categories/%E8%AE%B0%E5%BD%95/"/>
    
    
  </entry>
  
  <entry>
    <title>搭建WSL下STM32开发环境</title>
    <link href="https://blog.godmao.top/posts/54700/"/>
    <id>https://blog.godmao.top/posts/54700/</id>
    <published>2026-03-13T16:36:58.000Z</published>
    <updated>2026-04-03T07:21:43.097Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><p>在Linux上搭建一个通用且灵活的嵌入式开发环境，最经典的组合是 <strong>VS Code + GCC交叉编译器 + CMake/Make + OpenOCD</strong>。</p><p>但本文是在是在WSL环境下，并不是原生Linux环境，因此搭建略有不同。也可借此文章引申到搭建其他MCU运行环境，道理都是一样的。</p><blockquote><p>其实在win下开发MCU还是很方便的，直接用 keil+STM32CubeMX 就可以，我之所以想在WSL下开发主要是为后续使用Linux做主力开发系统做准备。</p></blockquote><blockquote><p>这里只是简单记录一下流程，以防遗忘，并不是教程</p></blockquote><h2 id="1-核心工具链"><a class="markdownIt-Anchor" href="#1-核心工具链"></a> 1. 核心工具链</h2><p>主要是交叉编译器，我们使用ARM Cortex-M 系列的通用交叉编译器：<br /><a href="https://developer.arm.com/downloads/-/arm-gnu-toolchain-downloads">Arm GNU Toolchain Downloads – Arm Developer</a></p><p>选择<code>x86_64 Linux hosted cross toolchains</code> 版本-<br /><code>AArch32 bare-metal target (arm-none-eabi)</code>工具链</p><p>下载到WSL然后解压缩，比如我下载的是 <code>.tar.xz</code><br />就使用命令 <code>tar -Jxvf</code> 解压缩。<br />解压缩后的文件里有个bin文件夹——我是把这个文件夹软链接然后把软链接加入环境变量。</p><p>然后还有cmake，没安装要安装一下。</p><p>差不多就这些。</p><h2 id="2-安装烧录与调试工具"><a class="markdownIt-Anchor" href="#2-安装烧录与调试工具"></a> 2. 安装烧录与调试工具</h2><p>OpenOCD (Open On-Chip Debugger) 是开源嵌入式调试的“瑞士军刀”。它支持市面上几乎所有的仿真器（ST-Link, J-Link, DAPLink）和各种芯片架构。</p><p>由于WSL对usb的支持还不是很完善，无法直接物理访问 Windows 宿主机上的 USB 接口，还要用工具，就挺麻烦的，因此我就直接采用如下方案：</p><ul><li><p><strong>Windows 充当 Server：</strong> 在 Windows 上下载并解压 Windows 版的 OpenOCD。将 ST-Link 插在电脑上，直接在 Windows 的命令提示符（CMD/PowerShell）里运行 OpenOCD。它会在本地建立一个 GDB Server（默认监听 3333 端口）。</p></li><li><p><strong>WSL2 充当 Client：</strong> WSL 环境中，专注于写代码和编译。使用 <code>arm-none-eabi-gcc</code> 编译出带调试信息的 <code>.elf</code> 文件。</p></li><li><p><strong>TCP 桥接：</strong> 在 WSL2 中运行 <code>arm-none-eabi-gdb</code>，通过 TCP/IP 协议连接到 Windows 主机的 IP 地址和 3333 端口。</p></li></ul><h2 id="3-配置-vs-code-及核心插件"><a class="markdownIt-Anchor" href="#3-配置-vs-code-及核心插件"></a> 3. 配置 VS Code 及核心插件</h2><p>VS Code 安装以下插件：</p><ol><li><p><strong>C/C++ (Microsoft)</strong>: 提供代码补全、跳转和基础解析。</p></li><li><p><strong>CMake Tools (Microsoft)</strong>: 方便在底部状态栏直接配置和编译 CMake 项目。</p></li><li><p><strong>Cortex-Debug (marus25)</strong>: <strong>极其关键的插件</strong>。专门为 ARM Cortex-M 系列优化，支持直接在 VS Code 里查看外设寄存器 (Peripherals)、内存、反汇编代码以及 RTOS 的任务栈信息。</p></li></ol><p><strong>另</strong>：要想使用VSCODE自带的CMake生成目标，还需要配置 <code>CMakePresets.json</code>——。VSCODE CMake插件的配置文件。</p><p><img src="Snipaste_2026-03-17_20-14-36.png" alt="" /></p><p>然后打开 <code>CMakePresets.json</code>，在configurePresets项里修改</p><pre class="line-numbers language-none"><code class="language-none">&#123;&quot;name&quot;: &quot;使用工具链文件配置预设&quot;,&quot;displayName&quot;: &quot;使用工具链文件配置预设&quot;,&quot;description&quot;: &quot;设置 Ninja 生成器、版本和安装目录&quot;,&quot;generator&quot;: &quot;Ninja&quot;,&quot;binaryDir&quot;: &quot;$&#123;sourceDir&#125;&#x2F;out&#x2F;build&#x2F;$&#123;presetName&#125;&quot;,&quot;cacheVariables&quot;: &#123;&quot;CMAKE_BUILD_TYPE&quot;: &quot;Debug&quot;,&quot;CMAKE_TOOLCHAIN_FILE&quot;: &quot;&quot;,&quot;CMAKE_INSTALL_PREFIX&quot;: &quot;$&#123;sourceDir&#125;&#x2F;out&#x2F;install&#x2F;$&#123;presetName&#125;&quot;    &#125;&#125;,<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>修改为这个：</p><pre class="line-numbers language-none"><code class="language-none">&#123;&quot;name&quot;: &quot;stm32-debug&quot;,&quot;displayName&quot;: &quot;stm32 交叉编译 (Debug)&quot;,&quot;description&quot;: &quot;使用 make 和 ARM 工具链编译&quot;,&quot;generator&quot;: &quot;Unix Makefiles&quot;,&quot;binaryDir&quot;: &quot;$&#123;sourceDir&#125;&#x2F;build&quot;,&quot;cacheVariables&quot;: &#123;&quot;CMAKE_BUILD_TYPE&quot;: &quot;Debug&quot;,&quot;CMAKE_TOOLCHAIN_FILE&quot;: &quot;$&#123;sourceDir&#125;&#x2F;cmake&#x2F;gcc-arm-none-eabi.cmake&quot;,&quot;CMAKE_RUNTIME_OUTPUT_DIRECTORY&quot;: &quot;$&#123;sourceDir&#125;&#x2F;bin&quot;&#125;&#125;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>主要就是设置cmake的输出目录，工具链，还有生成什么文件（generator，你填 <code>Unix Makefiles</code> 就生成makefile，填 <code>Ninja</code> 就是ninja文件）</p><p><strong>另</strong>：一般来说，使用cubemx生成的项目文件，VSCODE的c语言支持插件（IntelliSense）是不可能正常工作的，会报很多红，找不到路径等等。所以这里我们还需要配置 <code>c_cpp_properties.json</code>,重新配置一下这个IntelliSense。在项目文件夹下创建 <code>.vscode</code>文件夹并在该文件夹下创建<code>c_cpp_properties.json</code></p><p>在 <code>configurations</code> 下创建一个新配置：</p><pre class="line-numbers language-none"><code class="language-none">&#123;&quot;name&quot;: &quot;STM32_ARM&quot;,&quot;compilerPath&quot;: &quot;&#x2F;home&#x2F;gdm&#x2F;prjts&#x2F;ARM_TOOLCHAIN&#x2F;AArch32-BareMetal&#x2F;arm-none-eabi-PATH&#x2F;arm-none-eabi-gcc&quot;,&quot;compileCommands&quot;: &quot;$&#123;workspaceFolder&#125;&#x2F;build&#x2F;compile_commands.json&quot;,&quot;cStandard&quot;: &quot;gnu11&quot;,&quot;cppStandard&quot;: &quot;gnu++14&quot;,&quot;intelliSenseMode&quot;: &quot;linux-gcc-arm&quot;&#125;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><code>compilerPath</code> 指定你的系统编译器路径，这样一些标准头文件就不会被乱找。</p><p><code>compileCommands</code> IntelliSense就是通过它来解析各种路径，提供语法补全等支持，它由 Cmake自动生成。</p><p>CubeMX自动生成的<code>CMakeList.txt</code> 里的这个选项：</p><pre class="line-numbers language-none"><code class="language-none"># Enable compile command to ease indexing with e.g. clangdset(CMAKE_EXPORT_COMPILE_COMMANDS TRUE)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>就是开启生成它的宏。所以如果CMake没有自动生成，请确保开启了这个宏。</p><p><code>intelliSenseMode</code> 则确保 intelliSense 以32位arm架构的规则来为你提供语法分析。</p><p><strong>另</strong>：如果你又新创建了文件夹该怎么做呢？如果你新创建了文件夹，在这个文件夹里intelliSense又不会解析了。<br />在 cubeMX 生成的<code>CMakeList.txt</code> 里有下面两项：</p><p><img src="Snipaste_2026-03-17_20-33-08.png" alt="" /></p><p>注释已经写的很清楚了，添加好后，在重新使用cmake生成一下，cmake会自动更新 compile_commands.json ，然后intelliSense就可以正常工作了。</p><p>还是推荐逐个写明添加的文件，而不是使用file和glob，在 <a href="https://blog.godmao.top/posts/35476/">CMake简易使用文档</a>中有讲。</p><h2 id="4-项目的初始化"><a class="markdownIt-Anchor" href="#4-项目的初始化"></a> 4. 项目的初始化</h2><p>在初始化 STM32 的时钟树、引脚复用和生成外设 HAL 库时，<strong>STM32CubeMX</strong> 依然是最高效的工具。虽然WSL有WSLg，理论上支持Linux版的CubeMX，但是安装Linux版会有很多依赖也需要安装，所以我还是选择在Win下使用CubeMX。</p><ol><li><p>去 ST 官网下载 Win 版的 STM32CubeMX 并安装。</p></li><li><p>在 CubeMX 中配置好芯片、时钟、引脚以及你要用的 RTOS（如果需要）。</p></li><li><p><strong>关键步骤</strong>：在 Project Manager -&gt; Project 选项卡中，将 <strong>Toolchain/IDE</strong> 设置为 <strong>CMake</strong>（较新版本的 CubeMX 支持）或 <strong>Makefile</strong>。</p></li><li><p>点击 Generate Code。</p></li></ol><h2 id="5-整合工作流"><a class="markdownIt-Anchor" href="#5-整合工作流"></a> 5. 整合工作流</h2><p>另请参见——<a href="https://blog.godmao.top/posts/37312/">使用openOCD进行调试-GoDm@'s blog</a></p><h2 id="6-总结"><a class="markdownIt-Anchor" href="#6-总结"></a> 6. 总结</h2><p>就是瞎折腾，WSL负责项目的编译和构建，相当于把keil替换为了WSL环境下的VSCODE，且。其他依然在Win环境下。唉 <s>感觉</s> 写了一篇废话</p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="记录" scheme="https://blog.godmao.top/categories/%E8%AE%B0%E5%BD%95/"/>
    
    
  </entry>
  
  <entry>
    <title>大蟒蛇subprocess模块极简入门</title>
    <link href="https://blog.godmao.top/posts/48202/"/>
    <id>https://blog.godmao.top/posts/48202/</id>
    <published>2026-03-12T10:41:26.000Z</published>
    <updated>2026-03-12T10:50:39.797Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><p>写 Python 脚本时，经常会需要在 Python 代码里悄悄跑一个终端命令（比如跑一段 bash 脚本）。<br />那就不得不提到 Python 官方推荐的现代解决方案：<code>subprocess</code> 模块。由于我只是偶尔写点小脚本，只要掌握其中的一个函数就足够了，在此记录以防忘记。</p><h3 id="唯一真神subprocessrun"><a class="markdownIt-Anchor" href="#唯一真神subprocessrun"></a> 唯一真神：subprocess.run()</h3><p>自 Python 3.5 起，官方强烈建议使用 <code>subprocess.run()</code> 来替代其他老旧的函数。它的逻辑非常直白：运行指定的命令，等待它完成，然后返回一个包含执行结果的对象。</p><p>我们需要先导入它：</p><pre class="line-numbers language-Python" data-language="Python"><code class="language-Python">import subprocess<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>下面我们通过三个最常见的场景，来看看它到底怎么用。</p><h3 id="场景一只管执行不需要获取输出-fire-and-forget"><a class="markdownIt-Anchor" href="#场景一只管执行不需要获取输出-fire-and-forget"></a> 场景一：只管执行，不需要获取输出 (Fire and Forget)</h3><p>假设你只想在脚本里新建一个文件夹，不需要关心终端打印了什么。</p><pre class="line-numbers language-Python" data-language="Python"><code class="language-Python">import subprocess# 执行命令: mkdir new_foldersubprocess.run([&quot;mkdir&quot;, &quot;new_folder&quot;])print(&quot;文件夹创建完毕！&quot;)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>我们把命令写成了一个<strong>列表</strong> <code>[&quot;mkdir&quot;, &quot;new_folder&quot;]</code>，而不是一个长字符串 <code>&quot;mkdir new_folder&quot;</code>。 这是 <code>subprocess</code> 的推荐写法，它可以有效避免复杂的空格转义和安全漏洞（比如 Shell 注入）。<strong>第一个元素是命令，后面的元素是参数。</strong></p></blockquote><h3 id="场景二我需要拿到命令的输出内容"><a class="markdownIt-Anchor" href="#场景二我需要拿到命令的输出内容"></a> 场景二：我需要拿到命令的输出内容</h3><p>这是写脚本时最常见的需求。比如你想用 Python 获取当前的系统时间或者某个配置项的值，你需要把终端打印的字存到 Python 的变量里。</p><pre class="line-numbers language-Python" data-language="Python"><code class="language-Python">import subprocess# 运行 echo 命令，并捕获输出result &#x3D; subprocess.run([&quot;echo&quot;, &quot;Hello from terminal!&quot;], capture_output&#x3D;True, text&#x3D;True)# 打印获取到的内容print(f&quot;终端说: &#123;result.stdout&#125;&quot;)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>核心要点：</strong> 这里加了两个非常关键的参数：</p><ol><li><p><strong>capture_output=True</strong>：告诉 Python，不要把结果打印到屏幕上，而是偷偷拦截下来存进 <code>result</code> 对象里。</p></li><li><p><strong>text=True</strong>：告诉 Python 将输出结果作为普通的 <strong>字符串（String）</strong> 处理。如果不加这个参数，你得到的是带有 <code>b'...'</code> 前缀的字节流（Bytes），处理起来会很麻烦。</p></li></ol><h3 id="场景三如果命令执行失败了怎么办"><a class="markdownIt-Anchor" href="#场景三如果命令执行失败了怎么办"></a> 场景三：如果命令执行失败了怎么办？</h3><p>写脚本最怕的就是静默失败。如果外部命令报错了，Python 脚本默认还会继续往下跑，这可能会导致灾难性的后果。</p><p><code>subprocess.run()</code> 返回的 <code>result</code> 对象里，有一个叫 <code>returncode</code> 的属性。<code>returncode == 0</code> 代表成功，非 0 代表出错。</p><p><strong>温和的处理方式（手动检查）：</strong></p><pre class="line-numbers language-Python" data-language="Python"><code class="language-Python">import subprocessresult &#x3D; subprocess.run([&quot;ls&quot;, &quot;&#x2F;nonexistent_folder&quot;], capture_output&#x3D;True, text&#x3D;True)if result.returncode &#x3D;&#x3D; 0:    print(&quot;命令执行成功！&quot;)else:    print(f&quot;糟糕，出错了！错误代码: &#123;result.returncode&#125;&quot;)    print(f&quot;错误信息: &#123;result.stderr&#125;&quot;) # 报错信息通常存放在 stderr 中<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>暴力的处理方式（直接让脚本崩溃报错）：</strong></p><p>如果你希望外部命令一旦失败，Python 脚本就立刻停止并抛出异常，只需加一个 <code>check=True</code> 参数：</p><pre class="line-numbers language-Python" data-language="Python"><code class="language-Python">import subprocess# 如果这个命令失败，Python 会直接抛出 CalledProcessError 异常subprocess.run([&quot;ls&quot;, &quot;&#x2F;nonexistent_folder&quot;], check&#x3D;True)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h3 id="总结"><a class="markdownIt-Anchor" href="#总结"></a> 总结</h3><p>如果偶尔写写脚本，把下面这段代码存成代码片段（Snippet），遇到需要调用外部命令的时候直接复制粘贴修改即可：</p><pre class="line-numbers language-Python" data-language="Python"><code class="language-Python">import subprocessdef run_command(cmd_list):    &quot;&quot;&quot;    运行外部命令的通用模板    &quot;&quot;&quot;    try:        # capture_output 捕获输出, text&#x3D;True 返回字符串, check&#x3D;True 失败时抛出异常        result &#x3D; subprocess.run(cmd_list, capture_output&#x3D;True, text&#x3D;True, check&#x3D;True)        return result.stdout.strip() # strip() 用来去掉结尾多余的换行符    except subprocess.CalledProcessError as e:        print(f&quot;命令执行失败: &#123;e&#125;&quot;)        print(f&quot;错误输出: &#123;e.stderr&#125;&quot;)        return None# 使用示例：output &#x3D; run_command([&quot;git&quot;, &quot;branch&quot;])if output:    print(f&quot;当前分支信息：\n&#123;output&#125;&quot;)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这就够用了！至于更复杂的管道符（<code>|</code>）交互或者后台异步执行，那是<code>subprocess.Popen</code> 的工作，等到真的需要写复杂的系统脚本时再去了解它也不迟。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="python" scheme="https://blog.godmao.top/categories/python/"/>
    
    
  </entry>
  
  <entry>
    <title>ARM-CMSIS简介</title>
    <link href="https://blog.godmao.top/posts/11120/"/>
    <id>https://blog.godmao.top/posts/11120/</id>
    <published>2026-03-08T06:37:03.000Z</published>
    <updated>2026-04-03T07:32:19.777Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><h2 id="1-cmsis是什么"><a class="markdownIt-Anchor" href="#1-cmsis是什么"></a> 1. CMSIS是什么</h2><p>相信使用keil的朋友对“CMSIS”这个词并不陌生，在我们初入门时，并没有深究这个词的含义，只是知道系统启动和这个有关。那他究竟是什么呢？</p><p><strong>CMSIS ( Cortex Microcontroller Software Interface Standard )，翻译过来是ARM Cortex™ 微控制器软件接口标准</strong> 。简单来说，CMSIS就是 ARM 公司为了解决单片机（MCU）碎片化问题，而强行推行的一套“类 Linux 子系统框架”。</p><p>我们都知道，在程序设计中，上层直接依赖底层，代码耦合性非常的严重，底层代码一改，上层不知道要改多少地方，因此我们在程序设计中才会有依赖倒置原则。如果你了解程序设计中的依赖倒置原则，那理解ARM设计这套标准的初衷就不难了。</p><p>在早期的 ARM 单片机时代，ST（意法半导体）、NXP（恩智浦）、TI（德州仪器）等厂商都有自己的大爷脾气。大家用的都是 ARM Cortex-M 的内核，但每家定义的寄存器名字、写驱动的方式千奇百怪。代码换个芯片就得重写。所以 ARM 站出来制定了 CMSIS，也就相当于是接口层。</p><h2 id="2-cmsis的几个核心组件"><a class="markdownIt-Anchor" href="#2-cmsis的几个核心组件"></a> 2. CMSIS的几个核心组件</h2><h3 id="21-cmsis-core"><a class="markdownIt-Anchor" href="#21-cmsis-core"></a> 2.1. CMSIS-CORE</h3><p>ARM 自己设计的 CPU 内核（比如 Cortex-M3/M4/M7）里面带的东西，比如中断控制器（NVIC）、系统滴答定时器（SysTick）、MPU（内存保护单元）以及一些核心寄存器。因为这是 ARM 亲生的，所以 ARM 直接提供写好的 C 语言头文件和内联汇编。你调用的 <code>NVIC_EnableIRQ()</code>，在任何厂家的 Cortex-M 芯片上都是一模一样的，因为所有厂家都依赖它。</p><h3 id="22-device-header-files"><a class="markdownIt-Anchor" href="#22-device-header-files"></a> 2.2. Device Header Files</h3><p>ARM 规定了一个<strong>命名规范</strong>，强制要求各家厂商统一数据结构的长相。所有芯片厂商按照这个格式来定义自己外设（UART、SPI、I2C）的寄存器地址和位掩码。比如强制要求叫 <code>UART1-&gt;DR</code>，而不能叫 <code>UART1_DATA_REG</code>。</p><h3 id="23-cmsis-driver"><a class="markdownIt-Anchor" href="#23-cmsis-driver"></a> 2.3. CMSIS-Driver</h3><p>驱动接口层。这是一个<strong>纯粹的接口标准（API 定义）</strong>。ARM 定义了一套结构体，里面全都是函数指针。比如对于串口，它规定必须有 <code>Initialize</code>、<code>Send</code>、<code>Receive</code> 这几个接口。芯片厂商（如 ST）需要提供底层代码去填充这些函数指针。而上层则直接调用这些接口。</p><h3 id="24-cmsis-rtos"><a class="markdownIt-Anchor" href="#24-cmsis-rtos"></a> 2.4. CMSIS-RTOS</h3><p>这个很简单，由于每种 OS 的创建线程、信号量 API 都不一样。CMSIS-RTOS 提供了一层封装（Wrapper）。 如果你用 CMSIS-RTOS提供的API，不管你底层真正跑的是 FreeRTOS 还是 Keil RTX，你的应用代码都不用改。</p><h2 id="3-使用cmsis来进行开发"><a class="markdownIt-Anchor" href="#3-使用cmsis来进行开发"></a> 3. 使用CMSIS来进行开发</h2><p>上面的介绍可能带给你一点启发。既然对于所有采用ARMCortex内核的芯片，都支持CMSIS，那我直接用CMSIS提供的接口来进行开发，岂不是直接实现代码大一统？直接无缝切换mcu，代码依然能运行？</p><p>理想很丰满，但现实很骨感。如果代码完全不依赖具体硬件，那客户今天用 ST，明天嫌贵就可以无缝切换到其他厂商。像 ST 这样的巨头，为了维持客户粘性，虽然名义上支持 CMSIS-Driver，但他们更主推自己搞的 STM32 HAL 库。<br />所以在实际单片机开发中，<strong>CMSIS-CORE</strong> 是百分之百会用到的（大家都认），但 <strong>CMSIS-Driver</strong> 不一定每一家都完全支持或者好用，很多开发者依然会直接调用厂商提供的专属驱动库。</p><p>当然你也可以尝试使用CMSIS来进行开发，也很简单，这里贴一篇文章可以参考：<br /><a href="https://blog.csdn.net/yyz_1987/article/details/130731368?spm=1001.2014.3001.5502">还在老一套？STM32使用新KEIL5的IDE，全新开发模式RTE介绍及使用_keil rte-CSDN博客</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="嵌入式" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/"/>
    
    <category term="程序架构" scheme="https://blog.godmao.top/categories/%E5%B5%8C%E5%85%A5%E5%BC%8F/%E7%A8%8B%E5%BA%8F%E6%9E%B6%E6%9E%84/"/>
    
    
  </entry>
  
  <entry>
    <title>Linux异步通知</title>
    <link href="https://blog.godmao.top/posts/50845/"/>
    <id>https://blog.godmao.top/posts/50845/</id>
    <published>2025-12-19T15:13:42.000Z</published>
    <updated>2025-12-19T16:23:45.762Z</updated>
    
    <content type="html"><![CDATA[<details><summary>版权信息</summary><div class="admonition warning"><p class="admonition-title">warning</p><p>本文章为博主原创文章。遵循 <a href="https://creativecommons.org/licenses/by-sa/4.0/deed.zh-hans">CC 4.0 BY-SA</a> 版权协议，转载请附上原文出处链接和本声明。</p></div></details><hr /><p>Linux 异步通知机制（Asynchronous Notification）是 Linux 设备驱动中一个非常经典且实用的概念。</p><p>简单来说，它的核心思想是 <strong>“被动接收，而非主动查询”</strong>。</p><h2 id="1-什么是异步通知机制"><a class="markdownIt-Anchor" href="#1-什么是异步通知机制"></a> 1. 什么是异步通知机制</h2><p>想象一下你在钓鱼，这时候你有两种方式知道鱼上钩了：</p><ol><li><p><strong>轮询（Polling）</strong>：你每隔 5 秒钟拉起鱼竿看一看有没有鱼。这很累，而且如果你在看书，看书会被不断打断。</p></li><li><p><strong>异步通知（Asynchronous）</strong>：你在鱼竿上挂个铃铛，然后你就安心看书。当鱼上钩拉动鱼竿时，铃铛响了（<strong>信号</strong>），你再放下书去收杆。</p></li></ol><p>在 Linux 中，异步通知机制是基于 <strong>信号（Signal）</strong> 实现的。</p><ul><li><p><strong>非阻塞 I/O</strong>：应用程序需要不断循环读取（轮询），浪费 CPU。</p></li><li><p><strong>异步通知</strong>：应用程序告诉内核驱动：“如果有数据了，发个信号（通常是 <code>SIGIO</code>）告诉我。” 驱动程序在数据就绪时，主动向应用程序发送信号，应用程序暂停当前工作，进入信号处理函数处理数据。</p></li></ul><h2 id="2-异步通知的优缺点"><a class="markdownIt-Anchor" href="#2-异步通知的优缺点"></a> 2. 异步通知的优缺点</h2><h3 id="21-优点"><a class="markdownIt-Anchor" href="#21-优点"></a> 2.1. 优点</h3><ul><li><strong>节省 CPU 资源</strong>：应用程序不需要死循环轮询（Polling），CPU 可以处理其他任务或进入休眠。</li><li><strong>响应及时</strong>：数据一来，驱动立马发信号，比轮询的时间片机制反应更快。</li><li><strong>逻辑解耦</strong>：应用程序可以专心做主逻辑，只有在 I/O 事件发生时才被打断。</li></ul><h3 id="22-缺点"><a class="markdownIt-Anchor" href="#22-缺点"></a> 2.2. 缺点</h3><ul><li><strong>编程复杂度增加</strong>：需要处理信号并发、重入等问题。</li><li><strong>不适合高频数据</strong>：如果数据来得太快（比如网络包狂轰滥炸），不断触发信号中断会让 CPU 把时间都花在上下文切换上（类似“中断风暴”），此时轮询反而更高效。</li></ul><h2 id="3-理解异步通知机制"><a class="markdownIt-Anchor" href="#3-理解异步通知机制"></a> 3. 理解异步通知机制</h2><p>我们从用户态开始入手，看在使用异步通知时，内核里实际发生了什么。<br />首先我们需要了解三个核心API：</p><h3 id="31-fcntl"><a class="markdownIt-Anchor" href="#31-fcntl"></a> 3.1. <code>fcntl()</code></h3><p>它定义在 <code>&lt;linux/fcntl.h&gt;</code>。是<strong>用户态</strong>用来“控制已打开文件描述符行为”的系统调用。<br />它<strong>不负责读写数据</strong>，而是用来：</p><ul><li>改文件描述符的<strong>状态</strong></li><li>设置 <strong>进程 / 信号 / 锁</strong> 等属性</li></ul><p>函数原型：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;fcntl.h></span></span><span class="token keyword">int</span> <span class="token function">fcntl</span><span class="token punctuation">(</span><span class="token keyword">int</span> fd<span class="token punctuation">,</span> <span class="token keyword">int</span> cmd<span class="token punctuation">,</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> <span class="token comment">/* arg */</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><ul><li><code>fd</code>：已经打开的文件描述符</li><li><code>cmd</code>：你要干什么</li><li><code>arg</code>：参数（根据 cmd 不同）</li></ul><p>fcntl主要是操作 <code>struct file</code> 这个结构体，如下：</p><pre class="line-numbers language-none"><code class="language-none">用户态 fcntl()   ↓sys_fcntl()   ↓VFS   ↓struct file ├─ f_flags   ← O_NONBLOCK &#x2F; O_ASYNC ├─ f_owner   ← SIGIO 接收者 └─ f_op<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>四大类用法：</p><p><em><strong>1. F_GETFL / F_SETFL</strong></em></p><p>🟢作用：读取 / 修改 <code>file-&gt;f_flags</code></p><p>🟢常见标志位：</p><table><thead><tr><th>标志</th><th>作用</th></tr></thead><tbody><tr><td><code>O_NONBLOCK</code></td><td>非阻塞 I/O</td></tr><tr><td><code>O_ASYNC</code></td><td>启用异步通知（SIGIO）</td></tr><tr><td><code>O_APPEND</code></td><td>追加写</td></tr><tr><td><code>O_DIRECT</code></td><td>直接 I/O</td></tr></tbody></table><p>🟢用法：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">int</span> flags <span class="token operator">=</span> <span class="token function">fcntl</span><span class="token punctuation">(</span>fd<span class="token punctuation">,</span> F_GETFL<span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token function">fcntl</span><span class="token punctuation">(</span>fd<span class="token punctuation">,</span> F_SETFL<span class="token punctuation">,</span> flags <span class="token operator">|</span> O_NONBLOCK <span class="token operator">|</span> O_ASYNC<span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><blockquote><p><strong>不能直接 F_SETFL 一个新值</strong>，否则你会把原有 flags 覆盖掉。</p></blockquote><p><em><strong>2. F_SETOWN / F_GETOWN</strong></em><br />👉 <strong>异步通知必须用</strong></p><p>🟢作用：指定信号发给谁 修改 <code>file-&gt;f_owner.pid = pid</code></p><p>🟢用法：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token function">fcntl</span><span class="token punctuation">(</span>fd<span class="token punctuation">,</span> F_SETOWN<span class="token punctuation">,</span> <span class="token function">getpid</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><em><strong>3. F_DUPFD / F_DUPFD_CLOEXEC</strong></em></p><p>🟢作用：复制文件描述符</p><p>🟢用法：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">int</span> newfd <span class="token operator">=</span> <span class="token function">fcntl</span><span class="token punctuation">(</span>fd<span class="token punctuation">,</span> F_DUPFD<span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><em><strong>4. 文件锁</strong></em></p><p>用于防止多个进程同时操作资源。现在不深入。</p><h3 id="32-fasync_helper"><a class="markdownIt-Anchor" href="#32-fasync_helper"></a> 3.2. <code>fasync_helper()</code></h3><p>这是在驱动层使用的。<code>fasync_helper()</code> 用来维护“需要被异步通知的进程列表”。<br />它做的事只有一个核心目标——<br /><strong>哪个进程打开了设备并设置了 O_ASYNC，就把那个进程记下来。</strong></p><p>函数原型：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">int</span> <span class="token function">fasync_helper</span><span class="token punctuation">(</span><span class="token keyword">int</span> fd<span class="token punctuation">,</span>                  <span class="token keyword">struct</span> <span class="token class-name">file</span> <span class="token operator">*</span>file<span class="token punctuation">,</span>                  <span class="token keyword">int</span> on<span class="token punctuation">,</span>                  <span class="token keyword">struct</span> <span class="token class-name">fasync_struct</span> <span class="token operator">*</span><span class="token operator">*</span>fapp<span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><ul><li><p><code>fd</code>：</p><ul><li>用户态传进来的文件描述符</li><li><strong>只是标识用途</strong></li><li>内核真正关心的是 <code>file</code><br />可以把它当附带信息</li></ul></li><li><p><code>file</code>：</p><ul><li>对应用户态 <code>open()</code> 返回的 fd</li><li>即fcntl操作的 <code>struct file</code><ul><li><code>f_owner</code>（信号接收者）</li><li><code>f_flags</code>（是否 O_ASYNC）<br />异步通知最终是基于 file 的</li></ul></li></ul></li><li><p><code>on</code>：</p><ul><li><code>on != 0</code> 启用异步（加入队列）</li><li><code>on == 0</code> 关闭异步（移除队列）</li></ul></li><li><p><code>fapp</code>：<br />需要被异步通知的进程链表的链表头。</p></li></ul><p>函数逻辑伪代码：</p><pre class="line-numbers language-none"><code class="language-none">if (on) &#123;    &#x2F;&#x2F; 开启异步    if (file 不在 fapp 链表中) &#123;        分配 fasync_struct        设置 file &#x2F; f_owner        插入 fapp 链表    &#125;&#125; else &#123;    &#x2F;&#x2F; 关闭异步    if (file 在 fapp 链表中) &#123;        从链表删除        释放 fasync_struct    &#125;&#125;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="33-kill_fasync"><a class="markdownIt-Anchor" href="#33-kill_fasync"></a> 3.3. <code>kill_fasync()</code></h3><p>在驱动层使用。向应用程序发出信号的核心函数。</p><p>原型：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">void</span> <span class="token function">kill_fasync</span><span class="token punctuation">(</span><span class="token keyword">struct</span> <span class="token class-name">fasync_struct</span> <span class="token operator">*</span><span class="token operator">*</span>fp<span class="token punctuation">,</span> <span class="token keyword">int</span> sig<span class="token punctuation">,</span> <span class="token keyword">int</span> band<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><code>fp</code>：要操作的 fasync_struct。</li><li><code>sig</code>：要发送的信号。</li><li><code>band</code>：可读时设置为 POLL_IN，可写时设置为 POLL_OUT。</li></ul><h3 id="34-工作流程"><a class="markdownIt-Anchor" href="#34-工作流程"></a> 3.4. 工作流程</h3><ol><li>在用户态打开设备：</li></ol><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">int</span> fd <span class="token operator">=</span> <span class="token function">open</span><span class="token punctuation">(</span><span class="token string">"/dev/key"</span><span class="token punctuation">,</span> O_RDWR<span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>此步在 VFS 中创建 <code>struct file</code>，没启用异步。</p><ol start="2"><li>用户态登记进程PID：</li></ol><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token function">fcntl</span><span class="token punctuation">(</span>fd<span class="token punctuation">,</span> F_SETOWN<span class="token punctuation">,</span> <span class="token function">getpid</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>内核找到 <code>struct file</code>，把 <code>file-&gt;f_owner.pid</code> 设置为当前进程 PID，以后这个 file 触发信号，就发给这个进程。</p><ol start="3"><li>用户态请求开启异步通知：</li></ol><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">int</span> flags <span class="token operator">=</span> <span class="token function">fcntl</span><span class="token punctuation">(</span>fd<span class="token punctuation">,</span> F_GETFL<span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token function">fcntl</span><span class="token punctuation">(</span>fd<span class="token punctuation">,</span> F_SETFL<span class="token punctuation">,</span> flags <span class="token operator">|</span> O_ASYNC<span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p><strong>此步会触发驱动的 fasync 回调函数。</strong></p><ol start="4"><li>驱动必须实现 fasync 这个fop：</li></ol><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token keyword">static</span> <span class="token keyword">int</span> <span class="token function">key_fasync</span><span class="token punctuation">(</span><span class="token keyword">int</span> fd<span class="token punctuation">,</span> <span class="token keyword">struct</span> <span class="token class-name">file</span> <span class="token operator">*</span>file<span class="token punctuation">,</span> <span class="token keyword">int</span> on<span class="token punctuation">)</span><span class="token punctuation">&#123;</span>    <span class="token keyword">struct</span> <span class="token class-name">key_dev</span> <span class="token operator">*</span>kdev <span class="token operator">=</span> file<span class="token operator">-></span>private_data<span class="token punctuation">;</span>    <span class="token keyword">return</span> <span class="token function">fasync_helper</span><span class="token punctuation">(</span>fd<span class="token punctuation">,</span> file<span class="token punctuation">,</span> on<span class="token punctuation">,</span> <span class="token operator">&amp;</span>kdev<span class="token operator">-></span>async_queue<span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>fasync_helper：把当前进程封装成 <code>struct fasync_struct</code> 并加入 <code>kdev-&gt;async_queue</code>。</p><p>just like this：</p><pre class="line-numbers language-none"><code class="language-none">key_dev -&gt; 设备资源结构体 └── async_queue      ├── 进程A (PID xxx)      ├── 进程B<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><ol start="5"><li>事件发生：<br />例如，按键中断触发</li></ol><pre class="line-numbers language-none"><code class="language-none">irq_handler()&#123;    key_value &#x3D; 1;    kill_fasync(&amp;kdev-&gt;async_queue, SIGIO, POLL_IN);&#125;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>kill_fasync：通知 async_queue 里的所有进程：有 I/O 事件发生了。</p><p>just like this：</p><pre class="line-numbers language-none"><code class="language-none">kill_fasync └── 遍历 async_queue      └── send_sigio()           └── send_sig_info()                └── 把 SIGIO 放入目标进程的 signal queue<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>注意：此函数只是把只是把信号“挂”到目标进程上。</p><ol start="6"><li>用户态必须注册信号处理函数：</li></ol><pre class="line-numbers language-c" data-language="c"><code class="language-c"><span class="token function">signal</span><span class="token punctuation">(</span>SIGIO<span class="token punctuation">,</span> sigio_handler<span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>当内核发现：</p><pre class="line-numbers language-c" data-language="c"><code class="language-c">current<span class="token operator">-></span>pending signal <span class="token operator">==</span> SIGIO<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>则<br />→ 保存当前上下文<br />→ 跳转到 sigio_handler()</p><h2 id="4-使用异步机制"><a class="markdownIt-Anchor" href="#4-使用异步机制"></a> 4. 使用异步机制</h2><h3 id="41-应用程序端-user-space"><a class="markdownIt-Anchor" href="#41-应用程序端-user-space"></a> 4.1. 应用程序端 (User Space)</h3><p>应用程序需要做三件事（简称“三板斧”）：</p><ol><li><strong>绑定信号处理函数</strong>：使用 <code>signal()</code> 让应用知道收到 <code>SIGIO</code> 信号后该干什么。</li><li><strong>设置属主</strong>：使用 <code>fcntl(fd, F_SETOWN, getpid())</code> 告诉内核：“这个设备文件的信号要发给当前进程”。</li><li><strong>开启异步标志</strong>：使用 <code>fcntl(fd, F_SETFL, flags | O_ASYNC)</code> 启用异步通知功能。</li></ol><h3 id="42-驱动程序端-kernel-space"><a class="markdownIt-Anchor" href="#42-驱动程序端-kernel-space"></a> 4.2. 驱动程序端 (Kernel Space)</h3><p>驱动需要处理数据结构 <code>fasync_struct</code> 并实现三个部分：</p><ol><li><strong>定义结构体</strong>：在设备结构体中定义 <code>struct fasync_struct *async_queue;</code>。</li><li><strong>实现 <code>.fasync</code> 接口</strong>：当应用调用 <code>fcntl</code> 设置 <code>O_ASYNC</code> 时，内核会调用驱动的 <code>.fasync</code> 函数。驱动里只需调用内核辅助函数 <code>fasync_helper</code>。</li><li><strong>发送信号</strong>：当设备数据就绪（如中断来了），调用 <code>kill_fasync</code> 发送信号。</li><li><strong>清理</strong>：在 <code>.release</code> 函数中把节点从异步队列中删除。</li></ol><h3 id="43-番外嵌入还是指针"><a class="markdownIt-Anchor" href="#43-番外嵌入还是指针"></a> 4.3. 番外：嵌入还是指针？</h3><p>在设备资源结构体中，<br /><code>struct fasync_struct async_queue</code> 用嵌入还是指针？</p><p>用指针。因为它是“外部管理对象”，你只是引用。它由 <code>fasync_helper</code> 管理。</p><p>—END—</p>]]></content>
    
    
      
      
    <summary type="html">&lt;details&gt;
&lt;summary&gt;版权信息&lt;/summary&gt;
&lt;div class=&quot;admonition warning&quot;&gt;&lt;p class=&quot;admonition-title&quot;&gt;warning&lt;/p&gt;&lt;p&gt;本文章为博主原创文章。遵循 &lt;a href=&quot;https://c</summary>
      
    
    
    
    <category term="Linux" scheme="https://blog.godmao.top/categories/Linux/"/>
    
    <category term="内核编程" scheme="https://blog.godmao.top/categories/Linux/%E5%86%85%E6%A0%B8%E7%BC%96%E7%A8%8B/"/>
    
    
  </entry>
  
</feed>
